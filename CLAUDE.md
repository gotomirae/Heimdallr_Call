# Heimdallr_Call 작업 지침

> **PRD: `docs/PRD.md`** — 구현 전에 해당 절을 반드시 읽는다
> **함정: `docs/traps.md`** — 새 모듈을 만들기 전에 반드시 훑는다 (전부 "에러 없이 조용히 틀리는" 것들)
> 설계 결정: `docs/decisions/` · 세션 기록: `docs/sessions/` · Phase 프롬프트: `docs/phases.md`
> 참고 프로젝트: `C:\Claude\dev\HermesCall` (별도 repo/Supabase/Vercel — **읽기 전용 참고**)

## 프로젝트 개요

KOSPI/KOSDAQ 시가총액 1,000억원 이상 약 1,300종목을 대상으로,
**분기실적이 실제로 가속되고 있으면서 그 사실이 아직 주가에 반영되지 않은 종목**을
매 분기 자동 발굴해 텔레그램(🛡️ 표시명 **아이언맨의 Heimdallr** · 봇 ID **8933940541**)과
전용 대시보드(https://heimdallr-call.vercel.app)로 전달한다.
종목명을 보내면 그 종목 리포트를 회신한다(`src/notify/listen.py`).
※ @Invest_EarningCallBot(8605695587)은 **HermesCall 것이다** — 2026-08-15에 분리했다.

**표기는 `Heimdallr`(m)로 통일한다.** 단, 봇 **username만 `@Heinmdallr_bot`**으로 남아 있다 —
텔레그램이 봇 username 변경을 지원하지 않기 때문이다(바꾸려면 새 봇 = 새 토큰 = 대화 단절).
**동작에는 무관하다**: 코드는 username을 어디서도 참조하지 않고(`src`·`dashboard`·`.github`
전수 0건), 봇 판정은 토큰에서 뽑은 **숫자 ID**로 한다(`bot_id_of`). 다시 고민하지 말 것.
로컬 폴더명은 **2026-08-17에 `Heinmdallr_Call` → `Heimdallr_Call`로 변경**했다. 코드는
폴더명에 의존하지 않지만(루트를 전부 `Path(__file__).parents[N]`로 계산) **venv는 의존한다** —
폴더를 옮기거나 이름을 바꿨으면 **`pip install -e ".[dev]"`를 반드시 다시 돌려라**(T57).
editable 설치가 절대경로를 박아둬 안 하면 `import src`가 **루트 밖에서만** 깨진다.

발굴만으로는 목적을 달성하지 못한다 — **좋은 기업과 좋은 투자는 다르다.**

## 되돌리면 안 되는 설계 결정 (ADR)

1. **게이트는 "실적 가속화"다. 컨센서스 서프라이즈가 아니다.**
   코스닥 1,819사 중 1,089사(59.9%)가 최근 1년 증권사 리포트 0건(에프앤가이드, 2026-06).
   서프라이즈를 필수 조건으로 두면 발굴 가치가 가장 큰 커버리지 공백 구간이 통째로 탈락한다.
   서프라이즈는 스코어 C축(15점) 가점일 뿐이다.

2. **컨센서스가 없는 종목의 C축은 0점이 아니라 "분모에서 제외 후 정규화"다.**
   `score_norm = raw / (100 − 미측정축_배점) × 100`
   0점 처리하면 위 편향이 그대로 되살아나 이 시스템의 존재 이유가 사라진다.
   SC6(분기 상위 20 중 `has_consensus=false` 비율 ≥ 30%)으로 상시 감시한다.

3. **선별 단계에 LLM을 쓰지 않는다.** DART가 구조화 재무를 무료로 준다.
   LLM은 게이트 통과 상위 종목의 **해석**에만 쓴다. 여기가 비용 설계의 전부다.

4. **LLM 입력에 공시 원문 전체를 넣지 않는다.** 숫자는 이미 정확히 DB에 있다.
   구조화 표 + 발췌 2,000자, 총 5,000토큰 이내. 시스템 프롬프트에 종목별 내용을 넣으면
   Prompt Caching이 통째로 깨진다.

5. **주가반영도(PRI)를 스코어에 합산하지 않는다.** 펀더멘털 강도와 주가 반영도는 다른 축이고,
   한 숫자로 뭉개면 둘 다 못 읽는다. 2축 매트릭스(★○△·)로 병기한다.

6. **모든 테이블의 앵커는 `code`(6자리)다. watchlist 개념은 없다.**
   대상이 "등록된 종목"이 아니라 "시총 하한을 넘는 전 종목"이기 때문이다.

7. **QoQ를 점수에 쓰지 않는다.** 한국 기업은 계절성이 강해 QoQ는 계절적으로 좋은 분기에
   발표하는 기업을 구조적으로 유리하게 만든다. 대신 **TTM(4분기 누적) 추세**를 쓴다.
   QoQ 원자료는 저장·표시하되 점수에는 넣지 않는다.

8. **Supabase는 HermesCall과 분리된 신규 프로젝트다.** 스키마·부하 성격·장애 전파가 모두 다르다.

## 절대 금지

- **`setWebhook` 호출 금지.** 봇을 분리한 뒤(2026-08-15)에도 **영구 차단**이다.
  이 프로젝트는 웹훅이 필요 없고(`getUpdates` 폴링), 잘못 부르면 봇당 1개뿐인 웹훅이
  **에러 없이 덮어써져** 남의 봇 명령어가 조용히 죽는다.
- **공유 봇에서 `getUpdates` 금지.** 수신은 읽기가 아니라 **소비**다 — offset을 확정하면
  상대에게 다시 오지 않아 HermesCall 명령어가 "가끔 씹히는" 형태로 망가진다(T44).
  `TelegramClient`가 전용 봇(`HERMESCALL_BOT_ID`와 다름)인지 검사해 안에서 막는다.
  **발송은 공유 봇으로도 안전하다** — 분리 전에도 알림은 계속 나간다.

- **KIS 주문 API 호출 금지.** 시세 조회 엔드포인트 화이트리스트(`KIS_ALLOWED_PATHS`)
  밖은 호출하지 않는다. 이 프로젝트는 매매를 하지 않는다.

- **`os.environ`으로 시크릿 직접 읽기 금지.** 반드시 `src/utils/env.py`의
  `require_env`/`optional_env`(내부 `.strip()`). 값에 개행이 섞여 LLM 호출이 6일간
  조용히 실패한 사고가 참고 프로젝트에서 있었다.

- **KIND 상장법인목록을 `lxml`로 파싱 금지.** 코스닥 1,839행 중 1,282행에서 예외도 경고도
  없이 잘라낸다(30% 유실). `html.parser`를 쓰고 원문 `<tr>` 수와 대조하라.
  다른 collector는 lxml이 정상이니 **이 소스만 예외다.**

- **단위를 못 읽으면 추측해서 곱하지 말 것.** 그 항목만 건너뛰고 화면에 밝힌다.

- **부호가 바뀌는 구간(적자↔흑자)에서 성장률 % 계산 금지.** `None` + `status_label`
  (`'흑전'|'적전'|'적자축소'|'적자확대'`).

- **새 서드파티 임포트를 `pyproject.toml`에 선언하지 않고 쓰기 금지.**
  로컬 venv에 수동 설치돼 있으면 로컬은 통과하고 GitHub Actions에서만 죽는다(참고 프로젝트 3회 재발).
  검증: `python -m venv /tmp/x && /tmp/x/bin/pip install -e .` 후 실제 경로 1회 실행.

- **PostgREST 조회에서 `range()` 페이징 없이 1,000행 초과 테이블 읽기 금지.**
  `.limit(5000)`을 줘도 1,000행만 온다. 잘려나가는 건 하위 소형주 — 발굴 대상이 정확히 그 구간이다.

- **Anthropic 가격에 날짜 기준 전환 로직 금지.** Sonnet 5는 $2/$10이 정가로 확정됐고
  2026-09-01 인상은 시행되지 않는다.

## 급소 — 여기를 틀리면 전부가 조용히 틀린다

**DART 정기보고서 누적치 분해** (`src/finance/quarterize.py`)
- 1분기보고서(11013): `thstrm_amount` = 3개월 단독
- **반기보고서(11012): 6개월 누적 → `Q2 = 반기 − Q1`**
- 3분기보고서(11014): 3개월 단독 + 9개월 누적 둘 다 제공
- **사업보고서(11011): 12개월 누적 → `Q4 = 연간 − 3Q누적`**
- `thstrm_amount`(당기)와 `thstrm_add_amount`(당기누적)를 **반드시 구분**하라.
- 이 모듈은 **테스트를 먼저 쓴다.** `tests/test_quarterize.py`가 이 프로젝트에서 가장 중요한 테스트다.

## 코딩 컨벤션

- Python: snake_case 모듈/함수, PascalCase 클래스, import 순서(표준→서드파티→내부)
- 파일 상단에 `# PRD Ref: §{절}`, 핵심 로직에 `# SC: {성공기준}` 주석
- **순수 함수(`screener/`, `finance/quarterize.py`)는 외부 I/O 금지** — 테스트 가능성이 이 프로젝트의 방어선
- **임계값·배점은 전부 `src/config/constants.py` 한 곳에만.** 참고 프로젝트는 딥분석 문턱이
  3곳에 흩어져 조용히 어긋난 적이 있다.
- 환경변수: UPPER_SNAKE_CASE, `.env.example` 갱신 필수
- 결측은 `None`, 판정 불가도 `None`. **`False`와 `None`을 반드시 구분**한다.

## 테스트

```bash
python -m pytest tests/
```
- collector 테스트는 실제 외부 API를 호출한다(모킹하지 않음). **replay 우선 검증 원칙** —
  새 collector는 라이브 이벤트를 기다리지 말고 과거 데이터를 재생해 먼저 검증한다.
- 순수 함수는 손계산 대조를 주석에 남긴다.

## "완료" 선언 규칙

구현이 끝났다고 말하기 전에 **실제로 돌려서 확인한 수치**를 제시한다.
테스트 통과는 증거가 아니다 — 참고 프로젝트에서 가장 값비쌌던 실수(lxml 30% 유실,
단위 혼용, PostgREST 1,000행 절단)는 전부 에러 없이 품질만 나빠지는 종류였다.

## 세션 운영

- **한 세션 = 한 Phase.** 넘길 때마다 `/clear`. 컨텍스트가 커지면 위 규칙이 조용히 무시된다.
- 세션 종료 시:
  1. `docs/sessions/YYYY-MM-DD.md`에 상세 기록(한 일 / 실측 수치 / 막힌 것 / 다음 세션이 알 것)
  2. 새로 발견한 함정은 `docs/traps.md`에 추가 — **"무엇이 어떻게 조용히 틀리는가"** 포함
  3. 이 파일에는 아래 진행 상황에 **3줄 이내**만 추가. **250줄 초과 시 가장 오래된 항목 삭제**
  4. 되돌리면 안 되는 결정은 `docs/decisions/NNN-제목.md`

## 세션 진행 상황

- 신규 함정 **T74 ★★**: 시즌 화면이 **"재무를 수집했는가"로 "발표했는가"를 판정**했다.
  `disclosures`를 불러왔지만 캘린더에만 쓰고 판정에는 안 썼다 → **609종목(대한항공·
  리노공업 등)이 미발표로 표시**. 진행률 43.4% → 실제 **98.1%**. 세 상태로 분리:
  재무완료 482 / 공시만 609 / 진짜 미발표 21. **진행률은 공시 기준**으로 잰다.
  `earnings_disclosures.fiscal_quarter`는 전 행 null → **보고서명**에서 분기를 뽑는다
  (공시일 추정은 분기 경계에서 틀린다).
- 신규 함정 **T75**: 손으로 적은 지식에 **숫자를 섞지 마라.** 섹터 이벤트·지표는
  산업 지식이라 손으로 적되(`sectorPlaybook.ts`) **전망값은 실측에서 계산**한다
  (`projectSector`). 섹터명은 글자까지 같아야 한다 — 오타면 조용히 기본값이 나온다.
- **화면 정리 (2026-08-17)** — 시즌 미발표 표에 **섹터** 적용(발굴목록과 동일 기준) ·
  결과추적 전망을 **섹터별 매출·영업이익 범위 + 이벤트·지표·주의점**으로 교체.
- 신규 함정 **T54 ★**: 검사기는 **반드시 걸려야 하는 입력으로 먼저 검증하라** — 조용히 무력화된다.
- **LLM 분석 범위 확대 (2026-08-17 · A′+B)** — 게이트 통과 **238종목 전부**가 대상.
  실측 단가 캐시히트 **$0.0315** · 미스 $0.0363(절감 13%뿐 — 비용의 대부분이 출력)
  → 분기 **$7.51** · 월 실링 $8 → **$12**(63%). 전체 1,112종목은 $35로 4.4배라 보류.
- 신규 함정 **T72 ★★**: **발송 기준(등급 ★/○)과 분석 기준(스코어 75+)이 갈라져** 있었다.
  등급은 스코어×반영도 교차 판정이라 "○인데 스코어 74.9"가 빠진다 → 실측 **발송 70종목 중
  22종목이 해석 없이 알림만** 나갔고, 하필 **반영도가 낮은**(롯데케미칼 2.7·GKL 1.8) 쪽이었다.
  → 선정에 **발송 등급을 무조건 포함**. 같은 대상을 가리켜야 하는 두 기준은 코드로 묶어라.
- 신규 함정 **T73 ★**: LLM 배치는 **한 워크플로에서 안 끝난다**(호출당 36~56초 · 238종목
  2.4~3.7시간 vs timeout 15분) → `--max-seconds`로 **스스로 끊고** 멱등 누적.
  `disclosure_poll` 8분(신규 발표 우선) + 신규 `llm_batch.yml` 밤 3시간(따라잡기).
  `cancel-in-progress: false` 필수 — 겹치면 같은 종목에 돈이 두 번 나간다.
- **섹터를 DDL 없이 (2026-08-17)** — `industry`·`products`로 **읽는 시점에** 분류한다.
  규칙은 `sector_map.py` → `export_constants.py` → `constants.json` → `lib/sector.ts`
  (파이썬 단일 출처, TS 재정의 금지 · 테스트가 막는다). 실측 KRX 업종명 대신 반도체·
  기계·로봇·화학·소재로 표시됨. **새 컬럼 전에 "기존 컬럼으로 계산되나"를 먼저 물어라**(T71).
- **화면 정리 (2026-08-17)** — 종목코드 열 삭제 · 설명을 서술형 → **표·나열형**으로
  (게이트 G1/G2/G3 3줄, 하단 정의 5줄). 결과추적 note도 한 줄씩으로.
- **발표일 시점 현황** — D+5 292건 · D+20 **2건**(7/18 이전 발표가 2건뿐이고 그 2건은
  이미 측정됨 — 나머지는 거래일 부족) · D+60 0건. **D−5·당일은 DDL 4컬럼 필요.**
- **탭 통합·LLM 강화 (2026-08-17)** — 발굴목록+스크리너를 **한 화면**으로(게이트 필터).
  LLM 배치 `src/analysis/batch.py`(매력도 순 상위 N · 연속 호출로 캐시 히트) ·
  스키마에 **원인·결과·전망**(`earnings_change`) 추가 · 트리거에 영향도·성격.
  결과추적에 **섹터별 실적 + 다음 분기 전망**. 용어 설명은 기본 용어 삭제.
- 신규 함정 **T69 ★**: LLM 호출이 띄엄띄엄이면 **캐시를 매번 놓친다**(실측 4건 중 3건 미스).
  TTL 5분 → **연속 호출** 필수. `DAILY_ANALYSIS_LIMIT` 20은 66종목을 4일로 쪼개 매일
  첫 건이 미스였다 → **80**. 비용은 상한이 아니라 **월 실링**이 정한다(≈295종목/월).
- 신규 함정 **T70**: 스키마에 필수 필드를 더하면 **테스트 픽스처가 낡는다**(이번엔 테스트가
  잡았다). `required`를 스키마에서 파생시키고, **읽는 쪽**은 구 스키마 행에 null을 줘야 한다.
- **LLM 비용 실측** — 캐시히트 $0.0271 · 미스 $0.0346/건. 분기 100종목 ≈ **$2.71**(실링의 34%),
  238종목 ≈ $6.46(81%). 기본 `--top 100` · 스코어 75+ 대상 66종목.
- 신규 함정 **T36 ★**: 스크리너가 분기를 고정하면 **먼저 발표한 종목의 최신 실적이 통째로
  무시된다**. `--quarter latest` 기본 · **미래 분기 행**도 걸러라(비12월 결산).
- **실측 월 4,700분** — private 한도 2,000분의 2.4배. **repo를 public으로 둘 것**(PRD §10.1).
- **P11 대기 워크플로에 스케줄을 걸지 않았다.** 빨간 X가 일상이 되면 진짜 고장을 못 알아본다.
  "성공했는데 아무것도 안 하는" 잡은 더 나쁘다 → 수동 실행 + 모듈 없으면 명시적 실패.
- **P11 결과추적 완료 (2026-08-16)** — `outcome_tracking` **459행**(2026.2Q) ·
  `src/analysis/outcome.py`(순수) + `outcome_run.py` + `notify/promotion.py` +
  대시보드 `/outcome`. 대기 워크플로 2개 **스케줄 활성화**. tests 393 passed.
- **P11 실측**: D+1 459건 · D+5 292건 · **D+20 2건**(시즌이 2주 전에 끝나 거래일 부족).
  D+5 등급별 ★ +4.99(9) · ○ −0.68(18) · ✕ −4.20(16). **A축 IC가 두 시점 모두 최고**
  (D+1 +0.112 · D+5 +0.097) → ADR 1이 지금까지는 지지된다(한 시즌뿐, 확언 불가).
- **알림 간소화 · 대시보드 필터 (2026-08-16)** — 텔레그램 1,405자 → **535~653자**
  (서술형 기업설명 + 실적·스코어 한 덩어리 + PRI 반영/미반영 + PER 2개).
  분기추이·컨센 표는 대시보드 전용. 대시보드 3화면 모두 **게이트 통과분만**
  (매트릭스 1,108→**344점**, 목록 344행, 결과추적 459→193건). tests 392.
- **공시 폴링 15분 → 30분** (사용자 요청) — 월 4,700 → **4,108분**.
- **대시보드 7화면 전부 완료 (2026-08-16)** — `/screener`(전수·탈락사유 표시) ·
  `/season`(2026.2Q 43.3% · 481/1,112) · `/settings`(임계값 읽기전용 + 비용).
  `src/config/export_constants.py`로 `constants.py` → JSON 생성(단일 출처 유지).
  tests 403 passed.
- 신규 함정 **T51 ★**: **RLS는 에러가 아니라 빈 배열을 준다** → **0행을 보면 권한부터 의심하라.**
- **GitHub public 배포 완료 (2026-08-17)** — `gotomirae/Heimdallr_Call` · Secrets 8 ·
  `SEASON_MODE=off`. 실행 검증: outcome_update `459행` · disclosure_poll `1,111행` ·
  telegram_listen `봇 8933940541 전용봇 True` · ci 39초. **cron 7개 가동 중 — 로컬 `--watch` 금지**(T44).
- 신규 함정 **T53 ★**: dev 의존성이 없으면 테스트가 **실패가 아니라 skip**된다 →
  **기준선(passed+skipped 둘 다)과 대조하라.** passed 수만 보면 커버리지 소실을 못 본다.
- **LLM 정기 분석 ON (2026-08-17)** — `telegram_listen` cron이 `--analyze`를 못 받아
  **LLM 호출 0회**였던 것을 켰다(실증: `LLM 분석 ON` / `-f analyze=false` → `OFF`).
  상한은 코드가 건다(`check_budget` 월 $8·일 20건, 재질의 재사용 0원). timeout 5→12분.
- 신규 함정 **T55 ★**: GitHub 삼항 `${{ x && 'A' || 'B' }}`는 x=false에도 B를 준다.
  schedule엔 `inputs`가 없어 늘 B다 — **양방향으로** 조용히 무시된다. 분기는 셸로.
- **배포 완료 (2026-08-17)** — https://heimdallr-call.vercel.app · 미인증 200(보호 없음) ·
  `/api/cost` `available:true $0.0991`(DB 일치) · 7화면 실데이터 렌더. tests 413.
  **전 구성요소 실행 검증 완료 — 남은 건 시간뿐**(D+20·D+60 표본, 11월 `SEASON_MODE=on`).
- 신규 함정 **T56 ★**: 배포 URL을 **이름에서 추론하지 마라** — 틀려도 링크만 죽는다. `curl`로 확정.
- **폴더 rename 수습 (2026-08-17)** — `Heinmdallr_Call`→`Heimdallr_Call`로 **T57이 실제 발동**.
  `pip install -e ".[dev]"` 재실행 후 **루트 밖 `import src`**로 검증(루트 안·pytest는 깨진 채로도
  통과한다). tests **413 passed, 1 skipped** · 대시보드 빌드 10라우트 · 배포·cron 전부 정상.
- ★ **실적 가속의 정의 확정 (2026-08-17)** — 매출 YoY **와** 영업이익 YoY가 **둘 다 가속**.
  G2에 `op_yoy(t) > op_yoy(t−1) AND op_yoy(t) > 0` 추가(G1과 대칭). **흑전은 통과 인정**,
  전분기 성장률 결측은 **판정 불가(None)**. 실측 통과 **343 → 238** · 발송 **80 → 67** (T60).
- 신규 함정 **T59 ★★**: `dynamic = "force-dynamic"`은 **fetch 캐시를 막지 못한다.**
  supabase-js 응답이 `.next/cache/fetch-cache`에 디스크로 남고 **Vercel이 배포 사이에 복원**해
  배포 사이트가 이틀 전 숫자(80)를 200·MISS로 태연히 보여줬다 → 모든 `createClient`에
  `NO_STORE_OPTIONS`. **검증은 캐시를 지우고 하지 말 것 — 남은 채로 최신이어야 고쳐진 것이다.**
- 신규 함정 **T58 ★**: DART `dsab007/main.do?textCrpNm=`은 **검색을 실행하지 않는다**(빈 화면).
  원문은 `dsaf001/main.do?rcpNo=` 뿐이다 — `earnings_disclosures.rcept_no`를 쓰고,
  없으면 링크를 만들지 않는다(1,322 중 231종목). 링크 조립은 `notify/links.py`·`lib/links.ts`만.
- **화면 개편 (2026-08-17)** — 발굴목록: 섹터·종목명·등급·분기·스코어·반영도·시총·최근5일,
  헤더 고정 + **등급 순 정렬**(★25○42△18·126✕27). 상세: 차트 **9분기** + 영업이익 YoY 라인(주인공)
  + 값 라벨 + 주가, 밸류에이션은 **최근4분기 → 선행** 순(후행 PER 삭제), 용어집(`lib/glossary.ts`).
- **E2E 실측 (2026-08-17 · 042700 한미반도체 ★)** — 공시→재무→게이트→스코어→LLM→텔레그램
  →대시보드 전 구간 확인. G1 −65.5%→+39.5% · G2 −87.9%→+51.0% · 스코어 89.4 · PRI 16.5.
  LLM $0.0342 · 텔레그램 발송 ✓ · 화면·알림 선행PER 50.1배로 **일치**.
- 신규 함정 **T61 ★**: LLM이 `tool_use` **값 안쪽에** 닫는 태그와 다음 필드를 흘려 넣는다.
- 신규 함정 **T62 ★**: 새 컬럼 하나로 `price_run`이 PGRST204 크래시 → 같은 잡의 **스크리닝까지**
- 신규 함정 **T63**: cron이 `screener --save`를 하루 4회 돈다 → **로직 변경 후 로컬 저장 전에 푸시.**
- **DDL 적용·수집 완료 (2026-08-17)** — `quarter_prices` 3,069행(238종목, 실패 0) ·
  `price_snapshots` 1,112행 + `ret_5d` 1,111종목(KIS 실패 0). 재계산 후 ★27 ○43(발송 70).
  발굴목록 최근5일 **238/238** · 상세 차트 주가 **10/10**.
- 신규 함정 **T64 ★**: `overflow-x-auto`만으로는 **`sticky top-0`이 안 먹는다** →
  `max-h-[70vh] overflow-auto` + `bg-*`·`z-20`. **스크롤해 보기 전엔 안 드러난다.**
- 신규 함정 **T65**: 차트 색을 설명 글에 손으로 적으면 어긋난다 → `SERIES_COLOR` 단일 출처(`lib/`).
- **차트 색 확정** — 영업이익 YoY **노랑** · 매출 YoY **녹색** · 주가 **빨간 점선**(현재까지)
  · TTM 매출 **분홍 점선**. 색은 `lib/chart.ts` `SERIES_COLOR` 단일 출처(T65).
- **투자 섹터 분류 (2026-08-17)** — `src/universe/sector_map.py`. KRX 업종명은 투자에 쓰는
  말이 아니다('특수 목적용 기계' 93종목에 반도체장비·디스플레이·건설기계가 섞임) →
  **products를 먼저** 보는 키워드 규칙 30섹터. 미분류 10.3%→**1.2%**. 개별 종목 하드코딩 금지(T68).
- **결과 추적 재설계 (2026-08-17)** — 시점을 **발표 전5일·당일·후5·20·60일**로(음수 컬럼은 `dm5`).
  IC·SC6/SC8·측정건수 표는 제거. **회고 분석 + 이번 시즌 전략**이 화면 최상단이다.
- 신규 함정 **T66 ★★**: 집계 1위가 **음수인데 "가장 컸다"**고 말했다 — **부호를 안 본 것이 버그.**
- 신규 함정 **T67 ★**: 표본 2건 결론도 **문장은 완벽하게 그럴듯하다** → `MIN_SAMPLE=8` + 근거등급.
