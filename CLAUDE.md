# Heimdallr_Call 작업 지침

> **PRD: `docs/PRD.md`** — 구현 전에 해당 절을 반드시 읽는다
> **함정: `docs/traps.md`** — 새 모듈을 만들기 전에 반드시 훑는다 (전부 "에러 없이 조용히 틀리는" 것들)
> 설계 결정: `docs/decisions/` · 세션 기록: `docs/sessions/` · Phase 프롬프트: `docs/phases.md`
> 참고 프로젝트: `C:\Claude\dev\HermesCall` (별도 repo/Supabase/Vercel — **읽기 전용 참고**)

## 프로젝트 개요

KOSPI/KOSDAQ 시가총액 1,000억원 이상 약 1,300종목을 대상으로,
**분기실적이 실제로 가속되고 있으면서 그 사실이 아직 주가에 반영되지 않은 종목**을
매 분기 자동 발굴해 텔레그램(🛡️ **@Heinmdallr_bot** · 봇 ID 8933940541)과 전용 대시보드로
전달한다. 종목명을 보내면 그 종목 리포트를 회신한다(`src/notify/listen.py`).
※ @Invest_EarningCallBot(8605695587)은 **HermesCall 것이다** — 2026-08-15에 분리했다.

발굴만으로는 목적을 달성하지 못한다 — **좋은 기업과 좋은 투자는 다르다.**

## 되돌리면 안 되는 설계 결정 (ADR)

1. **게이트는 "실적 가속화"다. 컨센서스 서프라이즈가 아니다.**
   코스닥 1,819사 중 1,089사(59.9%)가 최근 1년 증권사 리포트 0건(에프앤가이드, 2026-06).
   서프라이즈를 필수 조건으로 두면 발굴 가치가 가장 큰 커버리지 공백 구간이 통째로 탈락한다.
   서프라이즈는 스코어 C축(15점) 가점일 뿐이다.

2. **컨센서스가 없는 종목의 C축은 0점이 아니라 "분모에서 제외 후 정규화"다.**
   `score_norm = raw / (100 − 미측정축_배점) × 100`
   0점 처리하면 위 편향이 그대로 되살아나 이 시스템의 존재 이유가 사라진다.
   SC6(분기 상위 20 중 `has_consensus=false` 비율 ≥ 30%)으로 상시 감시한다.

3. **선별 단계에 LLM을 쓰지 않는다.** DART가 구조화 재무를 무료로 준다.
   LLM은 게이트 통과 상위 종목의 **해석**에만 쓴다. 여기가 비용 설계의 전부다.

4. **LLM 입력에 공시 원문 전체를 넣지 않는다.** 숫자는 이미 정확히 DB에 있다.
   구조화 표 + 발췌 2,000자, 총 5,000토큰 이내. 시스템 프롬프트에 종목별 내용을 넣으면
   Prompt Caching이 통째로 깨진다.

5. **주가반영도(PRI)를 스코어에 합산하지 않는다.** 펀더멘털 강도와 주가 반영도는 다른 축이고,
   한 숫자로 뭉개면 둘 다 못 읽는다. 2축 매트릭스(★○△·)로 병기한다.

6. **모든 테이블의 앵커는 `code`(6자리)다. watchlist 개념은 없다.**
   대상이 "등록된 종목"이 아니라 "시총 하한을 넘는 전 종목"이기 때문이다.

7. **QoQ를 점수에 쓰지 않는다.** 한국 기업은 계절성이 강해 QoQ는 계절적으로 좋은 분기에
   발표하는 기업을 구조적으로 유리하게 만든다. 대신 **TTM(4분기 누적) 추세**를 쓴다.
   QoQ 원자료는 저장·표시하되 점수에는 넣지 않는다.

8. **Supabase는 HermesCall과 분리된 신규 프로젝트다.** 스키마·부하 성격·장애 전파가 모두 다르다.

## 절대 금지

- **`setWebhook` 호출 금지.** 봇을 분리한 뒤(2026-08-15)에도 **영구 차단**이다.
  이 프로젝트는 웹훅이 필요 없고(`getUpdates` 폴링), 잘못 부르면 봇당 1개뿐인 웹훅이
  **에러 없이 덮어써져** 남의 봇 명령어가 조용히 죽는다.
- **공유 봇에서 `getUpdates` 금지.** 수신은 읽기가 아니라 **소비**다 — offset을 확정하면
  상대에게 다시 오지 않아 HermesCall 명령어가 "가끔 씹히는" 형태로 망가진다(T44).
  `TelegramClient`가 전용 봇(`HERMESCALL_BOT_ID`와 다름)인지 검사해 안에서 막는다.
  **발송은 공유 봇으로도 안전하다** — 분리 전에도 알림은 계속 나간다.

- **KIS 주문 API 호출 금지.** 시세 조회 엔드포인트 화이트리스트(`KIS_ALLOWED_PATHS`)
  밖은 호출하지 않는다. 이 프로젝트는 매매를 하지 않는다.

- **`os.environ`으로 시크릿 직접 읽기 금지.** 반드시 `src/utils/env.py`의
  `require_env`/`optional_env`(내부 `.strip()`). 값에 개행이 섞여 LLM 호출이 6일간
  조용히 실패한 사고가 참고 프로젝트에서 있었다.

- **KIND 상장법인목록을 `lxml`로 파싱 금지.** 코스닥 1,839행 중 1,282행에서 예외도 경고도
  없이 잘라낸다(30% 유실). `html.parser`를 쓰고 원문 `<tr>` 수와 대조하라.
  다른 collector는 lxml이 정상이니 **이 소스만 예외다.**

- **단위를 못 읽으면 추측해서 곱하지 말 것.** 그 항목만 건너뛰고 화면에 밝힌다.

- **부호가 바뀌는 구간(적자↔흑자)에서 성장률 % 계산 금지.** `None` + `status_label`
  (`'흑전'|'적전'|'적자축소'|'적자확대'`).

- **새 서드파티 임포트를 `pyproject.toml`에 선언하지 않고 쓰기 금지.**
  로컬 venv에 수동 설치돼 있으면 로컬은 통과하고 GitHub Actions에서만 죽는다(참고 프로젝트 3회 재발).
  검증: `python -m venv /tmp/x && /tmp/x/bin/pip install -e .` 후 실제 경로 1회 실행.

- **PostgREST 조회에서 `range()` 페이징 없이 1,000행 초과 테이블 읽기 금지.**
  `.limit(5000)`을 줘도 1,000행만 온다. 잘려나가는 건 하위 소형주 — 발굴 대상이 정확히 그 구간이다.

- **Anthropic 가격에 날짜 기준 전환 로직 금지.** Sonnet 5는 $2/$10이 정가로 확정됐고
  2026-09-01 인상은 시행되지 않는다.

## 급소 — 여기를 틀리면 전부가 조용히 틀린다

**DART 정기보고서 누적치 분해** (`src/finance/quarterize.py`)
- 1분기보고서(11013): `thstrm_amount` = 3개월 단독
- **반기보고서(11012): 6개월 누적 → `Q2 = 반기 − Q1`**
- 3분기보고서(11014): 3개월 단독 + 9개월 누적 둘 다 제공
- **사업보고서(11011): 12개월 누적 → `Q4 = 연간 − 3Q누적`**
- `thstrm_amount`(당기)와 `thstrm_add_amount`(당기누적)를 **반드시 구분**하라.
- 이 모듈은 **테스트를 먼저 쓴다.** `tests/test_quarterize.py`가 이 프로젝트에서 가장 중요한 테스트다.

## 코딩 컨벤션

- Python: snake_case 모듈/함수, PascalCase 클래스, import 순서(표준→서드파티→내부)
- 파일 상단에 `# PRD Ref: §{절}`, 핵심 로직에 `# SC: {성공기준}` 주석
- **순수 함수(`screener/`, `finance/quarterize.py`)는 외부 I/O 금지** — 테스트 가능성이 이 프로젝트의 방어선
- **임계값·배점은 전부 `src/config/constants.py` 한 곳에만.** 참고 프로젝트는 딥분석 문턱이
  3곳에 흩어져 조용히 어긋난 적이 있다.
- 환경변수: UPPER_SNAKE_CASE, `.env.example` 갱신 필수
- 결측은 `None`, 판정 불가도 `None`. **`False`와 `None`을 반드시 구분**한다.

## 테스트

```bash
python -m pytest tests/
```
- collector 테스트는 실제 외부 API를 호출한다(모킹하지 않음). **replay 우선 검증 원칙** —
  새 collector는 라이브 이벤트를 기다리지 말고 과거 데이터를 재생해 먼저 검증한다.
- 순수 함수는 손계산 대조를 주석에 남긴다.

## "완료" 선언 규칙

구현이 끝났다고 말하기 전에 **실제로 돌려서 확인한 수치**를 제시한다.
테스트 통과는 증거가 아니다 — 참고 프로젝트에서 가장 값비쌌던 실수(lxml 30% 유실,
단위 혼용, PostgREST 1,000행 절단)는 전부 에러 없이 품질만 나빠지는 종류였다.

## 세션 운영

- **한 세션 = 한 Phase.** 넘길 때마다 `/clear`. 컨텍스트가 커지면 위 규칙이 조용히 무시된다.
- 세션 종료 시:
  1. `docs/sessions/YYYY-MM-DD.md`에 상세 기록(한 일 / 실측 수치 / 막힌 것 / 다음 세션이 알 것)
  2. 새로 발견한 함정은 `docs/traps.md`에 추가 — **"무엇이 어떻게 조용히 틀리는가"** 포함
  3. 이 파일에는 아래 진행 상황에 **3줄 이내**만 추가. **250줄 초과 시 가장 오래된 항목 삭제**
  4. 되돌리면 안 되는 결정은 `docs/decisions/NNN-제목.md`

## 세션 진행 상황

- 신규 함정 **T33 ★**: Sonnet 5는 `thinking` 생략 시 **사고가 켜지고** max_tokens를
  응답과 나눠 쓴다 → JSON이 잘린다. `thinking=adaptive` + `effort="low"` 고정.
- 신규 함정 **T34 ★**: 캐시 프리픽스가 **1,024토큰 미만이면 조용히 안 걸린다**(비용 10배).
  시스템 프롬프트 실측 1,497토큰. 캐시 범위는 `tools + system`(실측 3,242) —
  **도구를 바꾸면 캐시가 통째로 깨진다.**
- **P8 텔레그램 완료 (2026-08-13)** — `setWebhook`/`deleteWebhook`/`getUpdates` 클라이언트
  내부 차단 ✓ · 실발송 1건 ✓ · 2회차 **중복 차단** ✓(notifications 1행) · tests 244 passed.
  `dashboard/app/api/telegram/lookup` 미리 생성(미사용).
- **PRI 연결 · 텔레그램 표기 · 2026.2Q 반영 완료 (2026-08-15)** — 잠정 357행
  (삼성전자 2026.2Q 매출 1,714,995억) · 파생 10,072행 · 시세 **1,112행 KIS 실패 0** ·
  **PRI 1,103종목(분모 65)** · 등급 ★26·○55·△23··184·✕50(= 게이트 통과 338) · tests 262.
- **C축 배선 완료 (2026-08-15)** — 분모 **82가 72종목** / 67이 266종목 · 만점 7→5종목 ·
  삼성전자 손계산 대조(영업 +5.22%→3점, 매출 −1.36%→0점, 70/82=85.4) · tests 277.
  **SC6 정상**: 상위 20 중 컨센서스 없음 **19종목(95%)**, 중앙값 컨센있음 57.6 vs 없음 60.1.
- 신규 함정 **T37 ★**: 흑전 서프라이즈는 **어느 쪽으로 처리해도 0점**이 된다.
  %로 계산하면 −672%(→0점), T25대로 None을 주면 미측정(→9점 조용한 감점).
  라벨이 점수를 만들게 한다 — 게이트가 흑전을 G2 통과로 처리하는 것과 **같은 규칙**.
- 신규 함정 **T38**: 0점 항목을 메시지에서 숨기면 "측정해서 0점"과 "미측정"이 구분되지 않는다.
  전부 표시한다(`0/N` vs `미측정(-N)`).
- **매출 결측 복구 완료 (2026-08-15)** — 매출 None **166행 → 9행** · 전 분기 결측 종목
  **8 → 0** · 스크리너 대상 1,103 → **1,111** · 게이트 통과 338 → **344**. tests 281.
  (앞서 "219종목 누락"이라 적었던 것은 업종 제외 210과 혼동한 오기다. 실제는 29종목이었다.)
- 신규 함정 **T39 ★**: `fnlttMultiAcnt`는 회사에 따라 **매출 계정을 통째로 반환하지 않는다**
  (자산·부채·영업이익·순이익만). 에러가 아니라 그냥 없다 → 매출만 None이 되어 게이트가
  조용히 판정 불가. `영업이익은 있는데 매출이 없으면` `fnlttSinglAcntAll`로 폴백('영업수익').
  **폴백 응답에는 `fs_div` 필드가 없어** 주입하지 않으면 필터가 전 행을 걸러낸다.
- 신규 함정 **T40 ★**: `screen_results`는 분기 이력이 쌓이므로 **전체를 그냥 집계하면
  빈티지가 섞인다**(실측 게이트통과 511 vs 등급 344). **종목별 최신 1행으로 접어서** 읽어라.
  `--quarter` 고정 모드에서 미래분기 정리를 돌리면 **정상 최신 행이 통째로 삭제된다** → 건너뛴다.
- **P9 대시보드 완료 (2026-08-15)** — `/stock/[code]`·`/`·`/matrix` 3화면 브라우저 실검증.
  누락컬럼 **루프** 폴백(참고 프로젝트는 1회뿐) · `foldLatest()` · payload 필드단위 방어.
  차트 YoY 라인 좌표 253→27(226px 상승) · 매트릭스 1,108점 색상이 DB와 정확히 일치.
  **미완**: `/screener`·`/season`·`/outcome`·`/settings` · Vercel 배포(Protection 미확인).
- 신규 함정 **T41 ★**: `"use client"` 파일의 **비컴포넌트 export**를 서버가 import하면
  **빌드·tsc 통과 후 런타임 500**(`is not a function`). 변환 함수는 `lib/`에 둔다.
- 신규 함정 **T42**: Recharts 검증에서 셀렉터를 믿지 마라 — `ReferenceArea`는 `rect`가 아니라
  **`path`**, 진입 애니메이션은 rAF 없으면 **길이 0으로 멈춘다**(`isAnimationActive={false}`).
- 신규 함정 **T43**: dev 서버 켜둔 채 `npm run build` → 같은 `.next` 공유로 **청크 404**.
  페이지는 200이고 서버 로그엔 에러가 없어 앱 버그로 오인한다 → dev 재시작.
- 신규 함정 **T35 ★**: PRI는 P1(40점)이 지배적이라 **축 단위 정규화만으로 못 막는다.**
  `rel_ret_3m` 수집 전 전 종목이 분모 25(P2 단독)로 PRI를 받아 ★27·○88이 조용히 배정됐다
  → `PRI_MIN_DENOMINATOR=40` 미만이면 `pri=None`(0점 아님).
- 신규 함정 **T36 ★**: 스크리너가 분기를 고정하면 **먼저 발표한 종목의 최신 실적이 통째로
  무시된다**(357종목 넣어도 결과 무변화, 에러 없음) → `--quarter latest` 기본, 종목별 최신.
  비12월 결산 탓에 **미래 분기 행**도 섞인다(042520이 2026.3Q) → 끝난 분기까지만 고른다.
- **PER은 후행이다.** KIS `per`은 과거 12개월 EPS 기준 — 급가속 구간에서 과대평가된다
  (삼성전자 후행 41.8배 vs 최근 4분기 순이익 기준 10.6배, **3.9배**). 텔레그램에 둘 다 병기.
- 신규 함정 **T22 ★**: `corpCode.xml`을 정규식 하나로 훑으면 A사 corp_code에 B사 종목코드가
  붙는다(리노공업·HPSP 실제 오답). 매칭률로는 못 잡는다 → **DART 왕복 대조**로 검증.
- **T6 갱신**: `0126Z0` 같은 영숫자 종목코드를 버리지 마라(실체 기업 12곳, 9.98조 포함 소실).
- **텔레그램 봇 분리 완료 (2026-08-15)** — 공유 `8605695587` → 전용 **`8933940541`
  (@Heinmdallr_bot)**. `src/notify/listen.py` 수신 리스너: 종목명/코드를 보내면
  최신 분기 리포트 회신. 실측 "SK하이닉스" → `000660` exact 매칭 → 2026.2Q 발송 ✓.
  LLM은 **기존 분석 없을 때만** 호출(재질의해도 비용 0). tests 316 passed.
- 신규 함정 **T44 ★**: 공유 봇 `getUpdates`는 **상대 메시지를 가로채 소비**한다
  (offset 확정 시 서버에서 영구 삭제) → 전용 봇에서만 수신 허용. offset은 텔레그램이
  기억하므로 DB 저장 불필요, 단 **처리 후에** 확정해야 한다(먼저 확정하면 유실).
- **텔레그램 모바일 가독성 개편 (2026-08-15)** — `parse_mode="HTML"` + 숫자 표를 전부
  `<pre>`로. 잠정 우선·확정 시 전환·`confirmed_delta` 표기, 분기 추이 6분기,
  PRI **항목별 반영/미반영**, 밸류에이션에 분기별 PER 궤적(48.2→35.5→19.1→10.6)·F.PER 추가.
  실측 최대 줄폭 37칸 · 메시지 1,405자. tests 330 passed.
- 신규 함정 **T45 ★**: 텔레그램 본문은 **가변폭이라 공백 정렬이 안 된다**(`<pre>` 필수).
  `len()`으로 폭을 재면 한글이 절반이라 138칸 줄을 69자로 착각한다 → `display_width()`.
  HTML 모드에서 `<`는 태그로 해석돼 **발송이 400으로 죽는다** → `esc()`.
- **제품별 비중은 미수집.** `krx_universe.products`는 이름 나열뿐이고 비중은 DART
  사업보고서 §II '매출 실적' 표에 있다 → 별도 수집기 필요. 지금은 "미수집"이라 밝힌다.
- 신규 함정 **T46**: 롱폴링은 **HTTP 타임아웃이 대기 시간보다 길어야** 한다.
  25초 폴링에 30초 제한이라 정상 대기가 `ConnectTimeout`으로 찍혔다 →
  `대기+30초`로 늘리고 같은 오류는 한 번만 찍고 백오프(5→60초).
- **P10 GitHub Actions 완료 (2026-08-16)** — 워크플로 8개(ci·universe_daily·
  disclosure_poll·daily_digest·quarterly_backfill·telegram_listen + P11대기 2개).
  신규 `src/notify/batch.py`(--flash/--digest). 깨끗한 venv 설치·실행 검증 ✓ ·
  **시크릿 0개로 376 passed** · tests/test_workflows.py 41건.
- 신규 함정 **T47 ★**: 워크플로 주기는 **월 분(minute)을 계산하고** 정한다.
  telegram_listen 5분 주기가 월 8,640분(전체의 76%)이었다 → 15분·깨어있는시간으로
  월 2,040분. **Actions cron은 5~20분 밀려 챗봇 즉답용으로 부적합** — 즉답은 `--watch`.
- 신규 함정 **T48**: 테스트에서 상대경로(`Path(".github/...")`)를 쓰면 **실행 위치에 따라
  깨진다**. CI는 루트에서 돌아 영영 안 드러난다 → `Path(__file__).parents[1]`.
- **실측 월 4,700분** — private 한도 2,000분의 2.4배. **repo를 public으로 둘 것**(PRD §10.1).
- **P11 대기 워크플로에 스케줄을 걸지 않았다.** 빨간 X가 일상이 되면 진짜 고장을 못 알아본다.
  "성공했는데 아무것도 안 하는" 잡은 더 나쁘다 → 수동 실행 + 모듈 없으면 명시적 실패.
- **P11 결과추적 완료 (2026-08-16)** — `outcome_tracking` **459행**(2026.2Q) ·
  `src/analysis/outcome.py`(순수) + `outcome_run.py` + `notify/promotion.py` +
  대시보드 `/outcome`. 대기 워크플로 2개 **스케줄 활성화**. tests 393 passed.
- **P11 실측**: D+1 459건 · D+5 292건 · **D+20 2건**(시즌이 2주 전에 끝나 거래일 부족).
  D+5 등급별 ★ +4.99(9) · ○ −0.68(18) · ✕ −4.20(16). **A축 IC가 두 시점 모두 최고**
  (D+1 +0.112 · D+5 +0.097) → ADR 1이 지금까지는 지지된다(한 시즌뿐, 확언 불가).
- 신규 함정 **T49 ★**: 결과추적에서 **미측정을 0으로 채우면 "효과 없음"으로 보인다**.
  거래일로 셀 것(캘린더 아님) · 휴장일 발표는 다음 거래일 · 발표일은 **가장 이른 공시**.
  **D+1로 판단 금물** — ★가 D+1 −3.31인데 D+5 +4.99다(미반영은 반응이 늦다).
- 신규 함정 **T50**: 문자열 치환이 **엉뚱한 곳에 걸려도 출력은 그럴듯하다**.
  묶음 치환 후에는 grep으로 대상 라인을 직접 확인할 것.
- **알림 간소화 · 대시보드 필터 (2026-08-16)** — 텔레그램 1,405자 → **535~653자**
  (서술형 기업설명 + 실적·스코어 한 덩어리 + PRI 반영/미반영 + PER 2개).
  분기추이·컨센 표는 대시보드 전용. 대시보드 3화면 모두 **게이트 통과분만**
  (매트릭스 1,108→**344점**, 목록 344행, 결과추적 459→193건). tests 392.
- **공시 폴링 15분 → 30분** (사용자 요청) — 월 4,700 → **4,108분**.
- **대시보드 7화면 전부 완료 (2026-08-16)** — `/screener`(전수·탈락사유 표시) ·
  `/season`(2026.2Q 43.3% · 481/1,112) · `/settings`(임계값 읽기전용 + 비용).
  `src/config/export_constants.py`로 `constants.py` → JSON 생성(단일 출처 유지).
  tests 403 passed.
- 신규 함정 **T51 ★**: **RLS는 에러가 아니라 빈 배열을 준다.** `cost_log`는 anon 정책이
  없는데(의도) 그냥 읽어 "비용 $0"으로 조용히 표시됐다(실제 $0.0643) →
  `app/api/cost/route.ts`(service_role) 경유 + 실패 시 **0이 아니라 `—`와 사유**.
  **0행을 보면 권한부터 의심하라.**
- 신규 함정 **T52**: 미래 분기 **한 행**이 `/season`을 "2026.3Q · 0.1%"로 만들었다(T36 재발).
  **한 곳에서 막은 함정은 새 소비자가 생길 때마다 다시 뚫린다** — 날짜 상한을 다시 적용.
- **코딩 범위 완료, 배포만 남음 (2026-08-16)** — P0–P11 + 대시보드 7화면 전부 끝.
  GitHub public 전환 + Vercel 배포 절차를 단계별로 안내(코드 변경 없음,
  `docs/sessions/2026-08-16-dashboard-rest.md`의 "배포 절차 안내" 참조) — 실행은 사용자 확인 대기.
- **GitHub public 배포 완료 (2026-08-17)** — `gotomirae/Heimdallr_Call` · Secrets 8 ·
  `SEASON_MODE=off`. 실행 검증: outcome_update `459행` · disclosure_poll `1,111행` ·
  telegram_listen `봇 8933940541 전용봇 True` · ci 39초. **cron 7개 가동 중 — 로컬 `--watch` 금지**(T44).
- 신규 함정 **T53 ★**: dev 의존성이 없으면 테스트가 **실패가 아니라 skip**된다.
  `pyyaml` 누락으로 워크플로 검증 25건이 통째로 빠진 채 "378 passed"였다 →
  **기준선 `403 passed, 1 skipped`와 대조하라.** passed 수만 보면 커버리지 소실을 못 본다.
- 신규 함정 **T54 ★**: `git grep -c ... --cached | wc -l`은 옵션 위치 오류로 fatal이 나도
  **0**을 준다 → 시크릿 검사기가 "누출 0건"으로 조용히 무력화된다.
  **반드시 걸려야 하는 입력으로 검사기를 먼저 검증하라**(T51·T49와 같은 실패 모양).
