# Heimdallr_Call 작업 지침

> **PRD: `docs/PRD.md`** — 구현 전에 해당 절을 반드시 읽는다
> **함정: `docs/traps.md`** — 새 모듈을 만들기 전에 반드시 훑는다 (전부 "에러 없이 조용히 틀리는" 것들)
> 설계 결정: `docs/decisions/` · 세션 기록: `docs/sessions/` · Phase 프롬프트: `docs/phases.md`
> 참고 프로젝트: `C:\Codex\dev\HermesCall` (별도 repo/Supabase/Vercel — **읽기 전용 참고**)

## 프로젝트 개요

KOSPI/KOSDAQ 시가총액 1,000억원 이상 약 1,300종목을 대상으로,
**분기실적이 실제로 가속되고 있으면서 그 사실이 아직 주가에 반영되지 않은 종목**을
매 분기 자동 발굴해 텔레그램(🛡️ 표시명 **아이언맨의 Heimdallr** · 봇 ID **8933940541**)과
전용 대시보드(https://heimdallr-call.vercel.app)로 전달한다.
종목명을 보내면 그 종목 리포트를 회신한다(`src/notify/listen.py`).
※ @Invest_EarningCallBot(8605695587)은 **HermesCall 것이다** — 2026-08-15에 분리했다.

**표기는 `Heimdallr`(m)로 통일한다.** 단, 봇 **username만 `@Heinmdallr_bot`**으로 남아 있다 —
텔레그램이 봇 username 변경을 지원하지 않기 때문이다(바꾸려면 새 봇 = 새 토큰 = 대화 단절).
**동작에는 무관하다**: 코드는 username을 어디서도 참조하지 않고(`src`·`dashboard`·`.github`
전수 0건), 봇 판정은 토큰에서 뽑은 **숫자 ID**로 한다(`bot_id_of`). 다시 고민하지 말 것.
로컬 폴더명은 **2026-08-17에 `Heinmdallr_Call` → `Heimdallr_Call`로 변경**했다. 코드는
폴더명에 의존하지 않지만(루트를 전부 `Path(__file__).parents[N]`로 계산) **venv는 의존한다** —
폴더를 옮기거나 이름을 바꿨으면 **`pip install -e ".[dev]"`를 반드시 다시 돌려라**(T57).
editable 설치가 절대경로를 박아둬 안 하면 `import src`가 **루트 밖에서만** 깨진다.

발굴만으로는 목적을 달성하지 못한다 — **좋은 기업과 좋은 투자는 다르다.**

## 되돌리면 안 되는 설계 결정 (ADR)

1. **게이트는 "실적 가속화"다. 컨센서스 서프라이즈가 아니다.**
   코스닥 1,819사 중 1,089사(59.9%)가 최근 1년 증권사 리포트 0건(에프앤가이드, 2026-06).
   서프라이즈를 필수 조건으로 두면 발굴 가치가 가장 큰 커버리지 공백 구간이 통째로 탈락한다.
   서프라이즈는 스코어 C축(15점) 가점일 뿐이다.

2. **컨센서스가 없는 종목의 C축은 0점이 아니라 "분모에서 제외 후 정규화"다.**
   `score_norm = raw / (100 − 미측정축_배점) × 100`
   0점 처리하면 위 편향이 그대로 되살아나 이 시스템의 존재 이유가 사라진다.
   SC6(분기 상위 20 중 `has_consensus=false` 비율 ≥ 30%)으로 상시 감시한다.

3. **선별 단계에 LLM을 쓰지 않는다.** DART가 구조화 재무를 무료로 준다.
   LLM은 게이트 통과 상위 종목의 **해석**에만 쓴다. 여기가 비용 설계의 전부다.

4. **LLM 입력에 공시 원문 전체를 넣지 않는다.** 숫자는 이미 정확히 DB에 있다.
   구조화 표 + 절별 발췌만 싣는다. **길이는 `constants.py`가 정한다** —
   여기에 숫자를 적지 마라(적어 둔 "2,000자·5,000토큰"이 실제와 2배 어긋난 채
   방치됐다 · T97·T100). 시스템 프롬프트에 종목별 내용을 넣으면 캐싱이 통째로 깨진다.

5. **주가반영도(PRI)를 스코어에 합산하지 않는다.** 펀더멘털 강도와 주가 반영도는 다른 축이고,
   한 숫자로 뭉개면 둘 다 못 읽는다. 2축 매트릭스(★○△·)로 병기한다.

6. **모든 테이블의 앵커는 `code`(6자리)다. watchlist 개념은 없다.**
   대상이 "등록된 종목"이 아니라 "시총 하한을 넘는 전 종목"이기 때문이다.

7. **QoQ를 점수에 쓰지 않는다.** 한국 기업은 계절성이 강해 QoQ는 계절적으로 좋은 분기에
   발표하는 기업을 구조적으로 유리하게 만든다. 대신 **TTM(4분기 누적) 추세**를 쓴다.
   QoQ 원자료는 저장·표시하되 점수에는 넣지 않는다.

8. **Supabase는 HermesCall과 분리된 신규 프로젝트다.** 스키마·부하 성격·장애 전파가 모두 다르다.

9. **LLM Provider SDK를 분석 도메인에 직접 노출하지 않는다.** Canonical 요청·응답·usage
   계약 뒤에 격리하고, 실패 시 다른 Provider로 조용히 자동 폴백하지 않는다(ADR 9).

## 절대 금지

- **`setWebhook` 호출 금지.** 봇을 분리한 뒤(2026-08-15)에도 **영구 차단**이다.
  이 프로젝트는 웹훅이 필요 없고(`getUpdates` 폴링), 잘못 부르면 봇당 1개뿐인 웹훅이
  **에러 없이 덮어써져** 남의 봇 명령어가 조용히 죽는다.
- **공유 봇에서 `getUpdates` 금지.** 수신은 읽기가 아니라 **소비**다 — offset을 확정하면
  상대에게 다시 오지 않아 HermesCall 명령어가 "가끔 씹히는" 형태로 망가진다(T44).
  `TelegramClient`가 전용 봇(`HERMESCALL_BOT_ID`와 다름)인지 검사해 안에서 막는다.
  **발송은 공유 봇으로도 안전하다** — 분리 전에도 알림은 계속 나간다.

- **KIS 주문 API 호출 금지.** 시세 조회 엔드포인트 화이트리스트(`KIS_ALLOWED_PATHS`)
  밖은 호출하지 않는다. 이 프로젝트는 매매를 하지 않는다.

- **`os.environ`으로 시크릿 직접 읽기 금지.** 반드시 `src/utils/env.py`의
  `require_env`/`optional_env`(내부 `.strip()`). 값에 개행이 섞여 LLM 호출이 6일간
  조용히 실패한 사고가 참고 프로젝트에서 있었다.

- **KIND 상장법인목록을 `lxml`로 파싱 금지.** 코스닥 1,839행 중 1,282행에서 예외도 경고도
  없이 잘라낸다(30% 유실). `html.parser`를 쓰고 원문 `<tr>` 수와 대조하라.
  다른 collector는 lxml이 정상이니 **이 소스만 예외다.**

- **단위를 못 읽으면 추측해서 곱하지 말 것.** 그 항목만 건너뛰고 화면에 밝힌다.

- **부호가 바뀌는 구간(적자↔흑자)에서 성장률 % 계산 금지.** `None` + `status_label`
  (`'흑전'|'적전'|'적자축소'|'적자확대'`).

- **새 서드파티 임포트를 `pyproject.toml`에 선언하지 않고 쓰기 금지.**
  로컬 venv에 수동 설치돼 있으면 로컬은 통과하고 GitHub Actions에서만 죽는다(참고 프로젝트 3회 재발).
  검증: `python -m venv /tmp/x && /tmp/x/bin/pip install -e .` 후 실제 경로 1회 실행.

- **PostgREST 조회에서 `range()` 페이징 없이 1,000행 초과 테이블 읽기 금지.**
  `.limit(5000)`을 줘도 1,000행만 온다. 잘려나가는 건 하위 소형주 — 발굴 대상이 정확히 그 구간이다.

- **Anthropic 가격에 날짜 기준 전환 로직 금지.** Sonnet 5는 $2/$10이 정가로 확정됐고
  2026-09-01 인상은 시행되지 않는다.

## 급소 — 여기를 틀리면 전부가 조용히 틀린다

**DART 정기보고서 누적치 분해** (`src/finance/quarterize.py`)
- 1분기보고서(11013): `thstrm_amount` = 3개월 단독
- **반기보고서(11012): 6개월 누적 → `Q2 = 반기 − Q1`**
- 3분기보고서(11014): 3개월 단독 + 9개월 누적 둘 다 제공
- **사업보고서(11011): 12개월 누적 → `Q4 = 연간 − 3Q누적`**
- `thstrm_amount`(당기)와 `thstrm_add_amount`(당기누적)를 **반드시 구분**하라.
- 이 모듈은 **테스트를 먼저 쓴다.** `tests/test_quarterize.py`가 이 프로젝트에서 가장 중요한 테스트다.

## 코딩 컨벤션

- Python: snake_case 모듈/함수, PascalCase 클래스, import 순서(표준→서드파티→내부)
- 파일 상단에 `# PRD Ref: §{절}`, 핵심 로직에 `# SC: {성공기준}` 주석
- **순수 함수(`screener/`, `finance/quarterize.py`)는 외부 I/O 금지** — 테스트 가능성이 이 프로젝트의 방어선
- **임계값·배점은 전부 `src/config/constants.py` 한 곳에만.** 참고 프로젝트는 딥분석 문턱이
  3곳에 흩어져 조용히 어긋난 적이 있다.
- 환경변수: UPPER_SNAKE_CASE, `.env.example` 갱신 필수
- 결측은 `None`, 판정 불가도 `None`. **`False`와 `None`을 반드시 구분**한다.

## 테스트

```bash
python -m pytest tests/
```
- collector 테스트는 실제 외부 API를 호출한다(모킹하지 않음). **replay 우선 검증 원칙** —
  새 collector는 라이브 이벤트를 기다리지 말고 과거 데이터를 재생해 먼저 검증한다.
- 순수 함수는 손계산 대조를 주석에 남긴다.

## "완료" 선언 규칙

구현이 끝났다고 말하기 전에 **실제로 돌려서 확인한 수치**를 제시한다.
테스트 통과는 증거가 아니다 — 참고 프로젝트에서 가장 값비쌌던 실수(lxml 30% 유실,
단위 혼용, PostgREST 1,000행 절단)는 전부 에러 없이 품질만 나빠지는 종류였다.

## 세션 운영

- **한 세션 = 한 Phase.** 넘길 때마다 `/clear`. 컨텍스트가 커지면 위 규칙이 조용히 무시된다.
- 세션 종료 시:
  1. `docs/sessions/YYYY-MM-DD.md`에 상세 기록(한 일 / 실측 수치 / 막힌 것 / 다음 세션이 알 것)
  2. 새로 발견한 함정은 `docs/traps.md`에 추가 — **"무엇이 어떻게 조용히 틀리는가"** 포함
  3. 이 파일에는 아래 진행 상황에 **3줄 이내**만 추가. **250줄 초과 시 가장 오래된 항목 삭제**
  4. 되돌리면 안 되는 결정은 `docs/decisions/NNN-제목.md`

## 세션 진행 상황
- **수주 공시 확인 포인트 (2026-08-30)** — 목록 QoQ 대신 종목 상세에서 같은 분기 원문만 표시.
  실데이터 **454행→13건**(비공개 2·잘림 10), `수주상황` 제목까지 세면 45건 오탐(T127).
  QoQ·증가를 추론하지 않고 다음 정기보고서 재확인 신호로만 쓴다. **733 passed · build 성공**.
- ★★ **발송등급 88종목 완주 (2026-08-26, 사용자 지시)** — **88/88 · 실패 0 · $25.32/$27.**
  실링 $24→**$27**(실측 평균 $0.0622) · 일 상한 80→**120**(오늘 41건 소진분 때문에
  11건이 밀렸다 — 일 상한은 **배치 크기 조절이지 비용 방어가 아니다** · T83).
  ★ 신규 함정 **T103 ★★**: **검증기가 타입을 안 봤다.** `earnings_change`가
  `'{"cause":">skip'` 15자 **문자열**로 왔는데 `None`도 `""`도 아니라 통과 → `✓` 저장 →
  화면 빈칸. 스키마의 `type`과 **실제 타입을 대조**하게 고쳤고, 검증기 자신도
  `isinstance` 가드로 방어했다(터지면 '원인 불명 실패'가 되어 진단 정보가 사라진다).
  ★ 단건 재분석은 `run.py --code X --quarter 2026.2 --call --save` — `--refresh-before`를
    미래 날짜로 주면 **전체가 낡은 것으로 잡혀** 엉뚱한 종목이 재분석된다(실측 $0.3 낭비).
- ★★ **재분석이 같은 종목을 반복 결제할 뻔했다 (T102)** — `save()`가 upsert인데
  `created_at`을 안 넣어 **DB 기본값이 안 걸렸다**(INSERT 전용). `--refresh-before`가
  그 칸으로 판정하므로 **방금 돌린 종목이 계속 다시 대상**이 된다. → 직접 찍는다.
  `--refresh-before`에는 **오늘 날짜**를 준다(내일 날짜면 오늘 한 것까지 낡은 것이 된다).
  ★ **`| tee`가 죽으면 파이썬도 죽는다** — 파이프 버퍼가 차면 stdout 쓰기가 터진다.
    36건 돌던 배치가 exit 4로 죽은 원인이다. **로그는 리다이렉트로.**
- ★★ **발췌 파이프라인 점검 (2026-08-24)** — ① 공시 **1,576행 전부** 분기 칸이 NULL이라
  발췌가 어느 분기 것인지 몰랐다 — **폴백이 성공해 안 보였다**(T99). `period_of()` 신설 ·
  백필 1,154+452행 · **출처 라벨** 부착. ② 수집기 2,400 vs 분석기 2,000이 어긋나
  **438/453(97%)이 평균 513자씩** 잘렸고, 잘린 건 언제나 **`주요계약·연구개발` 절**(T100).
- ★★ **공시 원문 발췌를 LLM에 싣는다 (2026-08-23, 사용자 지시)** — `AnalysisInput.excerpt`가
  **항상 None**이라 모델은 숫자표만 보고 있었다(T93). `document.xml`로 정기보고서 4개 절을
  발췌한다(`dart_excerpt.py` · `excerpt_run.py`). `document.xml`은 **정기보고서에는 된다**
  (공정공시만 안 된다 · T94). ✓ `disclosure_excerpts` DDL 적용 완료.
- 신규 함정 **T95 ★★★**: **표의 셀 개행을 안 없애면 모델 출력 구조가 깨진다** —
  `earnings_change`가 **객체가 아니라 문자열**로 온다(strict 스키마 + 강제 tool_choice인데도).
  한 행 = 한 줄로 압축하면 정상. **지저분한 입력은 구조를 무너뜨린다.**
- 신규 함정 **T96 ★★**: **강제 `tool_choice`와 웹 서치는 함께 못 쓴다**(에러 없이 안 불린다).
  검색을 켤 때만 `auto`로 푼다. 서버 툴 오류는 **200 + 오류 블록**으로 온다.
- 신규 함정 **T97 ★★**: **PRD가 적어 둔 5,000토큰 상한을 코드가 검사한 적이 없다.**
  실제 입력은 발췌 전에도 이미 10,300토큰이었다 — 문서와 현실이 2배 어긋난 채 아무도
  몰랐다 → 14,000으로 정정하고 `analyze()`가 실제로 검사한다.
- 신규 함정 **T92 ★★★**: **`price_snapshots.per`는 직전 사업연도 EPS 기준 후행 PER이다.**
  실측 대조 — 고영 스냅샷 131.6 = 2025 연간 기준 130.8인데 **실제 TTM은 40.5**. 삼성전자 42.9 vs **10.9**.
  가속하면 분모가 1년 낡아 2~3배 부풀고, **이 시스템은 정확히 그런 종목만 고른다.**
  대시보드·텔레그램은 이미 재계산하는데 **LLM 입력만** `price` 딕셔너리를 통째로 덤프해
  후행 per을 넘겼다 → 본문이 "PER 102배"를 인용. `src/finance/valuation.py`로 통일하고
  입력에서 후행 per **제거**. 곁다리로 `analysis/run.py`에 **`np` 컬럼 누락**도 드러났다
  (새 TTM PER이 처음엔 전 종목 '계산 불가'였다 — `ttm_op`와 같은 모양).
- 신규 함정 **T93 ★★**: **"실적 발표는 트리거가 아니다"로 조이자 트리거가 0건이 됐다.**
  근본 원인은 프롬프트가 아니라 입력 — **`AnalysisInput.excerpt`가 항상 `None`**이라
  모델이 받는 건 숫자표뿐이다. CAPA 증설·신제품·고객사를 알 방법이 없다.
  숫자만 주고 사건을 쓰라면 지어내거나 비운다. **출력을 프롬프트로 밀기 전에
  입력에 그 정보가 있는지 확인하라.** 이벤트 트리거를 제대로 받으려면 공시 발췌를 실어야 한다.
- **종목 상세 개편 (2026-08-23, 사용자 지시)** — 차트 해설을 **사실 나열 → "이 모양이 뜻하는 것"**
  으로(영업 레버리지·기저효과·성장률과 절대금액의 구분까지 설명) · '주가 위치'에 **PER 2종 병기**
  (화면 계산값을 크게, 본문과 다르면 어느 쪽이 맞는지 명시) · **주가 시계열 원인**(`price_history`) 신설 ·
  실적 `cause`/`outlook`을 핵심 블록으로 격상 · 숫자·방향어 **자동 강조**(`Emphasized.tsx`).
- 신규 함정 **T91 ★★★**: **Anthropic tool 스키마는 `minItems`를 0과 1만 받는다.**
  트리거 하한으로 `minItems: 2`를 넣자 배치 호출이 **전부 400**으로 죽었다(3건 시도·3건 실패).
  파이썬 직렬화·테스트 555건이 **전부 초록불**이라 **실제 호출로만 드러난다** —
  밤 배치였으면 269종목 전부 실패하며 3시간을 날렸다. 개수 요구는 **스키마가 아니라
  프롬프트 문장**으로. 회귀 테스트가 `$defs`까지 훑어 막고, **검사기가 걸리는지도 따로 검증**했다(T54).
- 신규 함정 **T90 ★★**: **두 실행을 비교하면 로직 차이가 아니라 데이터 갱신분을 본다.**
  G4 도입 효과를 어제 실행(발송 107)과 오늘 실행(100)으로 재고 "G4가 7종목을 줄였다"고
  보고했는데, **동일 원자료로 G4만 켜고 끄니 100 → 100이었다.** 7은 그 사이 시세가
  갱신된 몫이다. 로직 효과는 **같은 입력에 두 로직**을 돌려서만 잴 수 있다.

- ★★ **G4(OPM YoY 상승) 게이트 추가 (2026-08-22, 사용자 지시)** — `F4_OPM_DELTA_MIN_PP`는
  **정의만 되고 안 쓰이던 죽은 상수**였다 → `G4_OPM_DELTA_MIN_PP = 0.0`(방향만 본다).
  **동일 원자료로 G4만 켜고 끈 실측**: 게이트 통과 297 → **269**(−28) ·
  발송 대상 100 → **100(변화 없음)**. OPM이 내려가면 B1(14점)이 0이라 스코어가 같이
  낮아진다 — G4의 효과는 **목록·매트릭스이지 알림이 아니다**.
- 신규 함정 **T85 ★★**: **읽는 쪽 키 이름이 달라 LLM 분석 4곳이 항상 비어 있었다.**
  시나리오 본문(`description` vs `condition`+`implication`) · 성장 엔진 · 틀릴 수 있는 이유 ·
  `price_position`(저장돼 있는데 화면에 아예 없었다). 데이터는 316행 중 310건 정상이었다 —
  **결측을 삼키는 방어 코드가 오타까지 삼켰다.** 스키마(`prompts.py`)와 `lib/analysis.ts`는 짝이다.
- 신규 함정 **T87 ★★**: 값은 `%`, **차이는 `%p`**다. 또 **표시 자릿수보다 작은 차이로
  판정이 뒤집혔다** → `EPSILON_PP = 0.05`. 보이는 것과 판정이 어긋나면 화면을 못 믿는다.
- 신규 함정 **T88 ★★**: **분기가 넘어가면 직전 분기 LLM 분석이 화면에서 사라진다**(63종목).
  → 없으면 최신 분석으로 물러서고 **그 분기 기준으로** 검증한다.
- 신규 함정 **T89 ★**: **한 곳만 고친 방어는 재발한다** — 부호 검사가 인사이트 ①에만 있었다.
- **화면 3종 개편 (2026-08-22, 사용자 지시)** — 발굴 목록: 정렬 고정 · 등급·섹터 복수 선택 ·
  **탭 복귀 시 상태 유지**. 종목 상세: LLM 분석 위치 이동 · **내러티브 검증** 신설.
  결과 추적: **시즌 결론** · **섹터별 매수 타이밍** · 섹터 클릭 → 발굴 목록 연동.

- 신규 함정 **T83 ★★**: **일 상한과 월 실링은 다른 것이다** — 일 상한은 내일 자동 해소되는데
  "실링 상향 후에"라고 안내했다. **틀린 안내는 사람을 틀린 행동으로 이끈다.**
- 신규 함정 **T82 ★★★**: **같은 상한이 다섯 곳에 있었고 넷이 서로 달랐다.**
  실링을 바꾸면 **constants · .env.example · PRD §7.3 · PRD 부록 · constants.json** 전부다.
  ① 워크플로가 env를 안 넘겨 저장소 변수가 **에러 없이 무시**됐다 → `vars.*` 배선.
  ② **PRD 대조가 `re.search`라 첫 매치만 봤다** → `findall` 전수 대조(T50 뿌리).
- **LLM Provider 경계 (2026-08-27)** — 기존 UI/payload와 Anthropic 기본 동작을 보존한 채
  OpenAI Responses Adapter 추가. 단가·모델 미설정은 호출 전 차단. **실호출·DB 쓰기 0건**.
  검증 **628 passed · 1 skipped · network 3 deselected**, Dashboard build 성공. ADR 9.
- **Provider offline eval (2026-08-28)** — 5축 결정론 평가·동일 사례집 강제(T105)·합성 replay **100/100**.
  전체 **641 passed · 1 skipped · network 3 deselected**. 실 Provider 비교·유료 호출·DB 쓰기 0건.
- **사례집 Stage B (2026-08-28)** — Supabase **27/30 GET**, 엘앤씨바이오·롯데정밀화학·한화시스템
  replay **3/3**(각 8분기·동분기 발췌·시크릿 0). DB/DART/Provider 쓰기 0; Windows temp ACL
  오염 방어(T118). **709 passed · 1 skipped · 3 deselected**; OpenAI Primary 보류.
- **실제 OpenAI 사례집 (2026-08-29)** — 대표성 ready=true인 4건 평가 평균 **84.64**, 품질 **0/4**.
  신규 3건 **$0.232626**, 모두 공시 단위 환산으로 저장 차단(93.57/80.71/72.14). grader 오탐
  4건은 T120으로 제거; Provider 비교·OpenAI Primary 전환은 보류. exact JSON 함정 T119.
- **Anthropic 실제 비교 (2026-08-29)** — 4건 **$0.263351**, 저장 성공 0/4(숫자 gate 3·상한 1),
  정정 평균 46.07 vs OpenAI 84.64. 롯데 숫자 자체감사는 $0.083343에 65→63.57·오류 6→8로
  악화돼 폐기·baseline hash 복구(T124). T122·T123 수정; Primary 전환 근거 없음.
- **대시보드 §9.1 완성 (2026-08-30)** — 수익률·백분위·EPS/FCF/Δ·밴드/PEG·섹터 상위5·
  종목별 결과·같은 분기 공시 토글·/screener 배선. 실데이터 상세 **200 · 157,412B**,
  전체 **737 passed · 1 skipped · network 3 deselected**, build 10/10. 외부 쓰기·배포 0.
- **사실 숫자 참조 3회 결론 (2026-08-30)** — low v2는 filler 11곳·**26.43점**, medium은
  참조값 3개 변조·unsupported 5건·**63.57점/$0.094465**. 모두 quality=false, 합계 $0.2328135,
  DB 쓰기 0 → 운영 기본에서 격리·canary 전용. T125·T126; **731 passed · 1 skipped · 3 deselected**.
- **대시보드 데이터 생산 운영 반영 (2026-08-30)** — ret/rel_ret 3·6·12M **각 1,145/1,145**,
  실패 0·1,601초. 스크리너 1,110행: flash 353·final 757·백분위 1,110·유효 delta 0(T128),
  SC6 11/20(55%). rel 6/12M DDL 적용; **747 passed**, build 10/10. 알림·유료 LLM 0. T129.
