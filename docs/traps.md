# Heimdallr_Call — 함정 목록

> **새 모듈을 만들기 전에 반드시 훑을 것.**
> 아래는 전부 **에러 없이 품질만 조용히 나빠지는** 종류다. 테스트 통과는 증거가 아니다.
> 출처 표기: `[HC]` = HermesCall에서 실측으로 확인된 사고 · `[설계]` = 설계 단계에서 예측된 위험
> 새 함정을 발견하면 **"무엇이 어떻게 조용히 틀리는가"**를 반드시 포함해 추가할 것.

---

## T1. DART 정기보고서 누적치 — **이 프로젝트의 급소** `[설계]`

DART 정기보고서는 보고서마다 누적 기준이 다르다.

| 보고서 | `reprt_code` | `thstrm_amount` 의미 | 분기 단독값 계산 |
|---|---|---|---|
| 1분기보고서 | 11013 | 3개월 단독 | 그대로 |
| **반기보고서** | 11012 | **6개월 누적** | **Q2 = 반기 − Q1** |
| 3분기보고서 | 11014 | 3개월 단독 (+ 9개월 누적도 제공) | 그대로 |
| **사업보고서** | 11011 | **12개월 누적** | **Q4 = 연간 − 3Q누적** |

- `thstrm_amount`(당기)와 `thstrm_add_amount`(당기누적)를 **반드시 구분**하라.
- 여기를 틀리면 **Q2/Q4의 YoY가 통째로 왜곡되는데 에러는 나지 않는다.**
- Q1이 없으면 Q2를 계산할 수 없다 → `None` 반환. 0으로 채우지 마라.
- → `src/finance/quarterize.py`로 분리하고 **테스트를 먼저 쓸 것.**
  `tests/test_quarterize.py`가 이 프로젝트에서 가장 중요한 테스트다.

**⚠️ 갱신 `[HD 2026-08-13 실측 — 위 표의 "반기 = 6개월 누적"은 이 엔드포인트에서 사실이 아니다]`**

`fnlttMultiAcnt.json`을 삼성전자(CFS)·리노공업(OFS)·에스티아이(CFS) 3사로 교차 확인한 결과:

| 보고서 | `thstrm_amount` | `thstrm_add_amount` |
|---|---|---|
| 1분기 11013 | 3개월 단독 | 3개월 누적(같은 값) |
| **반기 11012** | **3개월 단독(Q2)** | **6개월 누적** |
| 3분기 11014 | 3개월 단독(Q3) | 9개월 누적 |
| **사업 11011** | **12개월 연간** | **None (없음)** |

리노공업 2025 반기: `thstrm=112,522,064,123` / `add=190,932,307,676`,
1Q 누적 78,410,243,553 → `190,932,307,676 − 78,410,243,553 = 112,522,064,123` = `thstrm`.

**그래서 `thstrm_amount`를 분기값으로 그냥 쓰면 이 엔드포인트에서는 우연히 맞는다.**
그러나 회사·계정에 따라 다를 수 있으므로 신뢰의 근거로 삼지 않는다.
→ 분해는 **항상 누적(add_amount)의 차분**으로 하고, `thstrm_amount`는
   **교차검증에만** 쓴다(어긋나면 `standalone_mismatch`에 남겨 화면에 드러낸다).
→ 반기·3분기에 누적이 없으면 3개월인지 6개월인지 알 수 없다. **추측하지 말고 판정 불가**.

## T2. 연결(CFS) / 별도(OFS) 혼용 `[설계]`

종목별로 하나를 고정하고 `fs_div`에 기록한다. **분기마다 기준이 바뀌면 성장률이 조작된 것처럼 보인다.**
CFS 우선, 없으면 OFS.

## T3. 재작성(restated) 전년동기 `[설계]`

분할·합병·중단영업·회계기준 변경 시 전년동기가 재작성된다.
**공시에 실린 전년동기 값을 우선**하고, DB 저장값과 다르면 `restated=true`로 화면에 밝힌다.
조용히 예전 값을 쓰면 성장률이 실제와 달라진다.

## T4. 잠정 ≠ 확정 `[설계]`

잠정실적 공정공시 수치와 45일 뒤 확정치가 다를 수 있다.
`is_estimate` 플래그 필수, `delta_from_preliminary`에 변동 기록.
**차이 자체가 유용한 신호다** — 확정치가 잠정보다 나빠졌다면 경고다.

## T5. KIND 상장법인목록은 `html.parser`로 파싱 `[HC 2026-08-09 실측]`

`lxml`은 KOSDAQ 목록(823KB, 1,839행)에서 **1,282행에서 멈추고 예외도 경고도 내지 않는다.**
코스닥 상장사 557곳(30%)이 유니버스에서 사라지지만 아무 에러가 없어 알아채기 어렵다.

- 이 소스에만 `"html.parser"`를 쓴다. 다른 collector는 `lxml`이 정상이다.
- 원문 `<tr>` 개수와 파싱 행 수를 대조해 유실을 감지하고 **즉시 실패**시킨다.
- 회귀 방지: `tests/test_universe.py`가 코스닥 1,700개 이상을 강제한다.

## T6. KIND 응답의 종목코드 중복 `[HC 2026-08-09 실측 36건]`

같은 종목코드가 두 번 실려 오는 행이 있다(시알홀딩스 000480, 조선내화 462520 등이
완전히 동일한 내용으로 중복). code 기준 중복 제거를 안 하면 `krx_universe` PK 제약(23505)에
걸려 **캐시 저장이 통째로 실패**하고 매번 라이브 수집으로 되돌아간다
— 경고만 찍히고 분석은 계속돼 눈에 잘 안 띈다.

**⚠️ 갱신 `[HD 2026-08-13 실측 — 위 문장의 후반부는 틀렸다]`**

신규 상장 건에 `0126Z0`처럼 영문이 섞인 6자리 코드가 배정되는 것은 맞다.
그러나 **"시세 API에도 없다"는 전제는 사실이 아니다.** 실측 결과:

- 네이버 시총 API가 `itemCode='0126Z0'`으로 정상 제공한다(시총 9.98조).
- DART `corpCode.xml`에 영숫자 종목코드가 **56건** 있고,
  `company.json?corp_code=01965324` 조회도 정상이다(삼성에피스홀딩스).

이 코드를 버리면 **시총 1,000억 이상 실체 기업 12곳이 조용히 사라진다**
— 삼성에피스홀딩스 9.98조, 에임드바이오 1.75조 포함. 유니버스가 1,326 → 1,313으로
줄지만 아무 에러도 나지 않는다. KRX 종목코드 고갈로 신규 상장에는 계속 영숫자 코드가
배정되므로 시간이 갈수록 손실이 커진다.

→ 유효성 검사는 `code.isdigit()`이 아니라 **`len(code) == 6 and code.isalnum()`**.
   `krx_universe.code`가 TEXT라 저장에도 문제가 없다.
   회귀 방지: `tests/test_universe.py::test_alphanumeric_codes_are_kept`

## T7. PostgREST max-rows 1,000 `[HC 2026-08-09 실측]`

`.limit(5000)`을 줘도 **1,000행만 돌아온다**(저장 1,297행 → 조회 1,000행).
시총 내림차순으로 읽던 유니버스에서 잘려나간 건 하위 소형주 297개인데,
**이 시스템이 발굴하려는 대상이 정확히 그 구간**이라 에러 없이 품질만 나빠졌다.
1,000행을 넘길 수 있는 테이블은 반드시 `range()` 페이징.

## T8. 네이버 시총 페이징은 엄격한 내림차순이 아니다 `[HC 2026-08-09 실측]`

시총이 같은 억원대인 종목들 사이에서 순서가 흔들리는 지점이 3곳 발견됐다(915억/132억/107억 부근).
"하한 미만을 처음 본 순간"이 아니라 **그 페이지의 마지막 항목까지 하한 미만일 때**만 멈춰
최소 한 페이지 분량의 여유를 남긴다.

## T9. 시크릿에 개행이 섞인다 `[HC 2026-08-04 — 6일간 조용한 실패]`

값에 개행이 섞여 `httpcore.LocalProtocolError: Illegal header value b'***\n'`가 나고,
anthropic SDK가 그걸 `APIConnectionError("Connection error.")`로 감싸는 바람에
**모든 LLM 호출이 6일간 조용히 실패**했다.

`os.environ`을 직접 읽지 말고 반드시 `src/utils/env.py`의 `require_env`/`optional_env`
(내부에서 `.strip()`). 참고 프로젝트는 시크릿 오염이 반복돼 왔다
(`SUPABASE_URL`에 `/rest/v1`이 붙는 사고 3회 + 개행 1회).

## T10. 미선언 의존성 `[HC — 3회 재발]`

새 서드파티 임포트를 `pyproject.toml`에 선언하지 않으면, 로컬 venv에 수동 설치돼 있어
**로컬 테스트는 전부 통과하고 GitHub Actions의 깨끗한 `pip install -e .` 환경에서만 죽는다.**

`lxml`은 더 악질이었다 — 임포트 시점이 아니라 **실제로 파싱을 수행하는 순간**에만
`bs4.exceptions.FeatureNotFound`로 죽어서, 파싱할 대상이 없는 대다수 실행은 초록불로 끝났다.

검증: `python -m venv /tmp/x && /tmp/x/bin/pip install -e .` 후 실제 경로 1회 실행.
(`pip show <pkg>`의 `Required-by`가 비어 있으면 수동 설치된 것 = 선언 누락 신호)

## T11. 단위를 못 읽으면 추측해서 곱하지 말 것 `[HC 2026-08-06]`

잠정실적 공시 원문 표는 원/백만원/억원이 섞인다.
단위 파싱 실패 시 **그 항목만 건너뛰고** 그 사실을 화면에 밝힌다. 회사·분기마다 다르므로
종목별 고정 가정도 금물이다.

(DART API는 원 단위 정수라 이 문제가 없다 — L2' 규칙 파서·Haiku 폴백에만 해당)

## T12. 부호가 바뀌는 구간의 성장률 `[설계]`

적자→흑자, 흑자→적자에서 % 계산은 무의미하다.
분모가 0 또는 음수면 `None`을 반환하고 `op_status_label`에
`'흑전' | '적전' | '적자축소' | '적자확대'` 중 하나를 넣는다.

## T13. 텔레그램 웹훅 `[설계]`

**`setWebhook` 호출 금지.** 텔레그램은 봇당 웹훅 1개만 허용하고,
새로 등록하면 이전 것이 **에러 없이 덮어써진다.**
HermesCall 대시보드가 점유 중이라 등록 시 HermesCall 봇 명령어가 조용히 죽는다.
Heimdallr는 `sendMessage` 발송 전용.

발송 rate limit도 봇 토큰 공유로 함께 쓴다 — 연속 발송 1초 간격,
429의 `retry_after` 존중 백오프 3회.

## T14. 4Q의 특수성 `[설계]`

한국 기업 4Q는 일회성 비용(재고평가손·성과급·손상차손)이 집중되어 OPM이 구조적으로 낮고,
사업보고서 차감으로 산출되므로 **잔차에 모든 회계 조정이 몰린다.**
4Q 스코어에는 신뢰도 플래그를 붙이고, 4Q QoQ 관련 판정은 하지 않는다.

## T15. KIS API — 토큰 재발급 제한과 초당 유량 `[설계]`

- **접근토큰을 매 실행마다 새로 받으면 발급 자체가 막힌다.**
  `.cache/kis_token.json`에 만료시각과 함께 캐시하고 만료 전까지 재사용할 것.
  GitHub Actions 러너는 매번 초기화되므로 `actions/cache` 또는 Supabase에 보존해야 한다.
- **유량 실전계좌 초당 20건.** 1,300종목 순회 시 토큰버킷 스로틀러(안전 마진 18/초)가
  없으면 중간에 `EGW00201`로 죽는다.
- **응답 필드명을 추정으로 박지 말 것.** 실제 호출 1회로 확정한 뒤 코드에 넣는다.
- **주문 API 호출 금지.** 시세 조회 화이트리스트 밖은 클라이언트 내부에서 차단한다.

## T16. `.env`의 SUPABASE 값이 HermesCall 것일 수 있다 `[설계]`

프로젝트 폴더의 `.env.txt`에 이미 Supabase 값이 들어 있으나,
**Heimdallr는 신규 Supabase 프로젝트를 쓴다**(ADR 8).
값을 그대로 쓰면 HermesCall DB에 Heimdallr 테이블이 생성되어 분리 이유가 통째로 무너진다.
P0에서 `SUPABASE_URL`의 프로젝트 ref(`https://<ref>.supabase.co`)가 HermesCall과 다른지
**눈으로 대조**할 것.

**키 형식이 바뀌었다(2026 기준).** Supabase는 legacy JWT 키(`anon` / `service_role`)를
**2026년 말 deprecate** 예정이며, 신규 프로젝트는 `sb_publishable_...` / `sb_secret_...`를 쓴다.
- `SUPABASE_SERVICE_KEY` ← **secret 키**(`sb_secret_...`) — 서버사이드 전용, RLS 우회
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` ← **publishable 키**(`sb_publishable_...`) — 대시보드 읽기 전용
- 환경변수 **이름은 그대로 두고 값만 신규 형식**을 넣는다(코드 변경 불필요).
- publishable 키는 `anon`과 동일한 저권한이라 `schema.sql`의 `TO anon` RLS 정책이 그대로 동작한다.
  다만 **P0 검증 때 실제로 anon 조회가 되는지 1회 확인할 것** — 추측하지 말 것.
- ⚠️ `sb_secret_...`를 대시보드 프론트엔드 환경변수(`NEXT_PUBLIC_*`)에 절대 넣지 마라.
  RLS를 우회하므로 노출 시 전체 데이터가 열린다.

## T17. 컨센서스는 발표되면 사라진다 `[설계]`

FnGuide/네이버의 분기 컨센서스는 `(E)` 표기로 나오는데 실적이 발표되면 실적치로 덮여 사라진다.
**발표 후에 조회하면 이미 늦다.** 시즌 직전부터 주 1회 스냅샷해 `consensus_snapshots`에
쌓아야 F1(서프라이즈)이 성립한다.

## T18. 대시보드 — 부분 채움 JSONB와 미적용 컬럼 `[HC 2026-08-06 실제 발생]`

1. `analyses.payload`는 부분적으로만 채워질 수 있다. `payload && payload.scenarios.bull`처럼
   상위 객체만 확인하고 하위 필드를 읽으면 **페이지 전체가 500**이 난다. 필드 단위로 확인하라.
2. 새 컬럼을 추가하면 SQL Editor 적용 전까지 공백이 생기는데, 그 사이
   쓰기는 **PGRST204**로, 조회는 **42703**으로 각각 파이프라인과 페이지 전체를 죽인다.
   누락 컬럼을 감지해 제외하고 재조회하는 루프 패턴을 쓸 것
   (PostgREST는 누락 컬럼을 한 번에 하나씩만 알려주므로 루프가 필요하다).

## T19. Anthropic 가격에 날짜 기준 전환 로직 금지 `[2026-08-13 확인]`

HermesCall은 2026-09-01부터 Sonnet 5 단가를 $3/$15로 전환하도록 짜여 있으나,
Anthropic 공식 가격 페이지 확인 결과 **$2/$10이 정가로 확정됐고 인상은 시행되지 않는다.**
그대로 두면 비용이 50% 과대 계상되어 월 실링에 조기 도달한다.

## T20. GitHub Actions에서 `sleep` 루프 금지 `[설계]`

`sleep`도 과금된다. 긴 잡 하나보다 **짧은 잡을 여러 번** 띄우는 편이 싸다.
잡당 timeout 15분. 표준 러너는 잡당 최대 6시간이라는 별도 상한도 있다.

## T24. DART는 응답이 커지면 302로 에러 페이지에 보낸다 — **HTTP 200 + HTML로 돌아온다** `[HD 2026-08-13 실측]`

`fnlttMultiAcnt.json`에 corp_code 100개를 묶어 보내면 응답이 커져
`302 → https://opendart.fss.or.kr/error1.html`이 돌아온다.
`follow_redirects=True`(우리 `http_get`의 기본값)면 **최종 응답이 HTTP 200 · text/html**이라
**상태코드로는 절대 못 잡는다.**

- 스로틀링이 아니다. 30초 뒤 같은 요청도 똑같이 302다(실측). 재시도로는 안 풀린다.
- 같은 100개라도 회사 조합에 따라 되기도 한다(응답 크기 문제). 즉 **간헐적으로 보인다.**
- 실측 피해: 1,112종목 배치에서 **100종목이 3개 연도 × 4분기 내내 통째로 누락**
  (수집 성공 1,012사 / 저장 8,920행). 고친 뒤 1,112사 / 9,729행.
  `resp.json()`을 try/except로 감싸 빈 리스트를 돌려주는 흔한 처리를 했다면
  **"그 회사는 실적이 없다"와 구분되지 않아 영원히 못 찾는다.**

→ `content-type`에 `json`이 없으면 실패로 간주하고 **청크를 절반으로 쪼개 재귀 수집**한다.
  1개까지 쪼개도 실패하면 버리지 말고 `failed_batches`에 기록해 리포트에 드러낸다.
→ 배치 크기는 규격 상한 100이 아니라 `DART_MULTI_ACNT_BATCH_SIZE = 50`을 쓴다.

## T23. 장중에 유니버스를 갱신하면 경계 종목이 실행마다 바뀐다 `[HD 2026-08-13 실측]`

시총 하한(1,000억) 부근에 종목이 촘촘히 몰려 있다 — **1,000~1,100억 구간에만 57종목**.
장중(14시경) 30분 간격으로 두 번 돌린 결과 유니버스가 **1,326 → 1,322종목**으로 달라졌고,
직전 실행에서 corp_code 매칭에 실패했던 종목이 다음 실행에는 아예 사라져 있었다.

에러는 나지 않는다. 대신
- 같은 날 두 번 돌리면 대상 종목이 달라져 **스크리닝 결과를 재현할 수 없고**,
- `outcome_tracking`(P11)의 사후 검증에서 "그때 유니버스에 있었는가"가 흔들린다.

→ 유니버스 갱신은 PRD §10대로 **06:00 KST(장 시작 전) 1일 1회**만 한다.
   개발 중 장중 실행은 수치 확인용으로만 쓰고, 그 수치를 기준값으로 삼지 마라.

## T22. DART `corpCode.xml`을 정규식 하나로 훑으면 회사와 종목코드가 뒤바뀐다 `[HD 2026-08-13 실측]`

`corpCode.xml`은 118,706개 `<list>` 레코드 중 **종목코드가 있는 것이 3,982건뿐**이고
나머지는 `<stock_code> </stock_code>`(공백)다. 여기서 아래처럼 한 방에 잡으려 하면 —

```python
re.findall(r"<list>.*?<corp_code>(\d{8})</corp_code>.*?"
           r"<stock_code>(\S{6})</stock_code>.*?</list>", xml, re.DOTALL)   # ✗ 틀림
```

빈 `stock_code` 레코드에 걸린 순간 `.*?`가 **레코드 경계를 넘어** 뒤쪽 레코드까지
건너뛰며 매칭한다. 결과적으로 **A사의 `corp_code`에 B사의 종목코드가 붙는다.**

실측 피해: 리노공업 `00248008`(오답) → `00369657`(정답),
HPSP `00382269`(오답) → `01288827`(정답). 삼성전자는 우연히 맞았다.
**매칭률은 99.92%로 높게 나오고 예외도 경고도 없다** — 검증에서 "매칭률"만 보면 절대 못 잡는다.
이 상태로 P2에 들어가면 **다른 회사의 재무제표를 수집하고도 끝까지 모른다.**

→ `<list>...</list>` 블록을 먼저 잘라낸 뒤 블록 안에서 각 필드를 찾는다.
→ 검증은 매칭률이 아니라 **DART `company.json`으로 왕복 대조**해야 한다
   (`corp_code`로 조회한 `stock_code`가 원래 종목코드와 같은가).
   회귀 방지: `tests/test_universe.py::test_corp_code_does_not_cross_record_boundary`

## T21. Windows에서 출력을 파이프로 넘기면 등급 기호(★ ○ △ ·)에 죽는다 `[HD 2026-08-13 실측]`

`python -m src.db.init`을 터미널에서 직접 돌리면 멀쩡한데,
`| tee log.txt` 같이 **리다이렉트하는 순간** 죽는다:

```
UnicodeEncodeError: 'cp949' codec can't encode character '═'
```

Windows에서 stdout이 콘솔이면 Python이 콘솔 API(UTF-16)로 쓰지만,
**파이프로 리다이렉트되면 로케일 인코딩(cp949)으로 떨어진다.**
cp949는 `═`(U+2550)도 `★`도 인코딩하지 못한다 — 실측으로 확인했다.

이 프로젝트는 매트릭스 등급(★ ○ △ ·)과 표 괘선을 상시 출력하므로
**모든 CLI 엔트리포인트에서 재발한다.** 더 나쁜 건 실패 지점이
"출력 도중"이라 그 전까지의 DB 작업은 이미 수행된 뒤 스크립트만 죽는다는 것이다.
GitHub Actions 로그 캡처도 파이프라 여기에 해당한다.

- 대응: 엔트리포인트 첫 줄에서 `src/utils/console.py`의 `enable_utf8_stdout()` 호출.
- 텔레그램/대시보드는 UTF-8이라 무관하다. **콘솔 출력에만** 해당한다.

## T25. 성장률은 분모만 막으면 부족하다 — 흑자→적자에서 %가 계산된다 `[HD 2026-08-13 실측]`

T12는 "분모가 0 또는 음수면 None"이라고만 적혀 있다. 그대로 구현하면
**흑자(+100) → 적자(−30)** 구간이 그물을 빠져나간다. 분모(+100)가 양수라

```python
(-30 - 100) / 100 * 100 = -130.0     # "성장률 −130%"
```

이 값이 그럴듯하게 계산된다. 이건 성장률이 아니라 **적자전환**이다.

**실측 피해**: `op_status_label`이 '적전'인 **447건 전부에 `op_yoy`가 붙어 있었다.**
그대로 두면 스코어 A2(영업이익 YoY 델타)와 B축이 그 −130%를 먹는다.
게이트 G2(`op_yoy > 0`)는 통과시키지 않으니 조용히 넘어가고, 점수만 틀어진다.

- 손익 항목(op·np·eps): **분자와 분모 모두 0 이하인지 검사**한다 → `profit_growth_pct`
- 매출: 음수가 될 수 없으므로 분모만 검사한다 → `growth_pct`.
  매출 100 → 0은 −100%로 의미가 있으니 분자를 막으면 오히려 정보가 사라진다.
- **자기검증을 리포트에 넣어라**: "라벨이 있는데 %도 있는 행 = 0건". 이게 없었으면
  못 봤다. 회귀 방지: `tests/test_derive.py::test_label_and_percent_are_mutually_exclusive`

## T26. 정규화는 '축' 단위다 — 축 안의 결측은 분모에서 빠지지 않는다 `[HD 2026-08-13 실측]`

PRD §4.2의 정규화는 **축(A/B/C/D) 단위**다. 축 전체가 미측정이면 분모에서 빠지지만,
**축 안의 개별 항목이 결측이면 0점으로 들어가고 배점은 분모에 그대로 남는다.**

예: B축에서 b3(영업레버리지)만 측정되고 b1·b2·b4가 없으면
B = 6점인데 분모에는 32점이 통째로 들어간다 — **26점이 조용히 깎인다.**
축을 통째로 버리면 정보가 더 사라지므로 이 설계 자체는 옳다. 문제는 **보이지 않는다는 것**이다.

실측(2026.1Q, 게이트 통과 312종목): 종목당 평균 **4.8점**이 이렇게 깎였다.
항목별 결측 — a2 95종목 · b3 54 · b4 45 · a4 2.
(a2는 부호 전환으로 op_yoy가 None인 경우, b4는 같은 업종 종목이 5개 미만인 경우)

→ `ScoreResult.missing_items`에 **어떤 항목이 몇 점을 못 받았는지** 기록하고
  대시보드 스코어 분해 바에 반드시 표시한다. 안 그러면 "왜 점수가 낮은지"를 읽을 수 없다.

## T27. `document.xml`은 공정공시(거래소공시)에 쓸 수 없다 `[HD 2026-08-13 실측]`

PRD §5.3은 문서 원문을 `api/document.xml?rcept_no=...`로 받으라고 적고 있으나,
**잠정실적이 속한 거래소공시(`pblntf_ty=I`)에는 status `014` "파일이 존재하지 않습니다"**
가 돌아온다. rcept_no 9번째 자리가 `9`인 건이 그렇다(예: `20260813900811`).

즉 **P4가 파싱해야 할 문서가 정확히 API로 못 받는 구간**이다.
원문은 DART 웹 뷰어에서 2단계로 받아야 한다:

1. `https://dart.fss.or.kr/dsaf001/main.do?rcpNo=<rcept_no>`
   → 본문의 `viewDoc("<rcpNo>", "<dcmNo>", ...)`에서 `dcmNo` 추출
2. `https://dart.fss.or.kr/report/viewer.do?rcpNo=..&dcmNo=..&eleId=0&offset=0&length=0&dtd=HTML`

⚠️ 뷰어 응답은 **`charset=MS949`**다. utf-8로 읽으면 계정명('매출액' 등)이 깨져
   **계정을 하나도 못 찾고 파싱만 조용히 실패**한다. content-type을 보고 cp949로 디코딩할 것.

## T28. 같은 공시명으로 **월별 실적**을 내는 회사가 있다 `[HD 2026-08-13 실측]`

`영업(잠정)실적(공정공시)`는 보통 분기 실적이지만, 유통업 등 일부 회사는
**같은 공시명으로 월별 실적**을 낸다(실측: 이마트 `2026-07-01 ~ 2026-07-31`).

공시명·표 구조·계정명이 전부 같아서 **분기 실적으로 그대로 저장된다.**
그러면 그 분기 매출이 **1/3 수준으로 들어가 '가짜 급감'**이 되고,
YoY·QoQ·TTM이 통째로 오염되는데 에러는 나지 않는다.

→ **'당기실적' 행의 시작일~종료일 일수**로 판정한다. 80~100일이 아니면 분기 실적이 아니다.
  실측 60건 중 3건(5%)이 월별이었다. LLM 폴백을 불러도 여전히 월별이므로
  **폴백 대상이 아니라 '분기 실적 아님'으로 분리**해 집계해야 한다.

**함께 잡힌 것 — 실적기간을 표 전체에서 긁으면 연도가 1년 어긋난다.**
실적기간 표는 `당기실적 / 전기실적 / 전년동기실적 / 당기누계실적 / 전년동기누적실적`
순서라, 표 텍스트에서 날짜를 긁어 마지막을 쓰면 **전년 동기 누적의 종료일**을 집는다.
실측에서 2026.2Q 공시가 전부 2025.2Q로 찍혔고 **파싱은 '성공 100%'로 보고됐다.**
→ '당기실적'으로 시작하는 **행 안에서만** 날짜를 읽고, 공시일과 대조해 검증한다.

## T29. 잠정실적 표에서 데이터 표를 "행이 가장 많은 표"로 고르지 마라 `[HD 2026-08-13]`

문서에 표가 여러 개일 때 행 수로 고르면 **실적기간 표**가 뽑힐 수 있다.
그러면 계정을 하나도 못 찾아 `no_account_matched`로 실패하는데,
원문에는 멀쩡히 값이 있으므로 "이 회사는 파싱이 안 된다"고 오인하게 된다.
→ **계정명(매출액·영업이익·당기순이익)이 실제로 들어 있는 표**를 고른다.

## T30. FnGuide 구 URL은 죽었고, 네이버 표는 **열이 밀린다** `[HD 2026-08-13 실측]`

### (a) PRD가 지정한 컨센서스 소스가 사라졌다

PRD §5.1이 1·2순위로 지정한
`comp.fnguide.com/SVO2/ASP/SVD_Main.asp` · `SVD_Consensus.asp`는
**"페이지가 없습니다. 신버전 바로가기"**(1,829바이트)만 돌려준다.
Referer·UA를 붙여도 같다. 소스를 아래로 대체했다:

- 추정치   : `finance.naver.com/item/main.naver` 기업실적분석 표의 `(E)` 분기 컬럼 (단위 **억원**)
- 추정기관수: `navercomp.wisereport.co.kr/v2/company/c1010001.aspx`의 '추정기관수'

`navercomp.wisereport.co.kr/v2/company/c1050001.aspx`(컨센서스 화면)는 **AJAX 로딩**이라
HTML만 받으면 표가 빈 껍데기다 — 여기에 시간을 쓰지 말 것.

### (b) ★ 기업실적분석 표는 종목마다 열 구성이 다르다

대부분은 `연간 4 + 분기 6`이지만, **헤더 첫 칸이 비어 있고 연간이 3개**인 종목이 있다
(실측: 한화비전 489790 · SK이터닉스 475150).
헤더를 위치로 세면 **한 칸씩 밀려** 연간 (E) 컬럼이 분기로 잡힌다.

실측 피해: **연간 추정 매출 20,334억이 `2026.4Q` 분기 컨센서스로 저장**됐다.
실제 2026.2Q는 5,012억 — **4배 오차**다. 이 값으로 서프라이즈를 계산하면
C축(15점)이 통째로 뒤집히는데 에러는 나지 않는다.

→ 헤더에서 **기간 패턴이 있는 칸만** 모으고, 값 행은 **뒤에서부터 기간 수만큼** 잘라 정렬한다.
→ 연간/분기 구분은 "앞쪽에서 12월 결산이 연도 오름차순으로 이어지는 구간"으로 판정한다
  (2025.12는 연간·분기 양쪽에 나오므로 월만으로는 못 가른다).
→ 검증: `저장된 분기 분포`에 4Q가 섞여 있으면 의심하라. 시즌 중 4Q 컨센서스는 나올 수 없다.

### (c) 네이버는 페이지마다 인코딩이 다르다

`finance.naver.com/item/main.naver`는 **UTF-8**, `sise/management.naver`는 **euc-kr**다.
관행대로 euc-kr로 고정하면 숫자는 ASCII라 멀쩡하고 **한글 라벨만 깨져**
'매출액'을 못 찾고 "이 종목은 커버리지 없음"으로 조용히 집계된다.
→ `src/utils/http.decode_html()`이 content-type·meta charset을 보고 디코딩한다.

## T31. KIS의 PER·PBR·EPS `0.00`은 값이 아니라 결측이다 `[HD 2026-08-13 실측]`

`inquire-price`가 일부 종목에 밸류에이션 필드를 전부 `0.00`으로 돌려준다:

| 종목 | per | pbr | eps |
|---|---|---|---|
| 삼성전자(005930, KOSPI) | 40.83 | 4.19 | 6564.00 |
| SK하이닉스(000660) | 27.02 | 9.28 | 58955.00 |
| 에코프로비엠(247540, KOSDAQ) | 284.12 | 6.48 | 403.00 |
| **리노공업(058470, KOSDAQ)** | **0.00** | **0.00** | **0.00** |
| **HPSP(403870, KOSDAQ)** | **0.00** | **0.00** | **0.00** |

**시장 단위가 아니다** — 1,112종목 실측에서 KOSPI 12건(2.6%) · KOSDAQ 39건(6.1%)이
그렇다. 규칙성을 찾지 말고 **값 단위로 방어**하라.
(처음엔 리노공업·HPSP 둘 다 코스닥이라 "코스닥은 전부"로 오인했다 — 표본 2개로
 규칙을 세우면 이렇게 된다.)

`0.00`을 그대로 PER로 저장하면 **3년 PER 밴드 백분위에서 최하위**가 되어
PRI P3가 0점이 되고, 그 종목이 "아직 주가에 반영되지 않음"으로 잘못 읽혀
**★로 승격**된다.

**PER이 0인 기업은 존재하지 않는다** — 0은 값이 아니라 결측이다.
→ 밸류에이션 지표는 `_ratio()`로 읽어 `0`을 `None`으로 바꾼다.
  음수 PER(적자 기업)은 의미가 있으므로 **그대로 둔다** — 0과 구분할 것.
→ 결측 51종목의 PER이 필요하면 네이버 등 다른 소스를 붙여야 한다(P6 범위 밖).

**같이 확인한 것 — 네이버 폴백의 종가는 최상위에 없다.**
`m.stock.naver.com/api/stock/<code>/integration`에서 `closePrice`는 최상위 키가 아니라
`dealTrendInfos[0].closePrice`에 있다. 최상위만 보면 `close=None`이 되어
**폴백이 동작한 것처럼 보이지만 PRI P2가 통째로 미측정**이 된다.

## T32. KIS "초당 20건"은 문서값일 뿐 — **1.8건/초에서도 EGW00201이 난다** `[HD 2026-08-13 실측]`

PRD §5.4와 KIS 문서는 실전계좌 유량을 **초당 20건**이라고 한다.
안전 마진 18/초 토큰버킷을 넣고 1,112종목을 순회했는데:

```
실제 처리량 1.8건/초 (623초)   ← 네트워크 지연이 병목이라 스로틀엔 닿지도 않았다
KIS 실패 111종목 (10%)         ← 전부 EGW00201 "초당 거래건수를 초과하였습니다"
```

**초당 20건은커녕 2건/초에서도 난다.** 문서값을 믿고 스로틀만 걸면 10%가 조용히 샌다.

더 나쁜 건 **폴백이 이 실패를 가려준다는 것**이다. 네이버 폴백이 111종목 전부를
성공시켜 `전부 실패 0`으로 보고됐지만, 네이버에는 `per`·`hts_avls`(시총)·
`acml_tr_pbmn`(거래대금)·`lstn_stcn`(상장주식수)이 없다.
결과적으로 **시총 측정이 1,001/1,112로 조용히 줄었다**(D4 유동성·PRI P3에 직결).

→ EGW00201은 **재시도로 회복된다.** 백오프 0.6초 × 3회를 넣으니
  표본 180종목에서 재시도 17회가 전부 흡수되고 **최종 실패 0%**가 됐다.
→ 바로 폴백하지 말 것. 폴백은 KIS가 정말 죽었을 때만이다.
→ `KIS_RATE_LIMIT_PER_SEC`을 8로 낮췄다. 문서값(20)을 상수에 그대로 넣지 마라.
→ 감시: `KisClient.rate_limit_hits`(재시도 횟수)와 `시총 측정 건수`를 함께 볼 것.
  "전부 실패 0"만 보면 아무 문제 없어 보인다.

## T33. Sonnet 5는 `thinking`을 생략하면 사고가 켜지고, `max_tokens`를 함께 먹는다 `[HD 2026-08-13]`

Sonnet 4.6까지는 `thinking` 필드를 생략하면 사고가 **꺼진** 상태로 돌았다.
**Sonnet 5는 생략하면 적응형 사고가 켜진다.** 그리고 `max_tokens`는
**사고 + 응답 텍스트의 합**에 걸리는 상한이다.

PRD §7.3이 정한 `max_tokens=8192`를 그대로 두고 `thinking`을 생략하면,
사고가 출력 자리를 먹어 **tool-forced JSON이 중간에서 잘린다.**
`stop_reason == "max_tokens"`로 드러나기는 하지만 **비용은 이미 발생한 뒤**다.

→ 해석 과제는 깊은 사고가 필요 없다. `thinking={"type": "adaptive"}` +
  `output_config={"effort": "low"}`로 고정한다(`constants.LLM_EFFORT`).
  실측: 이 설정에서 출력 2,296~2,645토큰, 8,192 상한에 여유가 크다.

## T34. 프롬프트 캐시는 **1,024토큰 미만이면 조용히 안 걸린다** `[HD 2026-08-13 실측]`

Sonnet 5의 최소 캐시 프리픽스는 **1,024토큰**이다. 그보다 짧은 프리픽스에
`cache_control`을 붙여도 **에러도 경고도 없이** 그냥 캐시되지 않는다
(`cache_creation_input_tokens: 0`). 비용이 조용히 10배가 된다.

실측: 이 프로젝트의 시스템 프롬프트는 1,552자 = **1,497토큰**(한글 ≈ 0.96 토큰/자).
문턱을 46% 넘겼을 뿐이라, 나중에 프롬프트를 다듬다 아래로 내려갈 수 있다.
→ `tests/test_cost_guard.py::test_system_prompt_is_long_enough_to_cache`가 하한을 강제한다.

**같이 확인한 것 — 캐시 범위는 시스템 프롬프트만이 아니다.**
렌더 순서가 `tools → system → messages`라, 시스템 블록에 붙인 `cache_control`은
**도구 스키마까지 함께 캐시한다.** 실측 캐시 3,242토큰 = 시스템 1,497 + 도구 스키마 약 1,745.
도구 스키마가 큰 이 프로젝트에서는 이득이 두 배다 — 다만 **도구를 하나라도 바꾸면
캐시가 통째로 깨진다**는 뜻이기도 하다(도구는 프리픽스 맨 앞에 렌더된다).

## T19 검증 완료 `[HD 2026-08-13 — 공식 페이지 직접 확인]`

T19("Anthropic 가격에 날짜 기준 전환 로직 금지")가 **옳다는 것을 1차 출처로 확인**했다.
platform.claude.com pricing 페이지 원문:

> "The $2/$10 per million input/output token pricing for Claude Sonnet 5, announced at
> launch as introductory pricing through August 31, 2026, **is now the standard price.
> The previously scheduled increase to $3/$15 per million input/output tokens on
> September 1, 2026 will not occur.**"

참고 프로젝트의 `SONNET_5_PRICE_SWITCH_DATE = date(2026, 9, 1)` 분기 로직이 틀린 쪽이다.
이식하지 않았고, `tests/test_cost_guard.py::test_pricing_takes_no_date_argument`가
`get_pricing()`에 날짜 인자가 다시 생기는 것을 막는다.

## T35. PRI는 항목 하나가 지배적이라 "축 단위 정규화"만으로는 못 막는다 `[HD 2026-08-15 실측]`

스코어(A~D)는 축이 4개고 배점이 고르게 흩어져 있어 축 하나가 빠져도 나머지로 판정이 선다.
**PRI는 다르다.** P1(3개월 상대수익률) 하나가 40점으로 전체의 40%를 쥐고 있다.

`compute_pri()`는 원래 측정된 항목만으로 분모를 만들었으므로,
`price_snapshots`에 P2(52주 위치, 25점)만 있어도 **산술적으로 PRI가 나온다.**

실측 사고: `rel_ret_3m` 전 종목 수집 전에 스크리너를 돌렸더니
전 종목이 **분모 25로 PRI를 받아 ★27종목 · ○88종목**이 배정됐다.
"3개월간 시장 대비 얼마나 안 올랐나"를 **전혀 모르는 채로** 미반영을 선언한 것이다.
52주 저점 부근이기만 하면 이미 시장을 크게 이긴 종목도 ★가 된다 —
이 시스템이 피하려는 바로 그 오류(좋은 기업 ≠ 좋은 투자)를 정면으로 저지른다.

에러도 경고도 없다. 등급표는 그럴듯하게 채워지고, 발송 대상 115종목이 잡힌다.

→ `constants.PRI_MIN_DENOMINATOR = 40`. 분모가 하한 미만이면 **pri = None**(0점 아님).
  P1 단독(40) 또는 P2+P4(40) 이상이 모여야 판정한다.
  `tests/test_screener.py::test_pri_none_when_only_p2_measured`가 재발을 막는다.
  텔레그램에도 "분모 25점으로 부족해 판정 보류"를 그대로 노출한다 —
  숨기면 "반영도 0"과 구분되지 않는다.

## T36. 스크리너가 분기를 고정하면 먼저 발표한 종목의 최신 실적이 통째로 무시된다 `[HD 2026-08-15 실측]`

`src/screener/run.py`는 `--quarter 2026.1`을 **기본값으로 하드코딩**하고 있었다.
전 종목을 같은 분기로 평가한다는 뜻이다.

실측: 삼성전자 2026.2Q 잠정을 포함해 **잠정실적 357종목**을 `quarterly_fundamentals`에
넣었는데, 스크리너 결과는 **한 종목도 바뀌지 않았다.** 고정 인덱스가 2026.1Q였기 때문이다.
에러는 없다 — 최신 실적이 DB에 멀쩡히 있는데 읽지 않을 뿐이다.

한국 기업은 발표 시기가 제각각이라(잠정 7월 초 ~ 정기보고서 8월 중순),
"현재 분기"라는 단일 시점이 애초에 존재하지 않는다.
대상이 '등록된 종목'이 아니라 '시총 하한을 넘는 전 종목'이므로(ADR 6),
**평가 시점도 종목마다 달라야 한다.**

→ 기본값을 `--quarter latest`로 바꾸고 `target_index()`가 종목별 최신 발표 분기를 고른다.
  실측 분포: 2026.2Q 460종목 · 2026.1Q 636종목 · 2025.4Q 5종목 · 2025.2Q 1종목.

**같이 걸린 것 둘:**
1. **업종 백분위(B4)는 같은 분기끼리 비교해야 한다.** 분기가 섞이면 비교가 아니다.
   → 분기별 코호트로 나눠 `sector_percentiles()`를 각각 돌린다.
2. **미래 분기가 섞여 들어온다.** 한스바이오메드(042520)가 `2026.3Q` 행을 갖고 있다
   (10,072행 중 1행). 2026-08 시점에 3분기는 끝나지도 않았다 — 비12월 결산 추정.
   종목별 최신을 그냥 고르면 **존재할 수 없는 분기로 평가**된다.
   → 리포트에 "N분기 뒤" 및 "3분기 이상 뒤처진 종목" 경고를 찍어 눈에 보이게 했다.

## T37. 흑전 서프라이즈는 **어느 쪽으로 처리해도 0점이 된다** `[HD 2026-08-15 실측]`

T25("부호가 바뀌는 구간에서 % 계산 금지")를 컨센서스 서프라이즈에 그대로 적용하면
**최고의 서프라이즈가 조용히 사라진다.**

실측: 삼성SDI 2026.2Q — 컨센서스는 영업 **적자(−356억)** 를 예상했는데
실제는 **흑자(+2,038억)** 였다. 가능한 최강의 어닝 서프라이즈다.

두 경로가 다 틀린다:

| 처리 | 계산 | 결과 |
|---|---|---|
| %를 만든다 | `(2038 − (−356)) / (−356)` = **−672%** | `_tiered`가 "기준 미달"로 읽어 **c1 = 0점** |
| %를 막고 None (T25) | — | c1이 **미측정** → C축 안에서 **9점 조용한 감점**(T26) |

에러도 경고도 없다. 두 번째 경로는 T26 덕에 화면에 드러나기라도 하지만,
**첫 번째 경로는 "측정했고 0점"으로 보여 구분조차 안 된다.**

→ 라벨이 점수를 만들게 한다(`_c1_from_label`).
  **게이트가 흑전을 G2 통과로 처리하는 것과 같은 규칙**이다 — 새 정책이 아니다.

  - `흑전 서프라이즈`(적자 예상 → 흑자) → **만점**
  - `적자 쇼크`(흑자 예상 → 적자) → **0점** (미측정이 아니라 명백한 0)
  - `적자 예상 부합`(둘 다 적자) → **None**. 적자 폭 개선을 흑자 전환과 같은 급으로
    볼 수 없고, 얼마나 개선인지 %로 잴 수도 없다.

**교훈: "% 계산 금지" 규칙은 값을 버리라는 뜻이 아니다.** 버린 자리를 라벨이
메우지 않으면, 부호가 바뀌는 구간(= 가장 큰 변화가 일어나는 구간)이 통째로 0점이 된다.
`op_status_label`이 게이트에서 그 역할을 하고 있었는데 C축에는 대응물이 없었다.

## T38. 0점 항목을 화면에서 숨기면 "측정해서 0점"과 "미측정"이 구분되지 않는다 `[HD 2026-08-15 실측]`

`score_breakdown()`이 `value > 0`인 항목만 표시했다. 0점 항목은 `earned`에도
`missed`에도 안 들어가 **통째로 사라진다.**

실측: 삼성전자 2026.2Q가 `C 서프라이즈 3/15 — 영업이익 서프라이즈 3/9`로만 나왔다.
매출이 컨센서스를 **−1.36% 하회해 0/6**을 받은 사실이 어디에도 없다.
"C가 왜 3/15인가"를 읽을 수 없고, 나머지 6점이 미측정 때문인지 미달 때문인지도 모른다.

→ `value is None`이면 `미측정(-N)`, 아니면 `0/N`을 포함해 **전부 표시**한다.
  메시지가 길어지지만 텔레그램 상한 4,096자에 한참 못 미친다(현재 ~750자).

**이건 T26의 확장이다.** T26은 "축 안의 결측이 조용한 감점"이라는 것이었고,
여기는 "측정된 0점도 안 보이면 결측과 같아진다"는 것이다.

## T39. `fnlttMultiAcnt`는 회사에 따라 **매출 계정을 통째로 반환하지 않는다** `[HD 2026-08-15 실측]`

주요계정(다중회사) API가 어떤 회사에는 매출 줄을 아예 주지 않는다.
**에러가 아니라 그냥 없다.** 응답은 status 000이고 행 수도 정상(28행)이다.

한국경제TV(039340) 2025 사업보고서의 손익 계정 전부:

```
영업이익 · 법인세차감전 순이익 · 당기순이익(손실) · 총포괄손익
```

매출이 없다. 그런데 **영업이익·순이익은 멀쩡히 온다.**
수집이 성공한 것처럼 보이고 `revenue`만 None이 되어,
게이트가 `revenue_t`/`revenue_t4` 결측으로 **조용히 판정 불가**로 빠진다.

실측 피해: **29종목 166행**. 그중 8종목은 **전 분기 매출이 없어 스크리너 대상에서
통째로 사라졌다**(1,111 → 1,103). 한국경제TV·폴라리스오피스처럼 매출이 분명히 있는
회사들이다. 발굴 대상이 조용히 줄어든 것이고, 어떤 카운터에도 잡히지 않았다.

**탐지 신호**: `영업이익은 있는데 매출이 없다`. 이건 "이 회사는 매출이 없다"가 아니라
"이 API가 안 준다"는 뜻이다. 진짜 무매출 기업이면 영업이익도 그에 맞게 온다.

→ 그 조합일 때만 `fnlttSinglAcntAll.json`(단일회사 **전체** 재무제표)로 폴백한다.
  거기엔 **`영업수익`** 으로 들어 있다(한국경제TV 2025년 759억).
  한 콜에 한 회사·한 보고서라 비싸므로 **결측 종목에만** 쓴다.

**같이 걸린 것 — 폴백 응답에는 `fs_div` 필드가 없다.**
`fs_div`가 요청 파라미터라 응답 행에 안 들어온다. 그대로 `extract_figures()`에 넘기면
fs_div 필터가 **전 행을 걸러내 빈손**이 된다. 에러도 경고도 없이
"폴백했는데 못 찾았다"로 보인다 — 실측으로 2종목 20행이 전부 매출 None으로 남았다.
→ `row.setdefault("fs_div", fs_div)`로 주입한 뒤 넘긴다.

**분기 분해는 기존 로직이 그대로 처리한다.** 폴백 응답도 같은 형태다:

```
1분기 11013 : 당기 17,488,073,800 · 누적 17,488,073,800
반기   11012 : 당기 19,800,525,411 · 누적 37,288,599,211
3분기 11014 : 당기 18,393,672,433 · 누적 55,682,271,644
사업   11011 : 당기 75,910,005,501 · 누적 **빈값**   ← amount가 연간 누적
```

손계산 대조 결과 4개 분기 합 = 75,910,005,501 = 연간, **차이 0**.
`tests/test_quarterize.py::test_single_all_shape_decomposes_correctly`가 이 형태를 고정한다.

**복구 결과**: 매출 None **166행 → 9행** · 전 분기 결측 종목 **8 → 0** ·
스크리너 대상 **1,103 → 1,111** · 게이트 통과 **338 → 344**.
남은 9행은 이노스페이스(2024년 발사체 개발 단계)·큐라클처럼 **실제로 매출이 없던 구간**이다.

## T40. `screen_results`는 분기 이력이 쌓이는 테이블이다 — "전체 조회"는 빈티지가 섞인다 `[HD 2026-08-15 실측]`

PK가 `(code, fiscal_year, fiscal_quarter)`라 **종목당 여러 행**이 남는다.
종목별 최신 분기로 평가하면(T36) 같은 종목에 2026.1Q 행과 2026.2Q 행이 공존한다.
이 자체는 정상이고 P11(성과 추적)이 쓸 이력이다.

**문제는 전체를 그냥 세면 틀린다는 것이다.** 실측:

```
총 1,601행 · 게이트 통과 511 · 등급 부여 344    ← 안 맞는다
```

"등급 = 게이트 통과"여야 하는데 511 ≠ 344다. 원인은 두 가지였다:

1. **미래 분기 잔재** — T36의 가드가 생기기 전에 들어온 `2026.3Q` 행이 남아 있었다.
   존재할 수 없는 분기라 어떤 소비자도 읽으면 안 된다.
2. **구 로직으로 계산된 이력** — 2026.1Q 행 1,111개가 PRI·C축 배선 **이전**에 저장돼
   게이트 통과 312 중 등급이 145개뿐이었다. 값이 틀린 게 아니라 **계산 시점이 다르다.**

→ `_save()`가 평가 분기보다 **미래**인 행만 지운다. 과거 분기는 남긴다(이력).
→ 이력의 빈티지가 섞이면 `--quarter 2026.1 --save`로 그 분기를 **현재 로직으로 다시 만든다.**

**★★ 여기에 되돌릴 수 없는 함정이 하나 더 있다.**
분기 고정 모드에서 미래 분기 정리를 그대로 돌리면, 모든 종목의 평가 분기가 2026.1Q가 되어
**정상적인 2026.2Q 행 481개가 통째로 삭제된다.** 삭제 카운터만 보면 "정리했다"로 읽힌다.
→ `_save(rows, fixed_mode=True)`일 때는 정리를 **건너뛴다.**

**부수 효과도 알아둘 것**: 고정 모드는 그 분기를 **미보고한 종목에도 행을 만든다**
(`target_index`의 revenue 검사를 우회하므로). 그 행이 종목의 진짜 최신 결과를 가린다 —
실측 2종목. 다음 `latest --save` 실행의 미래 분기 정리가 걷어낸다.

**소비자 규칙**: `screen_results`를 읽을 때는 **반드시 종목별 최신 분기 1행으로 접기.**
전체를 그대로 집계하면 같은 종목이 여러 번 세어지고 빈티지가 섞인다.

## T41. Next.js — `"use client"` 파일에서 **컴포넌트가 아닌 export**를 서버가 가져오면 런타임에 죽는다 `[HD 2026-08-15 실측]`

차트 컴포넌트 파일에 데이터 변환 함수를 같이 두고, 서버 컴포넌트에서 이렇게 썼다:

```tsx
import QuarterlyChart, { toChartPoints } from "@/components/QuarterlyChart";
```

**`npm run build`와 `tsc --noEmit`이 모두 통과한다.** 그런데 페이지를 열면:

```
TypeError: (0 , ...QuarterlyChart__WEBPACK_IMPORTED_MODULE_3__.toChartPoints) is not a function
GET /stock/005930 500
```

클라이언트 경계를 넘어갈 수 있는 것은 **컴포넌트뿐**이다. 일반 함수는 서버 번들에서
참조가 비어 `undefined`가 되고, 호출하는 순간 500이 난다.

→ 변환 함수는 `lib/`의 **서버·클라이언트 공용 모듈**에 둔다. 컴포넌트 파일에는 컴포넌트만.
→ **빌드 성공은 증거가 아니다.** 이 프로젝트에서 반복되는 교훈이 프런트에도 그대로 있다 —
  PRD §9가 "브라우저로 실제 확인"을 검증 조건으로 못박은 이유다.

## T42. Recharts 검증은 DOM 셀렉터를 믿지 마라 — 태그도 렌더 방식도 버전마다 다르다 `[HD 2026-08-15 실측]`

차트가 그려졌는지 DOM으로 확인하다가 **두 번 오진했다.**

**① 애니메이션이 끝나지 않아 길이 0으로 멈춘다.**
`stroke-dasharray: "0px, 807px"`, 막대 0개. 브라우저 패널이 비표시라
`requestAnimationFrame`이 돌지 않아 진입 애니메이션이 첫 프레임에 멈춘 것이다.
백그라운드 탭에서도 같은 일이 난다 — **사용자에게 빈 차트가 보인다.**
→ 데이터 대시보드에 진입 애니메이션은 얻는 게 없다. `isAnimationActive={false}`.

**② `ReferenceArea`는 `rect`가 아니라 `path`로 렌더된다(Recharts 3.x).**
`.recharts-reference-area rect`로 세면 **0개**가 나와 "사분면이 안 그려졌다"고 판단했고,
있지도 않은 원인(선언 순서)을 고쳤다가 주석까지 틀리게 썼다.
실제로는 처음부터 3개가 정확한 좌표에 있었다.
→ 확인은 `.recharts-reference-area-rect`(클래스, 태그 무관) + `getBBox()`로 한다.

**③ 사분면 라벨은 SVG 안에서 렌더되지 않는다.**
`label={{...}}` prop도 `<Label>` 자식도 text 0개였다.
→ **HTML 범례로 뺐다.** 렌더가 보장되고 폭이 좁을 때 겹치지도 않는다.

**교훈: "요소가 없다"는 결론을 내리기 전에 그 요소가 어떤 태그로 그려지는지부터 확인하라.**
셀렉터가 틀렸을 뿐인데 코드를 고치면, 멀쩡한 것을 건드리고 원인은 그대로 남는다.

## T43. dev 서버가 떠 있는 채로 `npm run build`를 돌리면 dev가 깨진다 `[HD 2026-08-15 실측]`

같은 `.next` 디렉터리를 공유하므로 프로덕션 빌드가 dev의 청크를 덮어쓴다.
페이지 HTML은 200으로 오는데 정적 청크만 404가 나서, 화면이 비어 보이고
콘솔에는 404만 잔뜩 찍힌다 — **서버 로그에는 에러가 없어** 앱 버그로 오인하기 쉽다.

```
GET /matrix 200
GET /_next/static/chunks/app/matrix/page.js 404
```

→ 검증 중에는 build를 돌리지 말거나, 돌렸으면 **dev 서버를 재시작**한다.

## T44. 봇을 공유한 채 `getUpdates`를 돌리면 **상대 메시지를 가로채 소비한다** `[HD 2026-08-15]`

`setWebhook` 금지(T13)는 잘 알려져 있었는데, **수신 쪽에 같은 크기의 함정이 하나 더 있다.**

`getUpdates`는 읽기가 아니라 **소비**다. `offset = 마지막 update_id + 1`로 확정하는 순간
그 이하 업데이트는 텔레그램 서버에서 **영구 삭제**되어 다른 폴러에게 다시 오지 않는다.

즉 봇을 공유한 상태에서 Heimdallr가 폴링하면:

- HermesCall 앞으로 온 `/add`, `/list` 같은 명령이 **Heimdallr에게 먼저 잡혀 사라진다**
- HermesCall 쪽에서는 "가끔 씹힌다"로 보인다 — 항상이 아니라 **경쟁 상태**라서
- 재현이 안 되고, 로그에도 아무 에러가 없다

→ `RECEIVING_METHODS`(수신 계열)를 따로 두고, **전용 봇이 아니면 클라이언트 안에서 막는다.**
  `sendMessage`는 상태를 소비하지 않으므로 공유 봇으로도 안전하다 —
  **분리 전에도 알림은 계속 나가고 수신만 막힌다.** 기능을 통째로 세우지 않는 게 요점이다.

**분리 실측(2026-08-15)**: 봇 ID `8605695587`(공유) → `8933940541`(@Heinmdallr_bot 전용).
`chat_id`는 같다(사용자 본인 ID라 봇이 달라도 동일) — 다만 **새 봇에 `/start`를 누르기 전에는
`400 chat not found`** 가 난다. 봇을 바꾸면 대화를 새로 시작해야 한다.

**같이 확인한 것 — offset을 우리 DB에 저장할 필요가 없다.**
텔레그램이 이미 기억한다. 처리 **후에** 확정하면 GitHub Actions처럼 매번 새로 뜨는
환경에서도 중복 처리가 없고, 중간에 죽으면 미확정분이 다시 온다(at-least-once).
반대로 하면(먼저 확정) 죽는 순간 메시지가 조용히 사라진다.

**웹훅은 분리 후에도 열지 않았다.** 이 프로젝트는 공개 URL이 필요 없고,
필요도 없는 기능 때문에 남의 봇을 죽일 경로를 남길 이유가 없다.

## T45. 텔레그램 본문은 **가변폭이라 공백 정렬이 통하지 않는다** `[HD 2026-08-15 실측]`

숫자 표를 만들어 놓고 `f"{value:>8}"`로 열을 맞췄는데, **폰에서는 자릿수가 흩어진다.**
텔레그램 기본 폰트가 비례폭이라 공백 1칸과 숫자 1칸의 실제 너비가 다르기 때문이다.
데스크톱에서 보면 그럭저럭 맞아 보여서 넘어가기 쉽다.

→ 숫자 표는 **반드시 `<pre>` 안**에 넣고 `parse_mode="HTML"`로 보낸다.
  `<pre>`만 고정폭으로 렌더된다.

**같이 걸린 것 셋:**

**① `len()`으로 폭을 재면 한글이 절반으로 계산된다.**
"32자 이내"로 맞췄는데 실제로는 60칸이었다. 한글·이모지는 모노스페이스에서 **2칸**이다.
실측: 삼성전자 제품 줄이 `len` 기준 69자 → 실제 **138칸**, 폰에서 6줄로 접혔다.
→ `display_width()`(ascii 1 · 나머지 2)로 재고, `clip()`으로 자른다.
→ `test_every_line_fits_mobile_width`가 실데이터로 회귀를 막는다.

**② HTML 모드에서는 `<`가 태그로 해석돼 발송이 400으로 죽는다.**
`F.PER 10.5 <컨센 기준>`이라고 썼더니 텔레그램이 파싱에 실패했다.
`<pre>` 안이라고 예외가 아니다. 종목명에 `&`가 들어오는 경우도 실재한다.
→ 본문에 들어가는 값은 `esc()`를 거치고, 태그 화이트리스트를 테스트로 고정한다.

**③ 표 안에서 단위를 섞으면 표가 표로 안 보인다.**
`1,714,995억`과 `135억`이 한 열에 있으면 눈이 자릿수를 못 센다.
→ `pick_unit()`으로 **계열 전체의 최댓값** 기준 단위를 하나 골라 통일한다.

**부수 효과 — 라벨을 줄이면 테스트가 깨진다.** "성장 가속"→"성장가속",
"영업이익 YoY 델타"→"영업익YoY델타"로 줄이면서 기존 테스트 12건이 실패했다.
문구를 그대로 assert하고 있었기 때문이다. 의도한 계약 변경이므로 테스트를 맞췄지만,
**문구 전체를 assert하면 이런 개편마다 깨진다** — 키워드 단위로 확인하는 편이 낫다.

## T46. 롱폴링은 HTTP 타임아웃이 대기 시간보다 길어야 한다 `[HD 2026-08-15 실측]`

`getUpdates`에 `timeout=25`를 주면 텔레그램이 **25초까지 응답을 붙들고 있는다**
(그게 롱폴링이다). 그런데 클라이언트의 HTTP 타임아웃이 30초로 고정돼 있었다.

여유가 5초뿐이라 네트워크가 조금만 느려도 **정상 대기를 실패로 오인**한다.
실측으로 `ConnectTimeout`이 연속 3회 찍혔고, 루프는 5초 쉬고 재시도하므로
기능은 죽지 않지만 **로그가 경고로 뒤덮여 정작 처리 기록이 묻힌다.**

→ 읽기 제한을 `롱폴링 대기 + 30초`로, 연결 제한은 따로 15초.
→ 같은 오류가 이어지면 **한 번만 찍고 백오프**(5→10→20…60초).
  한 번 성공하면 다시 알린다. 매 주기 찍으면 로그가 쓸모없어진다.

**교훈: 오래 기다리는 게 정상인 호출에 짧은 타임아웃을 두면, 정상 동작이 에러로 보인다.**

## T47. 워크플로 주기를 정하기 전에 **월 분(minute)을 계산하라** `[HD 2026-08-16 실측]`

`telegram_listen`을 "5분마다"로 아무 생각 없이 잡았다. 계산해 보니:

```
5분 주기 = 12회/시 × 24시 × 30일 = 8,640회 = 월 8,640분
전체 합계 11,300분 · private 무료 한도의 5.7배
```

**한 워크플로가 전체의 76%를 먹고 있었다.** 그것도 대부분 "메시지 없음"을 확인하는 데.

게다가 **GitHub Actions cron은 부하가 걸리면 5~20분씩 밀린다.**
5분 주기로 잡아도 실제 응답은 그보다 느리다 — **챗봇 즉답용으로는 애초에 부적합하다.**
즉답이 필요하면 롱폴링(`--watch`)을 상시 띄워야 하고, 워크플로는
"그게 안 떠 있을 때 질문이 묻히지 않게 하는 안전망"으로 위치를 바꿔야 한다.

→ 깨어 있는 시간(07~24 KST)에 15분마다로 줄여 **월 2,040분**(전체 4,700분).
  텔레그램이 미확정 업데이트를 24시간 보관하므로 자주 돌 이유가 없다.

**교훈: 주기는 "얼마나 자주 필요한가"가 아니라 "이 주기가 무엇을 사는가"로 정한다.**
5분과 15분의 차이는 최악의 경우 10분 지연인데, 그 대가가 월 6,600분이었다.

## T48. 테스트에서 상대경로를 쓰면 **실행 위치에 따라 깨진다** `[HD 2026-08-16 실측]`

워크플로 YAML을 검증하는 테스트에 `Path(".github/workflows")`를 썼다.
리포지토리 루트에서 `pytest`를 부르면 통과하는데, 다른 디렉터리에서 부르니
`FileNotFoundError`로 2건이 실패했다.

CI는 루트에서 돌기 때문에 **CI만 보면 영영 안 드러난다.**
반대로 개발자가 다른 위치에서 돌리면 "내 환경에서만 깨지는" 테스트가 된다.

→ `Path(__file__).resolve().parents[1] / ".github" / "workflows"`.
  테스트 파일 위치를 기준으로 잡으면 어디서 불러도 같은 곳을 본다.

## T49. 결과 추적에서 "측정 불가"를 0으로 채우면 **아무 효과 없음으로 보인다** `[HD 2026-08-16 실측]`

발표 후 D+20 초과수익을 재는데, 시즌이 막 끝나면 **거래일이 모자라 대부분 측정이 안 된다.**

실측: 2026.2Q 459건 중 D+20 측정 가능 **2건**(발표일 20260715~20260813, 오늘 20260816).

여기서 미측정을 0%로 채우면:
- 평균·중앙값이 0 쪽으로 끌려간다
- 등급별 차이가 사라져 **"스코어가 아무 효과 없다"**는 결론이 나온다
- 실제로는 그냥 **아직 시간이 안 지난 것**이다

수익률 0%("안 움직였다")와 "20거래일이 안 지났다"는 완전히 다른 상태다.

→ `None` + 사유(`거래일 부족(아직 이르다)` / `거래정지 또는 상장폐지`).
  화면과 리포트에 **측정 건수를 값과 함께** 낸다(`+4.99(9)` — 중앙값과 n).
  n=1짜리 중앙값을 신호로 읽는 사고를 막는다.

**같이 걸리는 것 셋:**

**① 거래일로 세야 한다 — 캘린더 일수가 아니다.**
휴장일이 섞이면 20일이 14거래일이 되고 종목마다 다른 날을 비교하게 된다.
**숫자는 그럴듯하게 나온다.**

**② 발표일이 휴장일이면 다음 거래일이 기준이다.**
장 마감 후·주말 공시가 실제로 많다. 그날이 시장이 처음 반응할 수 있는 날이다.

**③ 발표일은 그 분기 공시 중 가장 이른 것.**
정정·상세 공시를 발표일로 잡으면 시장이 이미 반응한 뒤부터 재기 시작해
**초과수익이 통째로 사라진다.**

**그리고 D+1로 판단하면 안 된다.** 실측에서 ★ 그룹이 D+1 −3.31%p로 가장 낮은데
D+5에서는 +4.99%p로 가장 높다. **미반영 종목은 반응이 늦게 오는 게 설계상 정상이다**
(그래서 PRI가 낮은 것이다). 시점을 안 밝히고 IC를 보여주면 정반대로 읽힌다.

## T50. 문자열 치환이 **엉뚱한 곳에 걸려도 출력은 그럴듯하다** `[HD 2026-08-16 실측]`

리포트의 `for days in (20, 60)`을 `for days in HORIZONS`로 바꾸려고 치환했는데,
파일 앞쪽의 **다른** `for days in HORIZONS`에 매칭됐다. 원래 대상은 그대로 남았다.

그런데 같은 치환 묶음의 **다른 조각(메시지 문구)은 적용됐다.**
그래서 출력에 새 문구가 섞여 나왔고 — **적용된 줄 알았다.**
`grep`으로 확인하고 나서야 두 줄이 남아 있는 걸 알았다.

→ 여러 조각을 한 번에 치환할 때는 **치환 후 대상 라인을 직접 확인한다.**
  "출력이 바뀌었으니 됐다"는 증거가 아니다.

## T51. RLS로 막힌 테이블은 **에러가 아니라 빈 배열**을 준다 `[HD 2026-08-16 실측]`

`cost_log`에는 anon SELECT 정책을 **일부러 주지 않았다**(schema.sql 주석에 명시).
비용은 공개 대상이 아니고, 읽으려면 서버사이드 라우트를 경유하라는 설계였다.

그런데 `/settings` 화면을 만들면서 그걸 잊고 anon 클라이언트로 바로 읽었다.
결과는 **오류가 아니라 `[]`**:

```
이번 달 비용  $0.0000 / $8   (0.0%)
이번 달 호출  0건
누적 호출     0건
```

실제로는 `$0.0643 · 2건`이다. **화면은 200이고 콘솔도 조용하다** —
"아직 LLM을 안 썼구나"로 읽고 넘어가기 딱 좋다.

RLS는 권한이 없으면 **행을 숨긴다**. 401도 403도 아니다. 그래서
"조회 결과 0행"이 *데이터가 없음*인지 *볼 권한이 없음*인지 구분되지 않는다.

→ anon으로 읽는 테이블 목록을 실제로 대조했다:

```
cost_log                 anon 0행     ← 정책 없음(의도)
notifications            anon 1행
screen_results           anon 1,594행
outcome_tracking         anon 459행
quarterly_fundamentals   anon 10,091행
earnings_disclosures     anon 639행
```

→ `cost_log`는 `app/api/cost/route.ts`(service_role)를 경유하고,
  **키가 없거나 조회가 실패하면 0이 아니라 `available: false` + 사유**를 돌려준다.
  화면은 숫자 대신 `—`와 경고를 띄운다 — "0"으로 보이면 안 된다.

**교훈: 0행을 보면 권한부터 의심하라.** 특히 RLS를 켠 프로젝트에서
"데이터가 없네"는 절반의 확률로 "볼 수 없네"다.

## T52. 미래 분기 한 행이 화면 전체를 무의미하게 만든다 `[HD 2026-08-16 실측]`

`/season`에서 현재 시즌을 `max(fiscal_year*4 + quarter)`로 잡았더니:

```
2026.3Q 시즌 현황
발표 진행률  0.1%   (1 / 1,112종목)
```

2026-08 시점에 3분기는 **끝나지도 않았다.** 원인은 T36에서 이미 찾아낸
비12월 결산 기업(한스바이오메드)의 `2026.3Q` 행 **하나**다.

스크리너에서는 `last_reportable_index()`로 막아 뒀는데,
**새 화면을 만들면서 같은 규칙을 다시 적용하지 않았다.**
에러는 없고 화면만 조용히 쓸모없어진다 — 진행률 0.1%를 보고
"수집이 망가졌나" 의심하게 된다.

→ 대시보드에도 같은 상한을 적용해 **2026.2Q · 43.3%**(481/1,112)로 정상화.

**교훈: 한 곳에서 막은 함정은 새 소비자가 생길 때마다 다시 뚫린다.**
날짜 상한 같은 규칙은 조회 계층에 두거나, 최소한 주석으로 "같은 규칙"임을 못박아야 한다.

---

## T53. **깔려 있지 않은 dev 의존성은 테스트를 실패시키지 않고 skip시킨다** `[HD 2026-08-17 실측]`

배포 직전 `pytest tests/`를 돌리니 **378 passed, 26 skipped**로 나왔다.
직전 세션 기록은 **403 passed, 1 skipped**였다. "환경 차이겠지"로 넘기기 딱 좋다.

실제 원인은 로컬 venv에 `pyyaml`이 없었던 것이다:

```
SKIPPED [8] tests/test_workflows.py:41: could not import 'yaml': No module named 'yaml'
SKIPPED [8] tests/test_workflows.py:49: ...
SKIPPED [8] tests/test_workflows.py:57: ...
SKIPPED [1] tests/test_workflows.py:112: ...
```

`pyyaml`은 `pyproject.toml`의 `[project.optional-dependencies] dev`에 **정확히 선언돼 있었다**
(T10 규칙은 지켜졌다). 문제는 venv에 `pip install -e .`만 했고 `.[dev]`를 안 했다는 것.

**무엇이 조용히 틀리는가:** `pytest.importorskip` / 모듈 임포트 실패는 **초록색 통과로 보인다.**
하필 skip된 25건이 **지금 활성화하려는 워크플로 YAML을 검증하는 테스트 전부**였다 —
시크릿 하드코딩 검사(`test_secrets_are_not_hardcoded`), sleep 과금 검사,
`python -m` 대상 모듈 존재 검사가 통째로 안 돌고 "통과"로 보였다.
public repo에 push하기 직전에.

→ `pip install -e ".[dev]"` 후 재실행 → **403 passed, 1 skipped**로 복구.

**교훈: 통과 수를 직전 기록과 대조하라.** "passed"만 보면 안 된다 —
skip 수가 늘어난 것은 커버리지가 조용히 사라진 것이다.
CLAUDE.md의 "테스트 통과는 증거가 아니다"가 정확히 이 얘기다.
남은 skip 1건(`test_pending_workflows_have_no_schedule`)은 `PENDING_P11`이 비어서 나는
의도된 것이라 **정상 기준선이 `403 passed, 1 skipped`**다.

## T54. **`git grep`은 옵션 위치가 틀리면 fatal로 죽고, 파이프 뒤에서는 0으로 보인다** `[HD 2026-08-17 실측]`

public repo 전환 전에 "스테이징된 내용에 실제 API 키가 들어 있는가"를 이렇게 검사했다:

```bash
git grep -c -F -- "$val" --cached | wc -l     # ← 항상 0
```

`fatal: option '--cached' must come before non-option arguments`로 죽는데
stderr는 화면 밖으로 흘러가고 `wc -l`은 **0**을 준다. 즉 **키가 실제로 새고 있어도
"누출 0건"이 나온다.** 시크릿 검사기가 조용히 무력화되는 최악의 형태다.

```bash
git grep -l -F --cached -- "$val"             # ← 올바른 형태
```

**교훈: 검사기는 반드시 "반드시 걸려야 하는 입력"으로 자기 자신을 먼저 검증하라.**
여기서는 `OPENDART_API_KEY`(코드에 확실히 있는 문자열)로 스캐너를 쏴서
**15개 파일 매칭**을 확인한 뒤에야 "누출 0건"을 믿었다.
0을 돌려주는 검사기는 "안전하다"와 "검사가 안 됐다"를 구분해 주지 않는다 —
T51(RLS 빈 배열)·T49(미측정을 0으로)와 **완전히 같은 실패 모양**이다.

## T55. GitHub 삼항 `inputs.x && A || B`는 **양방향으로** 조용히 틀린다 `[HD 2026-08-17 실측]`

`telegram_listen`이 이렇게 돼 있었다:

```yaml
run: python -m src.notify.listen --once ${{ inputs.analyze && '--analyze' || '' }}
```

**틀림 ①: `schedule`에는 `inputs`가 존재하지 않는다.**
항상 빈 문자열이 되어 **cron 경로에서 LLM이 영영 호출되지 않았다.**
로그는 매번 `LLM 분석 OFF`를 정직하게 찍고 있었지만, 워크플로는 성공(초록)이고
텔레그램 회신도 정상적으로 나가서 — **해석 문단만 빠진 채** — 아무도 눈치채지 못했다.
`ANTHROPIC_API_KEY`는 시크릿으로 등록돼 있는데 **한 번도 안 쓰였다.**

**틀림 ②: 반대 방향도 안 된다.** GitHub 표현식에서 `false`는 falsy라
`inputs.x && A || B`는 x가 **false일 때도 B**를 준다.
즉 기본값만 뒤집으면 이번엔 "수동으로 끄기"가 에러 없이 무시된다.
JS의 같은 관용구를 그대로 옮기면 이 함정을 반드시 밟는다.

→ 분기를 **셸로** 옮겼다. 값 비교라 fall-through가 없다:

```yaml
env:
  ANALYZE_INPUT: ${{ inputs.analyze }}   # schedule이면 빈 문자열
run: |
  if [ "$ANALYZE_INPUT" = "false" ]; then
    python -m src.notify.listen --once
  else
    python -m src.notify.listen --once --analyze
  fi
```

실측 검증(두 경로를 **둘 다** 돌렸다 — 한쪽만 보면 ②를 못 잡는다):

```
기본 디스패치        → 허용 chat: [***] · LLM 분석 ON
-f analyze=false    → LLM 분석 OFF — 수동 실행에서 명시적으로 껐다
```

**곁다리로 드러난 것:** `--analyze`를 켜자 timeout 5분이 부족해졌다.
`confirm()`이 **배치를 다 처리한 뒤에야** offset을 확정하므로(`listen.py`),
BATCH 20건 × 분석 30초에서 죽으면 확정이 안 되고 **같은 20건이 계속 재배달된다**
→ 매번 같은 자리에서 죽는 빨간 X 루프가 되고 질문은 영영 답을 못 받는다. → 12분으로.

**교훈: 플래그를 켜는 변경은 켠 경로와 끈 경로를 둘 다 실행해 확인하라.**
켠 쪽만 보면 "동작한다"로 보이는데, 정작 되돌릴 수단이 죽어 있다.

## T56. **배포 URL은 프로젝트 이름에서 추론하지 말고 실제로 열어봐라** `[HD 2026-08-17 실측]`

Vercel 프로젝트명이 `heinmdallr`라는 말을 듣고 기본 URL을 `https://heinmdallr.vercel.app`으로
바꿨다(코드 상수 + `.env.example` + GitHub 저장소 변수 3곳). 그럴듯했다.

실제 배포 도메인은 **`heimdallr-call.vercel.app`**이었다:

```
https://heinmdallr.vercel.app/       → HTTP 404
https://heimdallr-call.vercel.app/   → HTTP 200
```

Vercel URL은 프로젝트 이름과 **항상 같지 않다** — 이름 충돌 시 접미사가 붙고,
저장소명·팀명에서 파생되기도 하며, 나중에 바꾼 이름이 도메인에 반영되지 않기도 한다.

**무엇이 조용히 틀리는가:** 이 값이 틀려도 아무것도 실패하지 않는다.
워크플로는 초록, 텔레그램 메시지는 멀쩡, 링크는 정상 형태.
**누가 폰에서 링크를 눌러보기 전까지** 전 메시지의 링크가 죽어 있다.
게다가 이 프로젝트는 링크를 fallback 상수 + 저장소 변수 **두 층**으로 두는데,
둘 다 같은 오답으로 맞춰 놓으면 이중화가 아무 의미가 없다.

→ `curl -o /dev/null -w "%{http_code}"`로 **양쪽을 다 찍어** 확정한 뒤 되돌렸다.
→ `test_dashboard_url_default_matches_env_example`로 두 선언이 어긋나는 것은 막았다
   (실제 도메인과의 일치는 테스트가 검증할 수 없다 — 네트워크가 필요하다).

**함께 확인한 배포 점검법** (로그인한 브라우저로는 못 잡는 것들):

```bash
curl -s -o /dev/null -w "%{http_code}" https://<url>/     # 401이면 Deployment Protection
curl -s https://<url>/api/cost                            # available:false면 서버 키 누락
```

배포자 브라우저는 Vercel에 로그인돼 있어 보호가 켜져 있어도 **그냥 열린다.**
`available:false`와 `spentUsd:0`도 화면에서는 둘 다 "0"처럼 보인다(T51).
**미인증 요청으로 확인하라 — 그게 폰과 같은 조건이다.**

## T57. 폴더명을 바꾸면 **editable 설치가 절반만 깨진다** `[HD 2026-08-17 실측]`

프로젝트 폴더명을 바꿔도 코드는 멀쩡하다 — 루트를 전부 `Path(__file__).parents[N]`으로
계산하고(T48), 추적 파일에 절대경로가 **0건**인 것을 `git grep`으로 확인했다.

깨지는 건 **venv 쪽**이다. `pip install -e .`가 절대경로를 박아둔다:

```
.venv/Lib/site-packages/__editable___heimdallr_call_0_1_0_finder.py
  → C:\Claude\dev\Heinmdallr_Call\src
```

**무엇이 조용히 틀리는가:** 완전히 죽지 않고 **절반만** 죽는다.
프로젝트 폴더 **안에서** 실행하면 현재 디렉터리 덕에 `import src`가 되고,
`pyproject.toml`의 `pythonpath = ["."]` 때문에 pytest도 대체로 통과한다.
그런데 다른 위치에서 부르거나 설치된 패키지로 참조하는 경로는 옛 폴더를 가리킨다 —
"되는 것 같은데 가끔 안 되는" 형태라 원인을 찾는 데 시간이 든다.

→ 폴더명을 바꿨으면 **반드시** 새 위치에서 다시 깔아라:

```bash
.venv/Scripts/python.exe -m pip install -e ".[dev]"
python -m pytest tests/ -q        # 기준선: 413 passed, 1 skipped
```

`.[dev]`를 빼면 T53(워크플로 검증 25건 조용한 skip)을 그대로 밟는다.
**passed 수만 보지 말고 skip 수까지 기준선과 대조하라.**

`pyvenv.cfg`의 `home`은 파이썬 본체(`C:\Python314`)를 가리키므로 venv를 새로 만들 필요는 없다.
(`pyvenv.cfg`의 `command` 줄에는 옛 경로가 남지만 **기록용이라 동작에 영향이 없다** — 고치지 말 것.)

**실제로 발생했다 `[2026-08-17 실측]`** — 폴더를 `Heinmdallr_Call` → `Heimdallr_Call`로 바꾼 뒤:

```
프로젝트 루트에서:  python -c "import src"  →  OK       ← 여기만 보면 정상으로 보인다
루트 밖(cd /c)에서: python -c "import src"  →  ModuleNotFoundError: No module named 'src'
```

**pytest도 통과한다**(`pythonpath = ["."]`). 즉 "테스트 초록"이 이 함정의 증거가 되지 못한다 —
**검증은 반드시 프로젝트 밖에서 `import src`를 해봐야 한다.** 재설치 후 finder가
`Heimdallr_Call`로 갱신됐고 루트 밖 import·`413 passed, 1 skipped` 모두 확인했다.

---

## 명명 규칙 — `Heimdallr`(m)로 통일, 봇 username만 예외 `[HD 2026-08-17 확정]`

코드·GitHub repo·Vercel 도메인·시크릿 이름·대시보드 표기는 전부 **`Heimdallr`**다.

**봇 username `@Heinmdallr_bot`만 다르다.** 텔레그램이 봇 username 변경을 지원하지 않아서다
(BotFather에 `Edit Name`은 있어도 `Edit Username`은 없다). 바꾸려면 새 봇을 만들어야 하고
= 새 토큰 = 시크릿 재등록 = 기존 대화 단절이다. **그 값어치가 없다.**

**동작에는 무관하다.** 코드는 username을 어디서도 참조하지 않는다
(`src`·`dashboard`·`.github` 전수 `git grep` 0건). 봇 판정은 토큰에서 뽑은 숫자 ID로 한다:

```python
def bot_id_of(token: str) -> str:
    return token.split(":", 1)[0].strip()      # 8933940541
```

표시명은 `아이언맨의 Heimdallr`로 변경 완료(실조회 확인). 사용자 눈에 보이는 곳은 통일됐다.
**다음 세션이 "이름이 왜 다르지"로 다시 시간 쓰지 말 것.**

---

## T58. DART **회사명 검색 URL은 검색을 하지 않는다** `[HD 2026-08-17 실측]`

상세화면의 "DART 공시 원문" 링크가 이렇게 돼 있었다:

```
https://dart.fss.or.kr/dsab007/main.do?textCrpNm={회사명}
```

**무엇이 조용히 틀리는가:** 링크는 **200**이고 페이지도 정상이며 검색창에 회사명까지
채워져 있다. 그런데 **결과가 하나도 안 나온다.** 죽은 링크가 아니라
"살아 있는데 아무것도 없는" 링크라 오류로 보이지 않는다 — 사용자가 "조회가 안 된다"고
말해 주기 전까지 아무도 모른다.

실측으로 원인을 확정했다. 파라미터를 준 응답과 안 준 응답을 통째로 diff하면
**차이가 `<input … value="삼성전자">` 두 줄, 24바이트뿐**이다:

```bash
$ curl -s ".../dsab007/main.do?textCrpNm=%EC%82%BC%EC%84%B1%EC%A0%84%EC%9E%90" -o a.html   # 112,035 B
$ curl -s ".../dsab007/main.do"                                                 -o b.html   # 112,011 B
$ diff a.html b.html   # → input value= 두 줄만 다르다
```

페이지의 `$(document).ready`를 읽어 보면 **어디에서도 `search()`를 부르지 않는다.**
셀렉트 박스만 세팅하고 끝난다. `autoSearch` 파라미터도 먹지 않는다.

→ **공시 원문은 접수번호로만 열린다.** 그리고 우리는 이미 갖고 있다
(`earnings_disclosures.rcept_no`, 1,558행 / 1,091종목).

```
https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20260814003699
  → <title>삼성전자/반기보고서/2026.08.14</title>     ✓ 실호출 확인
```

**접수번호가 없으면 링크를 만들지 않는다.** 회사명 검색 주소를 대신 넣으면 위 함정이
그대로 되살아난다 — 대신 네이버 공시 목록으로 보내고 그 사실을 밝힌다.
유니버스 1,322종목 중 231종목이 여기 해당한다.

조립은 `src/notify/links.py`와 `dashboard/lib/links.ts` **두 곳에서만** 한다.
규칙이 갈라지면 텔레그램과 화면이 서로 다른 곳을 가리키는데 **둘 다 200이라**
아무도 눈치채지 못한다. `tests/test_workflows.py`가 두 파일의 규칙 일치와
`dsab007`/`textCrpNm` 재등장을 막는다.

---

## T59. ★★ `force-dynamic`은 **fetch 캐시를 막지 못한다** `[HD 2026-08-17 실측]`

대시보드 전 페이지에 `export const dynamic = "force-dynamic"`이 걸려 있었다.
그런데도 **화면이 이틀 전 숫자를 보여줬다.**

게이트를 고쳐 DB의 발송 대상이 **67**이 된 직후:

| 경로 | 값 |
|---|---|
| DB (service key) | **67** ✓ |
| DB (anon key, 페이징) | **67** ✓ |
| node에서 supabase-js로 직접 재현 | **67** ✓ |
| 로컬 `next start` | **80** ✗ |
| 배포 사이트 (`X-Vercel-Cache: MISS`) | **80** ✗ |

**무엇이 조용히 틀리는가:** HTTP 200, 화면 정상, 에러 로그 없음, CDN 캐시도 MISS.
**숫자만 낡는다.** 쿼리 로직을 아무리 들여다봐도 안 나온다 — 재현해 보면 맞기 때문이다.

원인:

```
$ find .next/cache/fetch-cache -type f | wc -l
44                    ← 전부 8월 15일자 (이틀 전)
$ rm -rf .next/cache/fetch-cache && next start
발송 대상 67          ← 정상
```

`dynamic = "force-dynamic"`은 **페이지 렌더를 매번 다시 할 뿐**, 그 안에서 supabase-js가
부르는 `fetch`까지 막아 주지 않는다. Next는 그 응답을 `.next/cache/fetch-cache`에
**디스크로 남기고**, **Vercel은 그 빌드 캐시를 배포 사이에 복원한다.**
그래서 배포해도 안 고쳐진다.

→ 클라이언트를 만들 때 fetch를 감싸 캐시를 끈다. **`createClient` 호출 전부에 준다** —
페이지용 anon 클라이언트만 고치면 `/api/cost`(service_role)와
`/api/telegram/lookup`(HermesCall이 부른다)이 그대로 낡는다.

```ts
export const NO_STORE_OPTIONS = {
  global: {
    fetch: (input: RequestInfo | URL, init?: RequestInit) =>
      fetch(input, { ...init, cache: "no-store" as RequestCache }),
  },
};
export const supabase = createClient(url, anonKey, NO_STORE_OPTIONS);
```

**검증은 캐시를 지우고 하면 안 된다.** 캐시가 **남아 있는 상태에서** 최신 값이 나와야
고쳐진 것이다(실측: 캐시 파일 9개가 있는 채로 67 ✓, 새 캐시 파일도 안 늘어남).

`tests/test_workflows.py::test_every_supabase_client_disables_fetch_cache`가
`NO_STORE_OPTIONS` 없는 `createClient`를 막는다.

---

## T60. 게이트를 좁힐 때 **부호전환 종목이 조용히 사라진다** `[HD 2026-08-17 실측]`

"실적 가속 = 매출 **과** 영업이익 성장률이 둘 다 YoY 가속"으로 G2를 좁혔다.
단순하게 `op_yoy(t) > op_yoy(t−1)`만 요구하면 이렇게 된다:

```
현재 통과 343종목
  ├ 영업이익도 가속        182   → 통과
  ├ 가속 아님               69   → 탈락 (의도한 것)
  └ 부호전환이라 op_yoy=None 92   → ??? ★
       당분기 흑전 53 포함
```

**무엇이 조용히 틀리는가:** `op_yoy`는 부호가 바뀌는 구간에서 **애초에 None으로 저장된다**
(T25 — 적자↔흑자 사이에서 %를 만들면 안 되기 때문이다). 그 None을 `False`로 뭉개면
**흑전 종목 53개가 에러 없이 통째로 탈락한다.** 흑전은 이익 가속의 **가장 강한 형태**인데
정확히 그것만 사라지는, 최악의 방향으로 틀리는 형태다.

→ 라벨로 판정한다. 그리고 **전분기 성장률을 모르면 탈락이 아니라 판정 불가(None)**다.

```python
elif data.op_status_label == "흑전":
    result.g2 = True                       # %는 못 구하지만 이익이 는 건 명백하다
elif data.op_yoy_t is None or data.op_yoy_t1 is None:
    result.g2 = None                       # ★ False가 아니다
else:
    result.g2 = data.op_yoy_t > data.op_yoy_t1 and data.op_yoy_t > 0
```

`> 0`을 빼면 **영업이익 −30% → −10%가 '가속'으로 통과한다.** G1(매출)이 이미
`가속 AND 양(+)` 둘 다 요구하므로 대칭을 맞춘다.

실측 결과: 통과 **343 → 238** · 판정 불가 91 · 흑전 통과 76 · 발송 대상(★/○) 80 → 67.

**게이트를 만질 때는 반드시 "판정 불가로 빠지는 종목이 몇이고 왜인가"를 세어 보라.**
탈락 수만 보면 정상으로 보인다.
