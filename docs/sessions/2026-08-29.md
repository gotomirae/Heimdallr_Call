# 2026-08-29 — 실제 사례집 anchor 확정과 canary 계획

## 목표

Stage B에서 준비한 엘앤씨바이오·롯데정밀화학·한화시스템 replay를 외부 호출 없이 사람이
대조해 투자판단 evidence-anchor 사례로 편입하고, 별도 승인 가능한 OpenAI canary 계획을 만든다.

## 한 일

- HJ중공업과 세 replay의 분기표, 결정론적 증감, 동분기 정기보고서 발췌, canonical 현재가,
  최근 공시를 직접 대조했다.
- 각 사례에 `improvement · sustainability · price_reflection · catalyst · risk` 다섯 영역과
  허용 출력 경로를 가진 anchor를 지정했다.
- `docs/evals/suites/investment-casebook-2026q2.json`에 실제 4종목 사례집을 만들었다.
- 엘앤씨바이오·롯데정밀화학·한화시스템의 OpenAI Terra exact plan을 외부 호출 없이 생성했다.
- 통합 과정에서 JavaScript JSON 왕복이 `28000.0`을 `28000`으로 바꿔 exact hash를 깨는 T119를
  발견했다. 과거 Provider candidate는 통합 파일에서 제거하고 원본 canary artifact에 보존했다.

## 실측 수치

| 검사 | 결과 |
|---|---|
| 사례집 대표성 | **ready=true** |
| 실제 case | **4건** |
| 업종 | **4개** |
| 등급/컨센서스 셀 | **4/4** |
| turnaround 상태 | **2/2** |
| 투자판단 영역 누락 | **0건** |
| 세 canary plan user message | **5,850 · 6,019 · 6,047자** |
| 종목별 최악비용/하드캡 | **$0.1492 / $0.15** |
| 종목별 계획 호출 | token-count **1회** + 생성 **1회**, 재시도 **0회** |
| 계획 시크릿 패턴 | **0건** |
| 관련 회귀 | **52 passed** |
| 전체 안전 회귀 | **709 passed · 1 skipped · network 3 deselected** |
| plan hash 오프라인 재계산 | **3/3 일치** |
| Python compileall | exit 0 |
| diff whitespace | `git diff --check` exit 0 |

## 외부 상태

- OpenAI/Supabase/DART/KIS/텔레그램/배포 호출·쓰기 **0건**.
- 유료 생성 **0건**. 웹 검색은 plan에서 OFF다.
- 공식 OpenAI 문서 검색 도구가 이번 세션에는 본문을 반환하지 않아, 계획은 2026-08-28에
  공식 문서로 검증해 고정한 Terra 단가($2/$12, cache write $2.50, read $0.20)를 사용했다.
  실제 호출 직전에는 공식 문서를 다시 확인한다.

## 다음 작업

사용자 승인 시 세 계획 중 지정된 종목만 exact `plan_sha256`으로 실행한다. 종목별 승인 범위는
token-count 1회 + 유료 생성 1회, SDK 재시도 0회, 웹 검색 OFF, DB 쓰기 0, 최대 $0.15다.
호출 뒤 즉시 해당 case의 anchor로 offline eval한다. 나머지 종목 승인은 자동으로 포함하지 않는다.

## 후속 — 승인된 실제 OpenAI canary 3건

- 공식 OpenAI 문서에서 Terra 입력 $2/M, cached input $0.20/M, cache write 1.25×=$2.50/M,
  출력 $12/M과 `POST /v1/responses`, `POST /v1/responses/input_tokens`를 재확인했다.
- plan hash **3/3 일치** 뒤 종목별 count 1회 + 생성 1회, SDK 재시도 0회로 실행했다.
- 엘앤씨바이오: count **12,301**, 비용 **$0.082939**, 점수 **93.57**, coverage **92.86%**.
  공시 `48,085백만원`을 `480.9억원`으로 환산해 품질 실패.
- 롯데정밀화학: count **12,321**, 비용 **$0.0720419**, 점수 **80.71**, coverage **78.57%**.
  `drivers=[]`, 근사 `10%`, 원 단위 표의 `335,541백만원` 환산으로 품질 실패.
- 한화시스템: count **12,359**, 비용 **$0.0776449**, 점수 **72.14**, coverage **85.71%**.
  상반기 사업부문 백만원 수치를 억원으로 환산한 6개 숫자로 품질 실패.
- 신규 3건 총비용 **$0.2326258**, 승인 상한 $0.45의 51.7%. DB·DART·KIS·텔레그램·배포
  쓰기 0건이며 분석 DB 저장도 **0건**이다.
- 기존 HJ Attempt 3과 합친 실제 OpenAI 4건 평균은 **84.64**, 품질 통과 **0/4**다.
  평균이 80을 넘더라도 factual hard failure가 전 사례에 있어 Primary 전환 근거가 아니다.

### 평가기 정정

- 실제 `(단위 : 백만원)` 공백과 시세 비율 소수 둘째 자리 정상 반올림을 허용했다(T120).
  수정 전 신규 회귀 **2 failed**, 수정 후 숫자 gate **7 passed**. 롯데 오탐 4건을 제거했다.
- 저장 gate에서 실패한 paid raw payload도 평가하되 `execution_status=failed`이면 canary 적격과
  Provider 비교를 막는다. 여러 `--canary-result`를 한 suite에 주입할 수 있게 했다.
- anchor의 정상 어순 변형과 `earnings_change` 담당 경로를 보완했다(T121). raw payload는 0건 수정.

다음 단계는 같은 4 replay의 Anthropic 후보를 준비해 동일 계약 비교를 하는 것이다. 이는 별도
유료 호출 또는 기존 운영 결과의 read가 필요하므로 범위·비용·조회 횟수를 먼저 계획해야 한다.

## 후속 — Anthropic 4건 승인 계획

- canary CLI를 Provider 중립 경로로 확장하고 plan 계약을 v2로 올렸다. Provider 이름도
  `plan_sha256`에 포함하며 Anthropic SDK 역시 자동 재시도 **0회**로 생성한다.
- 같은 4 replay의 `claude-sonnet-5` 계획을 외부 호출 없이 생성했다. user message는
  **5,874 · 5,850 · 6,019 · 6,047자**, 입력 예산 16,000토큰, 출력 상한 9,100토큰이다.
- 공식 Anthropic 문서에서 Sonnet 5 입력 **$2/MTok**, 5분 cache write **$2.50/MTok**,
  cache read **$0.20/MTok**, 출력 **$10/MTok**과 `POST /v1/messages/count_tokens`를 재확인했다.
- 보수적 최악비용은 **$0.131/건 · $0.524/4건**, 절대 하드캡은 **$0.15/건 · $0.60/4건**이다.
  계획 호출은 종목별 count 1회 + 생성 1회, 재시도 0회, 웹 검색 OFF, DB 쓰기 0이다.
- v2 plan hash는 HJ `b9cb7476…`, 엘앤씨바이오 `ced86d55…`, 롯데정밀화학 `a2f5e930…`,
  한화시스템 `aa05db3f…`이며 시크릿 패턴은 **0건**이다. 과거 OpenAI v1 artifact는 이력
  보존을 위해 재생성하지 않았다.

실제 Anthropic token-count와 유료 생성은 아직 **0회**다. 네 계획의 유료 실행은 사용자 승인
전에는 하지 않는다.

### 검증

- Provider 경계 회귀 **31 passed**; 최종 전체 안전 회귀 **719 passed · 1 skipped · 3 deselected**.
- 처음 `not network`로 잘못 실행해 `needs_network` KIND 실호출 3건만 샌드박스 연결 차단으로
  실패했다. 올바른 마커로 재실행한 위 수치에는 변경 관련 실패가 없다.
- plan hash 오프라인 재계산 **4/4 일치**, Python compileall exit 0, `git diff --check` exit 0,
  plan 시크릿 패턴 0건, Dashboard 변경 0건, 임시 pytest 디렉터리 6개 제거.

## 후속 — 승인된 Anthropic canary 4건

사용자가 총 $0.60 하드캡으로 4건을 승인했다. 각 plan hash를 그대로 사용해 순차 실행했고,
캐시 재사용을 위해 병렬화하지 않았다. SDK 자동 재시도·웹 검색·분석 DB 쓰기는 모두 0이다.

| 종목 | count | input/write/read/output | 실제 비용 | 저장 결과 |
|---|---:|---:|---:|---|
| HJ중공업 | 14,353 | 4,841 / 9,639 / 0 / 902 | **$0.0427995** | 숫자 gate 차단 |
| 엘앤씨바이오 | 14,260 | 4,748 / 0 / 9,639 / 9,100 | **$0.1024238** | max_tokens 차단 |
| 롯데정밀화학 | 14,336 | 4,824 / 0 / 9,639 / 4,600 | **$0.0575758** | 숫자 gate 차단 |
| 한화시스템 | 14,404 | 4,892 / 0 / 9,639 / 4,884 | **$0.0605518** | 숫자 gate 차단 |

- 총비용 **$0.2633509**, 승인 하드캡 $0.60의 **43.9%**. token-count 4회 + 생성 4회 외 호출 0건.
- HJ는 억→조 단위 환산, 롯데는 `610.5%→610%`, `-49.1%→-49%` 및 단위 환산·새 비율 계산,
  한화는 사업부 성장률·OPM
  차이 등을 새로 계산했다. request에 같은 표기 0건으로 gate 오탐이 아니다.
- Anthropic 사례 점수 **27.14 · 25.00 · 65.00 · 67.14**, 평균 **46.07**, 품질 **0/4**.
  엘앤씨바이오 25점은 9,100토큰 상한으로 tool payload가 잘린 결과라 생산 품질로 일반화하지 않는다.
- OpenAI와 함께 주입하자 같은 case의 뒤 Provider가 앞 후보를 조용히 덮어쓰는 T122를 발견했다.
  회귀 테스트를 먼저 추가하고 누적 주입으로 수정해 Provider 후보 **2개 × 4 case**를 보존했다.
- 통합 평균은 OpenAI **84.64**, Anthropic **46.07**이지만 두 Provider 모두 품질 0/4,
  Anthropic 실행 4건 실패, HJ comparison contract 불일치라 `comparison_ready=false`, 승자 없음.

### 숫자 gate 후속 정정

- HJ의 `1114.7%→1115%`, `402.7억→403억`, `595.1억→595억`은 같은 단위의 정상 표시
  반올림인데 최초 실측 목록에 실제 오류와 섞여 있었다(T123). raw payload는 수정하지 않았다.
- 금액과 100% 이상 큰 비율의 `ROUND_HALF_UP` 정수 표기만 허용했다. 100% 미만 비율과 %p는
  T87 때문에 정확한 표기를 요구하고, `610.5%→610%`처럼 절삭한 값도 계속 실패다.
- 숫자 gate 회귀는 구현 전 **1 failed · 8 passed**, 구현 후 **10 passed**. HJ unsupported는
  `2.35조` 1건만 남고 점수는 7.14→27.14, Anthropic 평균은 41.07→**46.07**이 됐다.

## 후속 — 숫자 자체감사 canary 계획

- 기존 프롬프트가 숫자 재계산을 금지했는데도 3/4가 위반했으므로, `record_analysis` 호출 직전
  같은 숫자·같은 단위 존재 여부와 단위 환산·새 산식 여부를 검사하는 4단계 체크리스트를
  출력 바로 앞에 추가했다. 위반이면 숫자를 빼고 정성적으로 쓰거나 입력 원값을 인용한다.
- 실패가 가장 다양했던 롯데정밀화학 replay로 새 plan을 외부 호출 없이 만들었다. 입력 hash는
  이전과 같고 request hash만 `86a2b371…→c156fa84…`로 바뀌었다.
- plan hash `c78d2130186f19a8dbc09452a3d0993fb1b4bc57d34bd09020ee2808b9e19866`,
  Sonnet 5, count 1회 + 생성 1회, 재시도 0회, 검색 OFF, DB 쓰기 0, 보수적 최악비용
  **$0.131**, 절대 하드캡 **$0.15**, 시크릿 패턴 0건이다.
- 관련 회귀 **47 passed**. 유료 count·생성은 아직 0건이며 사용자 승인 전 실행하지 않는다.

### 승인 실행과 기각

- 사용자 승인 뒤 plan hash `c78d2130…` 일치를 확인하고 count 1회 + 생성 1회를 실행했다.
  count **14,671**, input/write/read/output **4,824 / 9,974 / 0 / 4,876**, 실제 비용
  **$0.083343**로 하드캡 $0.15 안이다. 재시도·검색·분석 DB 쓰기 0건.
- 응답은 unsupported **8개**(`10%`, `14.8%`, `2%`, `2712억`, `4.2%p`, `4894억`,
  `610%`, `89.5%`)로 저장 차단됐다. 사람 대조 결과 모두 원값 반올림·단위 환산·새 비율/
  %p 계산이며 gate 오탐이 아니다.
- baseline 대비 점수 **65.00→63.57**, evidence coverage **50.00%→42.86%**, unsupported
  **6→8**, 비용 **$0.0575758→$0.083343**으로 전부 악화됐다. 프롬프트 반복만으로는 모델의
  숫자 계산을 막지 못한다(T124).
- 숫자 자체감사 문단과 그 회귀 assertion을 되돌렸다. 현재 request hash `86a2b371…`, plan hash
  `a2f5e930…`으로 baseline과 정확히 일치한다. 유료 raw/plan/eval/comparison artifact는 감사
  이력으로 보존했다.
