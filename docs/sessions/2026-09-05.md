# 2026-09-05 — 마지막 차트 수정 운영 반영 확인

## 한 일 / 실측

- 이전 세션 이후 최종 커밋 `2338b1eef8385d6fcd7c9f9d12a3b6fc158b83f2`의 운영 반영을 재확인했다. `git ls-remote`의 원격 main과 로컬 HEAD가 동일하다.
- 해당 커밋 CI run `33884449133` 성공. 실제 로그는 **779 passed, 5 skipped, 15.78s**이며 진입점 임포트 확인도 성공했다.
- 같은 커밋의 `telegram_listen` run `33898101817`, `llm_batch` run `33915578163` 성공을 읽기 전용으로 확인했다.
- 운영 고영 상세 `/stock/098460` 응답 **HTTP 200, 189,939 bytes**. HTML에 `영업이익 · OPM`, `MACD`, `RSI`가 모두 존재했다. 이번 검증은 HTTP 응답 확인이며 브라우저 차트 시각 검증은 새로 수행하지 않았다.

## 막힌 것 / 다음 세션

- 이번 이어가기에서 미커밋 구현 변경이나 미배포 커밋은 발견되지 않았다. 마지막 차트 수정까지 운영 반영 확인을 마쳤다.
- 기본 Python 3.14에는 pytest가 없어 로컬 테스트 시도는 실행되지 않았다. 최신 커밋의 깨끗한 CI 환경 실행 로그로 확인했다. 이전 세션의 780건 결과와 이번 CI 결과를 혼용하지 않는다.
- 이번 세션은 코드 변경·DB 쓰기·유료 API 호출·텔레그램 발송을 수행하지 않았다. 새 함정이나 설계 결정 없음.

## 추가 요청 — 변경 자료 자동 반영

- 사용자 요청에 따라 공시 폴링을 매일 09:00~23:30 KST 30분 간격, 야간 3시간 간격으로 전환했다.
  SEASON_MODE 수동 전환 의존을 제거하고 최근 7일 재조회를 적용했다. GitHub 스케줄은 지연될 수 있다.
- 정정 발췌는 최신 접수번호를 수집·선택한다. 같은 날 새 공시는 detected_at까지 비교한다.
  처리한 잠정공시는 다시 파싱하지 않으며 기존 확정 재무를 잠정치로 덮어쓰지 않는다.
- 재무·발췌 내용 hash를 분석 메타에 저장하고 변경되면 재분석한다. 야간 배치에도 갱신 옵션을 배선했다.
  일일 시세 변경만으로 유료 재호출하지 않으며 기존 월·일 비용 상한과 성장 가속 대상 제한을 유지한다.
- 일일 배치에 전 유니버스 네이버 컨센서스/PER 수집과 정밀 재무 보충·재스크리닝을 연결했다.
- 열린 화면은 60초 간격으로 최신 서버 데이터를 읽고 탭 복귀·온라인 복구 때도 갱신한다.
  AI 해석은 재분석 전까지 이전 결과임을 안내한다.
- 결과 추적은 60분 상한·100행 분할 저장으로 바꿨다. 최초 저장된 발표 당시 등급·점수를 보존하며,
  일시적 조회 실패로 기존 측정 수익률을 NULL로 지우지 않는다.

### 실측

- 운영 점검: 분석 대상 **209**, 기존 분석 없음 **3**, 변경·확정 갱신 후보 **200**.
  후보는 레거시의 근거 갱신 시각 판정도 포함한다. 200건을 이번 세션에서 유료 재분석한 것은 아니다.
- 컨센서스 기존 **1,579행**, 최신 **2026-08-31T14:11:52Z**로 수집 누락 확인.
- 정정 발췌 **2/2 성공**, 각각 **2,460자·2,464자**, 재실행 대상 **0건**.
- 컨센서스 실수집 **3/3종목·6행 저장**, 결과 추적 **3행 저장**, D-5/D0/D+1/D+5 각 **3/3 측정**.
- 결과 추적 실패 run `33818739910`: 대상 **1,086**, 200건까지 진행 후 15분 상한으로 취소.
- 전체 **788 passed·1 skipped**, 마지막 추가 회귀 **116 passed·1 skipped**. 초기 대시보드 build **10/10** 성공.
- 새 함정 T136. 유료 LLM·텔레그램 발송·주문 API 호출 없음. 전체 컨센서스와 AI 갱신은 운영 배치가 이어간다.
