# 2026-08-24 — 정상 실행 점검 · 발췌 파이프라인의 조용한 결함 2건

## 요약

지난 세션에서 만든 **공시 발췌 → LLM** 배선이 실제로 도는지 확인하러 들어갔다.
테스트는 전부 초록불이었고 화면도 정상이었는데, **발췌가 제 몫을 하고 있지 않았다.**
둘 다 에러도 경고도 없이 품질만 나빠지는 종류다(T99 · T100).

## 시작 시점 상태

| | 값 |
|---|---|
| 테스트 | 578 passed · 1 skipped (기준선 555에서 +23) |
| `disclosure_excerpts` DDL | **적용돼 있었다** (지난 세션 미결 항목 해소) |
| 저장된 발췌 | 453건 (중앙값 2,461자 · 0자 행 없음) |
| `analyses` | 385건 (2026 2Q 290건) |
| 월 비용 | $16.79 / $20 |

## 발견 1 — 발췌가 **어느 분기 것인지 몰랐다** (T99)

453건 전부 `fiscal_year`/`fiscal_quarter`가 **NULL**이었다. 거슬러 올라가니
원인은 발췌가 아니라 그 위였다 — `earnings_disclosures` **1,576행 전부 NULL**.
`replay._save()`가 그 두 칸을 payload에 **한 번도 넣은 적이 없다.**

읽는 쪽(`load_excerpt`)은 그 칸으로 분기를 맞춰 보고, 안 맞으면 "가장 최근 것"으로
물러선다. 그래서 **폴백이 항상 성공했다** — 발췌가 한 분기치뿐이라 "가장 최근 것"이
곧 "이번 분기 것"이었기 때문이다.

**다음 분기에 터진다.** 3Q 분석이 도는 시점엔 3Q 발췌가 아직 없으므로 폴백이
2Q 원문을 3Q 분석에 싣고, 모델은 그걸 이번 분기 사실로 읽는다.

### 고친 것

- `dart_disclosure.period_of()` 신설 — 공시명 `(YYYY.MM)` → (연도, 분기).
  **12월 결산이 아니면 추측하지 않고 `(None, None)`** (`반기보고서 (2026.05)` —
  현대약품·JTC 2건 실측).
- `_save()`가 그 값을 싣는다. 잠정실적 공시는 이름에 기간이 없어 NULL이 정상.
- 폴백 정렬 키가 전부 `(0, 0)`으로 뭉쳐 `max()`가 무작위였다 → **접수번호 tie-break**.
- 발췌 앞에 **출처 라벨**을 붙인다. 다른 분기 것이면 경고문까지 붙인다:
  `★ 2026년 2분기 정기보고서 — **2026년 3분기 것이 아니다.** 이번 분기 사건으로 쓰지 마라.`
- 기존 데이터 백필: `python -m src.db.backfill_periods --save`

### 백필 실측

| 대상 | 채움 | 남긴 NULL |
|---|---|---|
| `earnings_disclosures` | **1,154행** | 422행 (잠정공시 418 · 비12월결산 4) |
| `disclosure_excerpts` | **452행** | 1행 |

백필 후 — 게이트 통과 262종목 중 **257종목이 같은 분기 발췌 보유**
(발송 등급 ★/○ 102종목 중 **101종목**).

## 발견 2 — 발췌의 **97%가 잘려 나가고 있었다** (T100)

수집기는 2,400자로 뽑는데(`dart_excerpt.DEFAULT_BUDGET_CHARS`) 분석기가
2,000자에서 잘랐다(`analyze.EXCERPT_MAX_CHARS`). **다른 파일에 각자 박힌 숫자였다.**

| | 옛 상한 2,000 | 새 상한 2,600 |
|---|---|---|
| 잘린 건수 | **438 / 453 (97%)** | **0** |
| 평균 손실 | **513자** | — |
| 마지막 절이 잘린 건수 | **438** | 0 |

절 순서상 잘려나가는 건 **언제나 `주요계약 및 연구개발활동`** —
수주계약·국책과제가 적힌, 발췌를 도입한 이유 그 자체인 절이다(T93).
한 종목 실측: 그 절 736자 중 **203자만 남았다(72% 손실)**.

**검사기도 죽어 있었다.** `test_excerpt_is_truncated`가 `"x" * 2_001`을 숫자로 박아
둬 수집기가 2,400으로 올라간 뒤에도 초록불이었다(T82와 같은 모양).

### 고친 것

- `constants.py` 한 곳으로 통일: `EXCERPT_BUDGET_CHARS`(2400) ·
  `EXCERPT_LABEL_HEADROOM_CHARS`(200) · `EXCERPT_MAX_CHARS`(= 합, 2600).
- `tests/test_excerpt_budget.py` — **`분석기 상한 > 수집기 예산`** 부등식을 지킨다.
- 기존 검사의 하드코딩을 상수 참조로 교체.
- PRD §7.1은 숫자를 **지우고 §7.3을 가리키게** 했다(대조 테스트가 §7.3·부록만 훑는다).

## 검증 (T54 — 검사기가 실제로 걸리는지 따로 확인했다)

고친 코드를 **되돌려 놓고** 새 테스트가 빨간불이 되는지 봤다.

| 되돌린 것 | 결과 |
|---|---|
| `_save`의 기간 배선 제거 | 2건 FAIL ✓ |
| `render`의 출처 라벨 제거 | 3건 FAIL ✓ |

라벨 제거 실험은 첫 두 번 **replace가 조용히 실패**해 초록불이 나왔다
(Git Bash 힙독으로 넘긴 한글이 깨졌다). `replaced: False`를 찍어 보고서야 알았다 —
**"테스트가 통과했다"를 확인하기 전에 "고친 것이 실제로 되돌아갔는지"부터 확인해야 한다.**

## 실행 검증 (실제 API 호출)

`--refresh-before`로 3건 재분석. 전부 성공 · 실패 0건.

```
✓ 이구산업(025820)  $0.0690 (캐시 미스)
✓ 미래반도체(254490) $0.0470 (캐시 히트)
✓ 이구산업(025820)  $0.0725 (상한 수정 후 재실행)
```

입력 토큰 실측 **11,662** — `LLM_INPUT_TOKEN_BUDGET`(14,000) 이내.
발췌 2,550자가 **잘리지 않고 통째로** 실렸고, 머리에 `[출처: 2026년 2분기 정기보고서]`가 붙었다.

분기 불일치 경로도 실측 확인 — `load_excerpt('000020', 2026, 3)`이
경고문이 붙은 2Q 발췌를 돌려줬다.

## 재분석 실행 (사용자 결정)

**비용 추정을 한 번 정정했다.** 처음 제시한 "★/○ 102종목 ≈ $3.5"는 **발췌 이전
단가($0.0362)**로 계산한 것이라 틀렸다. 발췌를 실으니 입력만 는 게 아니라
**출력이 함께 늘었다** — 비용의 대부분이 출력이라(T69) 체감 증가폭이 훨씬 크다.

| | 발췌 이전(149건) | 발췌 이후 |
|---|---|---|
| 출력 중앙 | 2,911토큰 | **4,369토큰 (+50%)** |
| 건당 비용 | $0.0362 | **$0.0560** (캐시 히트 5/6) |

→ 게이트 통과 262종목 분기 비용도 $9.5 → **약 $15**로 다시 잡아야 한다.
→ 월 실링 **$20 → $24** (사용자 결정). **일시 증액이 아니라 상시 값이다.**

`--min-score 101 --refresh-before 2026-08-24` — 발송 등급 ★/○ **105종목** 재분석.

### 발췌가 실제로 해석을 바꿨는가 (실측)

원문 없이는 나올 수 없는 내용이 본문에 들어왔다.

- **HJ중공업** — "공시상 조선 신조선 매출이 상반기 누적 **4,928억(비중 38.77%)**",
  "건축공사 2,776 → 4,706억 **매출유형별 표**" (← `매출 및 수주상황` 절)
- **롯데정밀화학** — "케미칼(ECH·가성소다·유록스) 및 암모니아 상품 매출이 반기 기준
  각각 **4,894억·3,119억**으로 전체의 73%", 원재료 **프로필렌·메탄올** 가격
  (← `매출 및 수주상황` + `원재료 및 생산설비` 절)

숫자표만 줬을 때는 "매출이 늘었다"까지밖에 못 쓰던 자리에 **무엇이 얼마나 늘었는지**가
들어왔다. T93이 노린 것이 이것이다.

## 이전 상태 — 왜 재분석이 필요했나

★★ **2026 2Q 분석 290건 중 발췌를 실제로 쓴 것은 3건뿐이다.**
나머지 287건은 발췌 수집(8/23 12:29)보다 **먼저** 만들어졌다 — 즉 지금 대시보드·
텔레그램에 나가는 분석은 대부분 **숫자표만 보고 쓴 것**이다.

- 게이트 통과 262종목 전량 재분석 = 약 **$9** (건당 $0.034)
- 남은 이번 달 예산 = **$3.03** ($16.97 / $20)

→ **선택지는 셋이다. 사용자 결정 사항이다.**
  1. 발송 등급 ★/○ **102종목만** 재분석 ≈ $3.5 — 실링 소폭 초과
  2. 9월 1일에 전량 재분석 (실링 리셋 후)
  3. `MONTHLY_COST_CEILING_USD`를 상향

## 후반 — "LLM 분석이 부실하다"의 정체 (사용자 지적)

사용자가 기업별 실적 탭의 분석이 부실하다며 **모델 교체**(Opus 5 / 타사)를 물었다.
갈라서 재보니 **모델 문제가 아니었다**(T101).

- 게이트 통과 262종목 중 **분석 행 없음 0건** · 필드 결측 **2%** · 화면 키 불일치 **없음**
- 진짜 원인: **발췌 없이 쓰인 옛 분석이 226/262 = 86%**
- 그리고 **DB에 있는데 입력에 넣은 적 없는 것 3가지**를 찾았다

| 빠져 있던 것 | 무엇이 망가졌나 |
|---|---|
| 분기말 종가 13분기 | `price_history`를 쓰라면서 **주가 궤적을 안 줬다** |
| 최근 공시 목록 | 트리거 `expected_date`를 **짐작으로** 잡았다 |
| 데이터 기준일 | 모델이 **지금이 언제인지 몰랐다** |

프롬프트에 *"주어진 과거 주가 정보는 52주 고저뿐"*이라고 못박혀 있어서,
모델은 **시킬 수 없는 일을 지시받고 있었다.** 그 문장부터 갱신했다.

마무리 지시도 *"각 필드는 간결하게"*였다 — 사용자 요구와 정반대라
**숫자 동반 · 고유명사 유지 · 판단에 반증조건 첨부**로 바꿨다.

**실측(HJ중공업)** — 새 입력으로 나온 본문:
> "2025.3Q 28,000원까지 급등했다가 현재 16,810원까지 −16.6% 3개월 연속 하락 —
> **실적 개선이 주가에 후행 반영되는 구간**"

주가 궤적과 실적을 나란히 놓고 국면을 판정했다. 이전 입력으로는 불가능하다.

### 모델 선택 (사용자 결정: Sonnet 5 유지)

실측 토큰 기준 — Opus 5는 **2.5배**($0.133 vs $0.053/건 · 262종목 $34.7 vs $13.9).
비용의 대부분이 출력인데 Opus 5는 출력 단가가 $10 → $25다.
**같은 입력으로 비교해야 모델 차이를 잰다**(T90) — 발췌 없는 Sonnet과 발췌 있는
Opus를 비교하면 또 틀린 결론이 난다.

### 딸려 나온 상한 세 개

| 상수 | 전 | 후 | 이유 |
|---|---|---|---|
| `LLM_INPUT_TOKEN_BUDGET` | 14,000 | **16,000** | 실측 최대 13,768 — 여유 232로는 긴 발췌가 실패 |
| `LLM_MAX_TOKENS` | 12,288 | **16,384** | 출력 중앙 2,911 → **4,989**. 9건 중 1건이 잘려 **$0.13 버려짐** |
| `MONTHLY_COST_CEILING_USD` | 20 | **24** | 건당 $0.036 → $0.056 (출력 +42%) |

★ PRD §7.1 제목이 **아직 "5,000토큰"**이었다 — T97에서 고쳤다던 값이 남아 있었다.

### 반복 결제 버그 (T102)

`save()`가 upsert인데 `created_at`을 안 넣어 **DB 기본값이 안 걸렸다**(INSERT 전용).
`--refresh-before`가 그 칸으로 판정하므로 **방금 다시 돌린 종목이 계속 다시 대상**이
된다 — 배치가 끊겼다 재개될 때마다 상위 종목만 반복 결제한다. 이 세션에서 배치가
두 번 끊겼으니 실제로 새어 나갈 뻔했다. → `save()`가 현재 시각을 직접 찍는다.
실측: 재시작 시 `이미 분석됨 6종목`으로 정확히 건너뛴다.

### 화면 — 구조가 깨진 분석을 빈칸으로 삼키지 않는다

`earnings_change`가 **문자열로 저장된 7건**(T95 잔재)에서 `asRecord`가 null을 줘
**화면이 통째로 빈칸**이었다. 본문은 멀쩡한데 읽는 쪽이 버린 것이다.
→ 문자열이면 싣되 **정상인 척 보여주지 않는다.** 실측(동부건설)에서 그 문자열은
`{"cause":","` 로 시작하는 JSON 파편에 한국어까지 뭉개져 있었다
("실전측면 검증합영 유하다") — 그대로 내보내면 독자가 분석 결과로 읽는다.
**"⚠ 저장 구조가 깨졌습니다 … 투자 판단에 쓰지 마십시오"** 경고를 붙였다.
브라우저에서 렌더 확인 · `tsc --noEmit` 통과.

### 배치 운영에서 배운 것

- **`| tee`가 죽으면 파이썬도 죽는다.** 파이프 버퍼(약 64KB)가 차는 순간 stdout
  쓰기가 터진다 — 36건 돌다 exit 4로 죽은 원인이 이것이었다. **로그는 리다이렉트로.**
- `--refresh-before`는 **오늘 날짜**를 준다. 내일 날짜를 주면 오늘 한 것까지 낡은
  것으로 본다.

## 발송등급 88종목 완주 (사용자 지시)

| | |
|---|---|
| ★/○ | **88 / 88 완료** · 실패 0 · 남김 0 |
| 타입 깨짐(★/○) | **0건** |
| 월 비용 | **$25.32 / $27** |
| 소요 | 66초/건 |

상한 두 개를 더 올렸다 — **둘 다 실측 근거가 있다.**
- 실링 $24 → **$27**: 실측 43건 평균 **$0.0622**(중앙 $0.0599 · 최대 $0.1336).
  $24로는 40건에서 멈춘다.
- 일 상한 80 → **120**: 오늘 41건을 쓴 상태라 80으로는 **11건이 다음 날로 밀린다.**
  일 상한은 **배치 크기 조절 장치이지 비용 방어가 아니다**(T83) — 올려도 실링을
  넘을 수는 없다.

### 마지막 1건 — 검증기가 타입을 안 봤다 (T103)

금강공업이 **재분석 후에도** `earnings_change`가 `'{"cause":">skip'` 15자
문자열이었다. 다른 필드는 전부 정상 — 모델의 일회성 오류다.
문제는 **`validate_payload`가 이걸 통과시킨 것**이다: `None`도 `""`도 `{}`도
아니므로 "있고 비어 있지 않다"로 판정됐고, 배치는 `⚠` 없이 `✓`를 찍었다.

→ `ANALYSIS_SCHEMA`에서 `type`을 끌어와 **실제 타입과 대조**한다.
→ 검증기 자신도 방어했다. `payload.get("scenarios") or {}`는 문자열이면
  그대로 돌려주고 다음 줄에서 `AttributeError`로 죽는다 —
  **검증기가 죽으면 그 종목은 '원인 불명 실패'가 되고 진단 정보가 사라진다.**

★ 내 실수 하나: 금강공업 한 건을 고치려고 `--refresh-before 2026-08-27`을 줬더니
**전체가 낡은 것으로 잡혀** 상위 5건이 불필요하게 재분석됐다($0.3). 단건은
`python -m src.analysis.run --code 014280 --quarter 2026.2 --call --save`로.

## 다음 세션이 알아야 할 것

- **발췌 배선 자체는 끝났다.** 남은 건 "언제 돈을 써서 다시 돌리나"뿐이다.
- 재실행할 땐 `--refresh-before 2026-08-24`를 쓴다. 그 날짜 이전 분석만 낡은 것으로 본다.
- `analyses`는 `(code, 연, 분기)` upsert라 **`created_at`이 갱신되지 않는다.**
  재분석 여부를 `created_at`으로 판단하면 틀린다 — 이번에도 한 번 헷갈렸다.
- 테스트 **607 passed · 1 skipped** (578 → 607, +29).
