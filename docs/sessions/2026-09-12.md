# 2026-09-12 — 반도체 6분류·전후공정 표식

## 완료한 것

- 반도체 투자 섹터를 **IDM·DSP·OSAT·소재·부품·장비** 여섯 역할로 확장했다.
  DSP는 설계·IP·주문형 반도체, OSAT는 패키징·범핑·테스트 서비스 증거로 구분한다.
- 전·후공정은 섹터명과 분리해 소재·부품·장비의 주요제품 증거로 판정한다. OSAT는 `후`,
  IDM·DSP는 표식 없음, 근거 부족은 `None`으로 유지한다.
- 발굴 목록 섹터 셀에 `전`·`후`를 **9px** 작은 위첨자 배지로 표시했다.
- Python 분류·공정 함수와 TypeScript 화면 판정을 동일 생성 상수로 동기화하고 실제 표본
  대조기가 두 결과를 함께 비교하게 했다.

## 운영 실측

- `krx_universe` **1,376행** 저장: IDM **28**, DSP **12**, OSAT **9**, 소재 **12**,
  부품 **17**, 장비 **31** — 반도체 합계 **109종목**.
- 공정 표식: OSAT 후 **9/9**, 부품 전 6·후 10·미판정 1, 소재 전 10·미판정 2,
  장비 전 14·후 10·미판정 7. 표식 대상 69종목 중 **59종목(85.5%)** 측정.
- 손 라벨 정답지 **84/86(97.7%)**, 반도체 세부 표본 **19/19** 일치. 두 오분류는 기존에
  문서화한 포스코퓨처엠·천보 한계다.
- 새 섹터로 스크리너 **1,150행** 재저장: 투자 매력도 270/275, 중앙값 **61.6**,
  p90 **79.1**, 최고 **94.1**; PRI **800/1,150**, ★ 29·○ 83, 발송 등급 112.
- 로컬 production 화면 **1,596,076자**에서 OSAT 9종목과 9px `후`를 확인했다.
  장비 표본은 HPSP `전`, 한미반도체·와이씨 `후`로 확인했다.

## 검증·안전

- 표적 회귀 **68 passed**, 전체 오프라인 **834 passed · 1 skipped · 3 needs_network deselected**,
  production build **10/10 route 성공**.
- LLM 유료 호출·텔레그램 발송·KIS 주문 API 호출은 **0건**이다.

### 배포

- 커밋 `0757d05`를 `origin/main`에 푸시했다. GitHub Actions CI
  [34659682618](https://github.com/gotomirae/Heimdallr_Call/actions/runs/34659682618)는
  **37초 · 성공**이었다.
- Vercel 운영 목록은 **HTTP 200 · 1,674,436바이트**였고 새 3줄 매크로, `낮은 PRI`
  추천 순서, `w-[268px]` 고정 그룹이 모두 포함된 것을 확인했다.
- CI·Vercel 결과는 아래 배포 기록에 이어 적는다.

## 배포

- 기능 커밋 `83c9c66`과 선행 적응형 정렬 커밋 `185bf23`을 `origin/main`에 푸시했다.
- GitHub Actions CI [34657516510](https://github.com/gotomirae/Heimdallr_Call/actions/runs/34657516510)는
  **42초 · 성공**이었다.
- Vercel 운영 목록은 **HTTP 200 · 1,594,665자**였고 `반도체 DSP`, `반도체 OSAT`,
  9px 공정 배지 클래스가 모두 포함된 것을 확인했다.

## 발굴 목록 고정 열·매크로 정렬·방향 수정

### 완료한 것

- 가로 스크롤 고정 범위를 **섹터·관심·종목명 3열(268px)**로 줄였다. 등급과 분기는 일반 열로
  이동하며 그룹 머리글도 `종목 정보` 3열과 `판정` 2열로 분리했다.
- 한국은행 공식 RSS에서 경제전망·산업/기업·금리/물가 자료를 주제별 하나씩 선택하고,
  현재 국면·향후 핵심 변수·추천 정렬을 3줄로 표시한다. 원문 실패 시 추정하지 않는 문구로 후퇴한다.
- 현재 `반도체 성장 + 물가·금리/지정학 위험` 국면에는 최신 분기 → 등급 → 섹터 기회 →
  낮은 PRI → 높은 투자 매력도 → 낮은 F.PER → 높은 F.ROE → 높은 영업이익 YoY를 최초 정렬로 적용했다.
- 머리글을 누르면 새 열이 기존 체인 끝이 아니라 **즉시 1순위**가 되도록 수정했다.

### 운영 실측

- 발굴 목록 **1,151행**, 성장 가속 **209종목**, 발송 대상 **112종목**을 실제 브라우저로 렌더했다.
- 계산 스타일은 섹터 `sticky/0px`, 관심 `sticky/112px`, 종목명 `sticky/156px`; 등급·분기는
  `static/auto`로 확인했다.
- 18개 숫자 열을 각각 기본→내림→오름→기본으로 전수 클릭했다. 측정값이 있는 **17/17열**이
  두 방향 모두 단조 순서이고 결측은 맨 뒤였다. `후 60일`은 측정값 **0건**이라 방향 판정 제외.
  예: 투자 매력도 **94.1→38.1 / 38.1→94.1**, 매출 QoQ **+393.4→-57.5 /
  -57.5→+393.4**, 시총 **1,575.6조→960억 / 960억→1,575.6조**.

### 검증

- 전체 오프라인 **834 passed · 1 skipped · 3 needs_network deselected**, production build
  **10/10 route 성공**. 전체 테스트의 네트워크 3건은 샌드박스가 KIND 접속을 막아 실패했고,
  변경과 무관한 외부 collector 테스트다.
- LLM 유료 호출·텔레그램 발송·KIS 주문 API 호출은 **0건**이다.
