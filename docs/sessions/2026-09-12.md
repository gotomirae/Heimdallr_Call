# 2026-09-12 — 반도체 6분류·전후공정 표식

## 완료한 것

- 반도체 투자 섹터를 **IDM·DSP·OSAT·소재·부품·장비** 여섯 역할로 확장했다.
  DSP는 설계·IP·주문형 반도체, OSAT는 패키징·범핑·테스트 서비스 증거로 구분한다.
- 전·후공정은 섹터명과 분리해 소재·부품·장비의 주요제품 증거로 판정한다. OSAT는 `후`,
  IDM·DSP는 표식 없음, 근거 부족은 `None`으로 유지한다.
- 발굴 목록 섹터 셀에 `전`·`후`를 **9px** 작은 위첨자 배지로 표시했다.
- Python 분류·공정 함수와 TypeScript 화면 판정을 동일 생성 상수로 동기화하고 실제 표본
  대조기가 두 결과를 함께 비교하게 했다.

## 운영 실측

- `krx_universe` **1,376행** 저장: IDM **28**, DSP **12**, OSAT **9**, 소재 **12**,
  부품 **17**, 장비 **31** — 반도체 합계 **109종목**.
- 공정 표식: OSAT 후 **9/9**, 부품 전 6·후 10·미판정 1, 소재 전 10·미판정 2,
  장비 전 14·후 10·미판정 7. 표식 대상 69종목 중 **59종목(85.5%)** 측정.
- 손 라벨 정답지 **84/86(97.7%)**, 반도체 세부 표본 **19/19** 일치. 두 오분류는 기존에
  문서화한 포스코퓨처엠·천보 한계다.
- 새 섹터로 스크리너 **1,150행** 재저장: 투자 매력도 270/275, 중앙값 **61.6**,
  p90 **79.1**, 최고 **94.1**; PRI **800/1,150**, ★ 29·○ 83, 발송 등급 112.
- 로컬 production 화면 **1,596,076자**에서 OSAT 9종목과 9px `후`를 확인했다.
  장비 표본은 HPSP `전`, 한미반도체·와이씨 `후`로 확인했다.

## 검증·안전

- 표적 회귀 **68 passed**, 전체 오프라인 **834 passed · 1 skipped · 3 needs_network deselected**,
  production build **10/10 route 성공**.
- LLM 유료 호출·텔레그램 발송·KIS 주문 API 호출은 **0건**이다.

## 최신 거시·일봉·다음 분기 컨센서스 통합

### 완료한 것

- 한국은행 2026년 9월 통화신용정책보고서, 8월 경제전망, KDI 9월 경제동향을 공식 링크와 함께
  대시보드에 고정했다. 현재 국면은 기준금리 2.50%→3.00% 인상, 목표 상회 물가와 주택·가계대출
  부담, AI·반도체 수출/설비투자 강세, 완만한 소비 회복으로 요약했다.
- 추천 최초 정렬을 섹터 기회·낮은 PRI·높은 투자 매력도·낮은 내년 F.PER·높은 내년 F.ROE·
  영업이익 YoY 뒤에 등급과 최신 분기를 적용하도록 바꿨다. 사용자가 누른 열은 즉시 1순위가 되며
  기존 정렬 방향은 동률 해소 체인으로 보존한다.
- 네이버 연간 기업실적분석의 올해/내년 예상 열로 PER·F.PER·ROE·F.ROE 계약을 통일했다.
  모바일 연간 응답이 내년 열을 생략하면 같은 네이버 WiseReport 연간 표로 보완한다.
- 종목 상세에 네이버 일봉과 일간 MACD(12·26·9)·RSI(14), DART 분기실적 발표일 표식,
  네이버 다음 분기 컨센서스 `(E)`를 추가했다. 각 그래프 바로 아래에는 현재 위치가 나온 원인,
  52주 고점 대비 하락률, 추세 해석과 매수·축소 활용 기준을 표시한다.
- 결과 탭은 다음 분기·전년 동기·현재 분기를 결합해 섹터별 매출/영업이익 YoY·QoQ와 예상 가속
  종목 비율을 계산한다. 컨센서스 표본 5개 미만은 기존 추세 범위로 명시적 폴백한다.
- LLM 3단계는 지난 4분기 실적 발표·경영진 인터뷰·가이던스의 주장을 현재 숫자와 대조하는
  `narrative_verification`을 저장·표시한다. 선진짱 주식공부방·소중한추억.을 포함한 최근 자료는
  실제 검색이 반환한 직접 URL만 출처로 남긴다. 기존 저장 분석은 재결제하지 않고 다음 적격 3단계
  자동 실행부터 채운다.

### 운영 실측

- 삼성전자 네이버 연간 예상치: **2026 PER 5.38배·2027 F.PER 3.84배·2026 ROE 56.40%·
  2027 F.ROE 49.19%**. 네이버 일봉은 **729개**, 2023-09-12~2026-09-11을 수집했고 마지막
  종가는 **259,500원**이었다.
- 로컬 production 삼성전자 상세는 **HTTP 200·237,961바이트**, 일봉·다음 분기 `(E)`·연도별
  PER 문구를 확인했다. 결과 탭은 **HTTP 200·661,209바이트**였다.
- 다음 분기 가속 예상 상위 실측은 반도체 장비 **27%→79%(+52%p)**, 의료기기
  **30%→69%(+39%p)**, 디스플레이 **13%→50%(+37%p)**였다.

### 검증·안전

- 전체 라이브 테스트 **838 passed · 1 skipped**, production build **10/10 route 성공**.
- LLM 유료 호출·운영 DB 쓰기·텔레그램 발송·KIS 주문 API 호출은 **0건**이다.
- 연간 컨센서스 저장값은 기존 일일 수집 작업에서 새 연도 계약으로 갱신된다. 종목 상세는
  네이버 실시간 보조 경로를 사용하므로 배포 즉시 새 계약으로 표시된다.

### 배포

- 커밋 `0757d05`를 `origin/main`에 푸시했다. GitHub Actions CI
  [34659682618](https://github.com/gotomirae/Heimdallr_Call/actions/runs/34659682618)는
  **37초 · 성공**이었다.
- Vercel 운영 목록은 **HTTP 200 · 1,674,436바이트**였고 새 3줄 매크로, `낮은 PRI`
  추천 순서, `w-[268px]` 고정 그룹이 모두 포함된 것을 확인했다.
- CI·Vercel 결과는 아래 배포 기록에 이어 적는다.

## 배포

- 기능 커밋 `83c9c66`과 선행 적응형 정렬 커밋 `185bf23`을 `origin/main`에 푸시했다.
- GitHub Actions CI [34657516510](https://github.com/gotomirae/Heimdallr_Call/actions/runs/34657516510)는
  **42초 · 성공**이었다.
- Vercel 운영 목록은 **HTTP 200 · 1,594,665자**였고 `반도체 DSP`, `반도체 OSAT`,
  9px 공정 배지 클래스가 모두 포함된 것을 확인했다.

## 발굴 목록 고정 열·매크로 정렬·방향 수정

### 완료한 것

- 가로 스크롤 고정 범위를 **섹터·관심·종목명 3열(268px)**로 줄였다. 등급과 분기는 일반 열로
  이동하며 그룹 머리글도 `종목 정보` 3열과 `판정` 2열로 분리했다.
- 한국은행 공식 RSS에서 경제전망·산업/기업·금리/물가 자료를 주제별 하나씩 선택하고,
  현재 국면·향후 핵심 변수·추천 정렬을 3줄로 표시한다. 원문 실패 시 추정하지 않는 문구로 후퇴한다.
- 현재 `반도체 성장 + 물가·금리/지정학 위험` 국면에는 최신 분기 → 등급 → 섹터 기회 →
  낮은 PRI → 높은 투자 매력도 → 낮은 F.PER → 높은 F.ROE → 높은 영업이익 YoY를 최초 정렬로 적용했다.
- 머리글을 누르면 새 열이 기존 체인 끝이 아니라 **즉시 1순위**가 되도록 수정했다.

### 운영 실측

- 발굴 목록 **1,151행**, 성장 가속 **209종목**, 발송 대상 **112종목**을 실제 브라우저로 렌더했다.
- 계산 스타일은 섹터 `sticky/0px`, 관심 `sticky/112px`, 종목명 `sticky/156px`; 등급·분기는
  `static/auto`로 확인했다.
- 18개 숫자 열을 각각 기본→내림→오름→기본으로 전수 클릭했다. 측정값이 있는 **17/17열**이
  두 방향 모두 단조 순서이고 결측은 맨 뒤였다. `후 60일`은 측정값 **0건**이라 방향 판정 제외.
  예: 투자 매력도 **94.1→38.1 / 38.1→94.1**, 매출 QoQ **+393.4→-57.5 /
  -57.5→+393.4**, 시총 **1,575.6조→960억 / 960억→1,575.6조**.

### 검증

- 전체 오프라인 **834 passed · 1 skipped · 3 needs_network deselected**, production build
  **10/10 route 성공**. 전체 테스트의 네트워크 3건은 샌드박스가 KIND 접속을 막아 실패했고,
  변경과 무관한 외부 collector 테스트다.
- LLM 유료 호출·텔레그램 발송·KIS 주문 API 호출은 **0건**이다.

## 성장 지속·MACD 상향 접근 텔레그램 알림

### 완료한 것

- 사용자 조건을 `src/screener/technical_setup.py` 순수 판정 함수와 `src/notify/technical_alert.py` 일일 배치로 반영했다. 기업 매출·영업이익 YoY가 각각 2개 분기 연속 엄격히 개선되고, 산업 중앙값도 2개 분기 연속 양수(유효 기업 5개 이상)인 종목만 네이버 일봉 조회 대상으로 삼는다.
- 가격은 50일 고점 대비 **-20% 이하** 조정 뒤 20일 수익률 **음수** 하락 또는 10일 수익률 **±3%·고저폭 8% 이하** 횡보를 허용한다. MACD(12·26·9) 히스토그램은 음수·종가 대비 절대 **0.30% 이내**·3거래일 연속 상승, RSI(14)는 **50 이하**에서 3거래일 연속 상승이어야 한다.
- `.github/workflows/daily_digest.yml` 장 마감 배치(기존 요약 직전)에 연결했다. 하루 최대 2건, 종목·평가분기·신호종류별 중복 차단, 실패 시 기존 요약 발송은 계속한다.

### 검증·안전

- 기술 알림·워크플로·상수 회귀 **110 passed · 1 skipped**, 전체 오프라인 **844 passed · 1 skipped · 3 deselected**, production build **10/10 route 성공**.
- 새 기준 운영 dry-run에서 펀더멘털 후보 **179종목**, 네이버 일봉 **179/179 성공**, 최종 기술 신호 **0건**, 실패 **0건**을 실측했다. 조건을 만족하지 않으면 억지로 2건을 채우지 않는다. 이번 작업에서 실제 발송·DB 쓰기·LLM 유료호출·KIS 주문 API 호출은 **0건**이다.

## 대시보드 연결·미국 매크로·LLM 3단계 근거 보강

### 완료한 것

- 발굴 목록/종목 상세 운영 기준 응답이 각각 **6.22초/5.57초**까지 열린 채 남는 것을 재현했다.
  페이지 렌더 중 공식 RSS를 기다리는 경로를 없애고, 400개 종목 링크의 상세 RSC 프리패치를 끄며,
  클릭·입력·스크롤 뒤 20초 동안 60초 전체 갱신을 보류했다. 목록은 화면에 필요한 최소 열만 읽고,
  연결 실패에는 필터 상태를 보존하는 재시도 화면을 추가했다.
- 발굴 목록 매크로를 미 연준·BLS·BEA·IMF 공식 자료 중심으로 바꾸고, 미국 AI·데이터센터 투자와
  연결된 반도체·전력 공급망 중 실제 ★/○가 있는 섹터를 최초 정렬에서 우선한다. 그 안에서는 높은
  투자 매력도→낮은 PRI→낮은 F.PER→높은 F.ROE→높은 영업이익 YoY를 사전식으로 유지한다.
- 다중 정렬은 먼저 누른 규칙의 우선순위·방향을 고정하고 새 열을 뒤에 추가한다. 같은 열은
  내림↔오름만 전환하며 개별 × 또는 기본 복원으로만 제거한다.
- LLM 3단계 입력에 직전 4개 분기 DART 정기보고서 최신 정정본의 제한 발췌와 접수번호 직접 링크를
  추가했다. 4개 절에 예산을 균등 배분해 마지막 주요계약·연구개발 절이 잘리지 않게 했고, 지정
  텔레그램 2곳·일반 웹검색의 최신 리포트와 함께 당시 주장을 이번 분기 매출·영업이익·OPM에 대조한다.

### 실측·검증

- 실데이터 로컬 production 예열 후 목록 **1.82초**, 삼성전자 상세 **1.29초**, 모두 HTTP 200이며
  서버 연결 오류 0건. 첫 목록 콜드 요청은 **4.72초**였다.
- 관련 회귀 **72 passed**, 전체 오프라인 **848 passed·1 skipped·3 deselected**, KIND 네트워크
  **3 passed**, Dashboard production build **10/10 route 성공**. 실제 LLM 유료호출·텔레그램 발송·
  DB 쓰기·KIS 주문 API 호출은 **0건**이다. ADR 18·T159.

### Digest 889877318 후속 안정화

- 운영 `/`은 확인 시점 6/6 HTTP 200으로 회복돼 Digest의 원문 서버 로그는 재현되지 않았지만,
  응답 완료는 16.9~22.6초였다. 로컬 실데이터 production에서 목록 본문 **1,667,199바이트**와 동시
  렌더 working set 증가를 확인했다.
- PostgREST 연결·pool·timeout·429/5xx만 150ms→450ms 두 번 지터 재시도한다. 부가 시세·컨센서스·
  발표 후 수익률·분기 실적은 최종 실패를 목록 전체 500으로 전파하지 않고 미수집 경고로 격리한다.
  인증·영구 4xx·스키마 오류는 숨기지 않는다.
- 목록은 전수 필터·정렬을 유지하면서 120행씩 점진 렌더한다. 수정 production 빌드에서 단일 요청
  **200·1,433,107바이트·2.83초**, 동시 **6/6 200·1.82~2.71초**였다. 외부 DB 쓰기·LLM 호출·
  텔레그램 발송·KIS 주문 API 호출은 0건이다. 전체 오프라인 **850 passed·1 skipped·3 deselected**,
  Dashboard production build **10/10 route 성공**. 배포 확인은 이 절에 이어 기록한다.
