# 2026-09-09 — 가치·가격 운영 반영 재점검

## 확인한 것

- 운영 대시보드에는 아직 `최근 4Q PER`·`F.PER` 열이 없어 이전 구현이 배포되지 않은 상태임을 실화면에서 확인했다.
- 운영 Supabase 컬럼 점검 결과 ROE 관련 6개가 실제로 없었다.
  - `price_snapshots.roe_est`, `roe_next_est`, `roe_next_year`
  - `consensus_snapshots.roe_est`, `roe_next_est`, `roe_next_year`
- 수정 코드를 운영 데이터에 연결한 로컬 화면에서 다음을 직접 확인했다.
  - 발굴목록: `최근 4Q PER`·`F.PER`·`ROE` 열과 고영 최근 4Q PER **38.9배**
  - 고영 상세: 최근 4Q PER **38.9배**, F.PER **33.1배**, 올해→내년 ROE, 8축 섹터표
  - 섹터표: 시총·최신 분기 매출·영업이익·OPM·올해/내년 ROE·최근 4Q PER·F.PER
- ROE는 스키마가 없어서 `—`가 정상 방어 표시였다. DDL 적용 뒤 컨센서스 수집을 실행해야 실제 값이 채워진다.

## 보강

- `check_migration`이 ROE 열을 `price_run`으로 채우라고 잘못 안내하던 문제를 수정했다. 실제 수집기는 `consensus_run`이다.
- `docs/MIGRATION.md`에 ROE 6개 컬럼과 정확한 데이터 채움 명령을 추가했다.
- 네이버 실호출 1종목(에스엘): 2026 F.PER **6.47**, ROE **13.68% → 13.40%**.

## 검증 수치

- 관련 테스트 **65 passed**
- 대시보드 production build **10/10 route 성공**
- 로컬 운영데이터 렌더: 발굴목록 **HTTP 200**, 고영 상세 **HTTP 200**

## 남은 운영 작업

- Supabase 로그인 없이는 DDL을 실행할 수 없어 운영 DB 변경은 중단했다. 프로젝트 ref는 반드시 `drpxciqkbjlruximqbox`여야 한다.
- DDL 적용 후 `python -m src.collectors.consensus_run --save` 1회 실행이 필요하다. LLM 유료 호출은 없다.

---

## 종목 상세 현재가·차트·분석 단계 보강

- 상세 요청 시 네이버 모바일 공개 JSON 2개를 최대 1분 캐시로 읽는다. 현재가·최근 4Q PER·
  F.PER·연간 (E) ROE를 같은 네이버 값으로 표시하고 거래 기준일을 붙인다. 실패 시에만 DB
  마지막 스냅샷으로 물러선다.
- 고영 실측(2026-09-09): **27,350원 · PER 39.35배 · F.PER 31.55배 · 2026E ROE 16.52%**.
  운영 DB 연결 로컬 production 렌더 **HTTP 200 · 212,982B**에서 동일 값 확인.
- 분기 실적 추이와 히스토리를 정확히 **10분기**로 맞췄다. 매출·영업이익 YoY를 실제 %의
  한 좌표에 표시하고, 10분기 원값 표로 기저효과에 눌린 작은 선과 흑전·적전 결측을 함께 밝힌다.
- 네이버 일봉을 ISO 주 마지막 거래일로 접은 주봉에 표준 MACD(12·26·9)를 적용했다. 직선으로
  실제 관측점을 연결하고 최신 MACD·Signal·Histogram을 숫자로 표시한다.
- 매출·영업이익·OPM·두 YoY·수주·주간 종가·MACD·RSI의 최신 위치·의미·다음 확인사항을
  LLM 없이 결정론적으로 설명한다.
- LLM 분석은 1·2·3단계 시점/상태 카드로 분리하고, upsert 때 이전 `stage_history` 완료시각을
  보존한다. 시세 변화는 재분석하지 않고 동일 단계·동일 근거 실패는 재결제하지 않는다.

## 검증

- 변경 관련 **131 passed · 1 skipped**
- 전체 오프라인 **809 passed · 1 skipped · 3 needs_network deselected**
- Dashboard production build **10/10 route 성공**

---

## 발굴목록 F.PER·F.ROE 기준 수정

- `consensus.py`의 네이버 연간 추정 파싱에서 `fwd_per`를 가장 가까운 추정연도가
  아니라 **다음 추정연도(내년도) PER**로 변경했다. 다음 연도가 없으면 올해 값으로
  대체하지 않고 `None`을 유지한다.
- 발굴목록의 ROE 원천을 `roe_next_est`로 바꾸고 머리글을 **F.ROE**로 변경했다.
  상세 화면의 올해·내년 ROE 비교용 `roe_est`/`roe_next_est`는 그대로 보존한다.
- 관련 회귀 테스트 **27 passed**, 오프라인 전체 **812 passed · 1 skipped · 3 deselected**,
  대시보드 production build **10/10 route 성공**. 네트워크·LLM·DB 쓰기는 실행하지 않았다.

## 후속 기준 정정

- 사용자 기준을 다시 반영해 발굴목록의 `F.PER`는 **올해** 네이버 예상 PER로
  되돌리고, `ROE`는 올해, `F.ROE`는 내년 값으로 분리했다.
- 발굴목록에 매출 QoQ·영업이익 QoQ와 전체 정렬 키를 추가했고 표기는 `영업이익 YoY`로 통일했다.

## 발굴목록 QoQ·올해/내년 가치지표 분리

- 매출 QoQ와 영업이익 QoQ 열·정렬을 추가하고, `영업익 YoY` 표기를 `영업이익 YoY`로 통일했다.
- `F.PER`는 네이버 올해 예상 PER, `ROE`는 올해 예상 ROE, `F.ROE`는 내년도 예상 ROE로
  분리해 화면과 수집기 의미를 일치시켰다.
- 관련 테스트 **39 passed**, 오프라인 전체 **811 passed · 1 skipped · 3 deselected**,
  대시보드 production build **10/10 route 성공**. 네트워크·LLM·DB 쓰기는 실행하지 않았다.
- 유료 LLM 호출·DB 쓰기 **0건**

---

## PRI 2.0 간소화 반영

- 기존 52주 하락률·RSI·외국인 5일 수급 중심 PRI를 유지하지 않고, 실적 발표 후 초과수익(30),
  이익 전망·주가 괴리(30), TTM PER·F.PER 반영도(20), 3·6·12개월 상대주가(20)의 4축으로
  교체했다. 데이터 신뢰도는 점수에 섞지 않고 측정 가능 배점으로 별도 표시한다.
- P6 수집기는 기존 저장 컨센서스 빈티지에서 최근 90일 이익 전망 변화를 계산하므로 추가 유료
  호출이 없다. 상세 PRI에는 4개 항목별 점수·원자료·설명을, 신뢰도 80 미만이면 참고용이라는
  안내를 표시한다. 구 저장 행은 legacy 계산으로 읽어 회귀를 보호한다.
- 스키마/마이그레이션에 PRI 2.0 입력 5개를 추가했고 설정 화면·Telegram 라벨도 새 기준으로
  맞췄다.

## 검증

- 오프라인 전체 **811 passed · 1 skipped · 3 needs_network deselected**
- Dashboard production build **10/10 route 성공**
- 전체 pytest에서 남은 3개 실패는 KIND 네트워크 차단, 기존 Telegram 회귀 1건은 호환 수정 전
  확인된 것이며 이후 오프라인 재실행에서 해소됐다.

---

## 발굴목록 ETF·주요제품 섹터 표시 보강

- 기존 제품 우선 섹터 분류를 유지하면서, 제품으로 판정되지 않을 때 ETF와 비교 가능한
  넓은 투자 테마를 함께 제공하도록 `SECTOR_ETF_THEMES`를 추가했다. ETF 상품명·코드는
  운용사별 변경 가능성이 있어 저장하지 않고 테마명만 생성물로 내보낸다.
- 발굴목록 섹터 셀에 `ETF 테마`와 `주요제품`/`ETF 유사 테마`/`미분류` 근거를 표시한다.
  필터링에 쓰는 기존 섹터 키는 바꾸지 않아 링크·저장 필터 호환성을 유지했다.

## 검증

- 섹터·생성물 테스트 **47 passed**
- 오프라인 전체 **811 passed · 1 skipped · 3 needs_network deselected**
- Dashboard production build **10/10 route 성공**

---

## 컨센서스 저장 실행 시도

- 사용자의 요청으로 `python -m src.collectors.consensus_run --save`를 실행했다.
- Supabase 최초 조회 단계에서 Windows 네트워크 권한 오류 `WinError 10013`으로 중단되어
  수집·DB 쓰기 **0건**이다. 외부 네트워크 권한 재시도는 사용량 제한으로 거부됐다.

## 화면 클릭 지연 완화

- 발굴목록의 분기별 재무 조회를 순차 `await`에서 `Promise.all` 병렬 조회로 바꿔 분기 수만큼
  누적되던 왕복 대기시간을 제거했다.
- 화면 전환 즉시 `loading.tsx`가 표시되도록 추가했고, 종목 상세의 네이버 요청에는
  5초 timeout을 넣어 외부 응답 지연이 페이지 전체를 오래 붙잡지 않게 했다.
- 오프라인 전체 **811 passed · 1 skipped · 3 deselected**, Dashboard production build
  **10/10 route 성공**.

## 투자지표 원천·PBR 표시 정리

- 종목 상세 투자지표는 네이버 값을 우선하도록 정리했다: 최근 4Q PER, 올해 F.PER,
  올해 ROE, 내년 F.ROE, 네이버 공개 PEG.
- PBR 표시를 제거하고 LLM 주가 위치 카드의 해당 칸에 `올해 ROE → 내년 F.ROE`를 배치했다.
  네이버가 PEG를 공개하지 않는 종목은 자체 성장률 계산으로 대체하지 않고 결측으로 표시한다.
- 관련 대시보드 테스트 **13 passed**, production build **10/10 route 성공**.
