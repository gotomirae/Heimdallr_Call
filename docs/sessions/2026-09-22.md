# 2026-09-22 — 종목 상세 PRI·수주·LLM 요청·Telegram 대시보드 진입

## 구현한 일

- 주가반영도(PRI)를 `실적 발표 뒤 가격 반응`, `시장 기대와 실적의 차이`, `이익 성장 대비 멀티플`, `밸류에이션 부담`, `단기 과열·선반영`의 5개 핵심 항목으로 재구성했다. 기존 원시 신호는 유지하고 각 핵심 항목 안에서 측정된 신호만 정규화한다.
- 종목 상세에 5개 항목별 점수·입력 근거·해석을 표시하고, 전체 측정 분모가 60점 미만이면 등급을 만들지 않는다. 기존 v2/v3 저장 행도 계속 렌더한다.
- 모든 종목의 최신 정기보고서를 대상으로 수주 발췌를 증분 수집하는 예약 작업을 추가했다. 수주잔고·신규수주 숫자, 영업비밀, 해당 없음, 언급만 있음, 발췌 잘림, 미언급, 수집 전을 구분해 항상 표를 표시하고 DART 원문을 연결한다.
- 주가 그래프 아래 해설을 주요 시기별 상승·하락·횡보 원인과 현재 반영 수준·향후 전망으로 나눴다. 프롬프트는 일반적인 기술 설명을 줄이고 공시·검색 근거가 있는 실제 원인을 기간 형식으로 작성하게 했다.
- 모든 종목 상세에 `LLM 분석 실행` 버튼과 private 요청 큐를 추가했다. 5분 예약 작업이 기업 개요·실적 변화 원인·핵심 투자 아이디어·위험·향후 확인 항목을 분석하며, 사용량 부족 시 `deferred`로 남겨 갱신 뒤 자동 재개한다.
- Telegram 전용 봇의 채팅 메뉴 버튼을 `📊 Heimdallr 대시보드`로 고정하도록 추가했다. 수신기 시작 때 대시보드 URL을 설정하며 실패해도 기업명 수신은 계속한다.

## 실측·검증

- 운영 Supabase 읽기 전수 재생: 유니버스 **1,150종목**, 게이트 통과 **275종목**, PRI 산출 **800종목**, 중앙값 **25.6**.
- 5개 항목 측정 수: 가격 반응 **1,066**, 과열 **1,149**, 기대차 **400**, 밸류 부담 **830**, 이익 대비 멀티플 **333**.
- 측정 분모 0/20/40/60/80/100점: **1/27/322/266/361/173종목**. 새 등급은 ★ **30**, ○ **83**, · **106**, ✕ **2**, 미부여 **929**.
- 전 종목 발췌 대기 **1,070건**을 확인했다. 예약 작업은 회당 최대 500건·30분 범위에서 이어 처리한다.
- 전체 오프라인 Python 회귀 **909 passed·1 skipped·3 network deselected**, Next.js production build **10/10**을 확인했다. 격리 환경에서 KIND 실호출 3건은 Windows 네트워크 차단으로 별도 제외했다.
- 운영 `screen_results` **1,150행**을 갱신했다. 페이징 재조회에서 v4 상세 **1,149행**(원자료 0건 1종목 제외), PRI 수치 **800행**을 확인했다.
- 전 종목 발췌 첫 배치 **3/3 성공·실패 0**이었고, 운영 재조회에서도 새 3건 모두 `공시 수주지표 확인` 표식을 확인했다.
- Telegram 전용 봇 **8933940541**에 `📊 Heimdallr 대시보드` 메뉴 설정 성공을 확인했다. 대기 메시지는 0건이었다.

## 남은 운영 적용

- Supabase `dashboard_analysis_requests` 테이블·인덱스·RLS를 운영 프로젝트 `drpxciqkbjlruximqbox`에 적용했다. `src.db.init`에서 service 접근 **16/16**, anon 읽기 **13/13**, private 큐 0행 노출을 확인했다.
- 커밋 `c77ab94`를 운영 `main`에 푸시했고 GitHub CI run `35666872438`이 성공했다. 클릭형 LLM worker run `35666982835`도 빈 큐에서 **완료 0·실패 0·대기 0**으로 성공했다.
- Vercel 운영 `/stock/000660`은 **HTTP 200·216,868자**였다. 실제 브라우저에서 PRI 5개 요인, 주요 시기별 주가 원인 6개, LLM 분석 실행 버튼, 수주잔고·신규수주 표와 DART 링크를 확인했다.
- 운영 분석 API는 GET **200 `idle`**, 부적합 분기 POST **400 `invalid`**로 큐 조회·서버 검증을 확인했다. 유료 LLM 호출이 생기는 정상 POST는 사용자가 버튼을 누를 때 수행하도록 남겼다.
- 전 종목 수주 수집 workflow run `35666985912`를 시작했다. 시작 시 운영 발췌 누적은 **478건**이고, 남은 최신 정기보고서는 예약 작업이 시간 예산 안에서 계속 채운다.

## 대시보드 투자판단·전체 화면 링크 개편

- Telegram `web_app` 메뉴를 기본 메뉴로 해제하고 일반 HTTPS inline 버튼을 고정 메시지로 전환했다. 운영 봇 **8933940541**, 허용 채팅 **1923569789**에서 `setChatMenuButton(default) → sendMessage → pinChatMessage`가 성공했고, 수신 1건은 텍스트 없음으로 정상 무시됐다.
- 미국·글로벌 매크로는 기존 **06:45 예열·07:00 본 실행·07:15/07:30/08:00 재시도**를 유지했다. 2026-09-22 12:27 KST 실제 재수집에서 미국 거래일 **2026-09-21**, S&P 500 **+1.49%**, 나스닥 **+2.26%**, 필라델피아 반도체 **+4.29%**, VIX **14.87**을 기록했다. 종가 출처에 TradingView를 추가하고 영문 링크는 한국어 번역 URL로 연다.
- 발굴 목록 필터를 표 바로 위로 옮기고 조건·등급·섹터·시총·`vs 컨센`·분기를 제공했다. `vs 컨센`은 평가 분기 영업이익 실제치와 같은 분기 네이버 컨센서를 비교한다. 추천 배너 옆 기본 추천 순서 복원 버튼, 선택 조건에 따라 바뀌는 매크로 섹터 기준, `분기실적 발표 전후 주가 변화` 명칭과 발표 당일 종가 기준을 반영했다.
- 종목 상세에는 최근 정기보고서의 명시된 제품/사업 매출 비중, 녹색 매출+흰색 GPM, 황금색 영업이익+흰색 OPM, 공시 수주 그래프, 과거 모든 잠정/확정 발표선, MACD/Signal·RSI 45 현재 설명, 네이버 PER/F.PER/ROE/FCF/PEG, 분석 기준일·현재 기준 LLM 재분석, 제품 설명 유사도 우선 피어와 고정 종목명 열을 반영했다. 제품 비중은 공시에 적힌 `%`만 읽고 금액으로 추정하지 않는다.
- 주가반영도 화면과 계산을 발표 당일~현재 반응, 과거 3개년 평균 PER 대비 F.PER, 섹터 PEG, 이익 전망 반영, 가격 모멘텀·과열의 5항목으로 재배치했다. 결측은 0점으로 바꾸지 않고 60점 이상 측정 분모에서만 판정한다.
- 운영 `krx_universe` **1,376행**을 제품 우선 규칙으로 재분류했다. 결과는 반도체 IDM **28**, DSP **12**, OSAT **9**, 소재 **12**, 부품 **17**, 장비 **31**, 전자부품 **55**, 미분류 **26(1.9%)**였다.
- 최종 검증: Next build **10/10**, Python 오프라인 **911 passed·1 skipped·3 network deselected**. 네트워크 포함 테스트는 샌드박스에서 KIND 3건이 차단됐으나 같은 오프라인 묶음은 전부 통과했다.
- 운영 `main` 커밋 **0831635**와 CI run **35685194819** 성공을 확인했다. Vercel 홈은 **HTTP 200·833,532자**, 종목 상세(`/stock/000660`)는 **HTTP 200·216,873자**였고, 홈의 `분기실적 발표 전후 주가 변화`·`vs 컨센`·TradingView·추천 순서 복원과 상세의 제품 비중·현재 기준 LLM 재분석·주가반영도·GPM 문자열을 모두 재조회했다.

## 매크로 내부 번역·LLM 단계 진행률·정기보고서 제품 비중 명확화

- 매크로 `한글 번역` 링크를 Google 번역 프록시에서 `/macro/translation` 내부 페이지로 바꿨다. 저장된 공식 발표 한국어 핵심 번역, 발표일·스냅샷 확인 시각, 공식 원문 버튼을 함께 표시한다. 로컬 프로덕션 렌더는 **HTTP 200·12,198바이트**, CPI 번역 제목·공식 원문 링크를 확인했다.
- 종목별 LLM 요청에 단계 기준 **25% 접수·대기 / 65% 입력·원문·분석·검증 / 100% 저장 완료** 진행 바와 접수·처리 시작 KST 시각을 추가했다. 페이지 재진입 시 활성 큐를 다시 폴링하고 완료·실패 후 타이머를 정리한다. 모델 내부 토큰 진행률은 아님을 명시했다.
- 제품·사업 매출 비중은 1분기/반기/3분기/사업보고서와 누적 기간, DART 원문, 발췌/수집 상태를 표시한다. 독립 `%` 셀 외에 머리글 `비중(%)`, `금액(98.3%)`, 금액행 다음 비율행을 처리하고 금액 역산은 하지 않는다.
- 운영 발췌 읽기 전용 재생: **478행**, 제품 절 **477행**, 명시 비중 **323개 보고서·790개 항목**, 발췌 잘림 **456행**. 로컬 종목 상세 `123410`은 **HTTP 200·235,568바이트**로 제품 비중 표·보고서 기간·DART 링크·LLM 요청 패널을 확인했다.
- 검증: Python 오프라인 **911 passed·1 skipped·3 deselected**, Next.js production build **11/11 route 성공**. 유료 LLM 호출·DB 쓰기·Telegram 발송은 0건이다. T180~181.
