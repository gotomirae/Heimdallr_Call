# 2026-09-18 — 07시 미국 매크로·기술 알림 조건 재정의

## 한 일

- 미국·글로벌 매크로 예약을 06:45 KST 예열, 07:00 기준 갱신, 07:15·07:30·08:00 재시도로 변경했다. 예열이 먼저 성공해도 07시 이후 기준 시각을 다시 기록한다. 미국 지수 4개는 동일한 마지막 완료 거래일만 사용한다. 목록의 갱신 경고는 한국 날짜뿐 아니라 뉴욕에서 완료된 평일 거래일과 `marketDate`를 비교한다. 미국 휴장 가능성은 경고로만 표시하고 없는 종가를 만들지 않는다.
- 발굴 기본 필터를 성장 기회(초기 전환 우선)로 바꾸고, 같은 매크로 적합 섹터에서는 흑전·게이트 통과·★/○·낮은 PRI를 먼저 배치했다. 수주 증가 근거는 현재 구조화 데이터에 없으므로 공시 확인 사항으로 명시했다.
- 텔레그램 2건/일 상한은 유지했다. 5일선의 20일선 상향 교차 직전/당일과 MACD의 Signal 상향 교차 직전/당일을 필수로, RSI 45 미만 상승은 강력 신호 보강으로 변경했다. 3거래일 연속 상승은 요구하지 않는다. 발표 후 주가가 덜 반영된 회복도 가격 게이트에 포함했다.
- 기존 부호 전환 구간 영업이익 YoY를 퍼센트로 만들지 않고 초기 흑전을 별도 단계로 처리했다. 매수 확정·실제 수주 증가로 단정하지 않는 메시지로 정리했다.

## 실측

- 2026-09-18 07:40 KST 로컬 매크로 수집: 미국 9/17 완료 장, S&P 500 +1.14%, 나스닥 +1.69%, 필라델피아 반도체 +3.14%, VIX 15.44. 기존 운영 화면은 미국 9/16 장·9/17 09:52 KST 갱신값이었다.
- 읽기 전용 기술 알림 재생: 펀더멘털 135, 발표일 보유 128, 일봉 128/128, 가격 68, 5·20일선 8, MACD 4, RSI 보강 0, 하루 상한으로 출력 2, 실제 발송·DB 쓰기 0.
- 오프라인 테스트 870 passed·1 skipped·3 network deselected. 전체 테스트 3건 실패는 KIND 외부 접속이 샌드박스에서 차단되어 발생했다. 대시보드 `next build` 10/10 성공.

## 주의·다음 확인

- 첫 운영 `main` 직접 push는 자동 검토가 워크플로 포함 20개 파일의 운영 변경 위험을 이유로 거부했다. 우회하지 않고 사용자 승인을 받은 뒤 최신 운영 스냅샷(9/18 09:46 KST)을 보존해 배포했다. 배포 커밋 `62cac53`, GitHub CI 성공, Vercel 성공, 실제 대시보드 HTTP 200·미국 9/17 장·초기 흑전 우선 정렬 문구 확인.
- GitHub Actions cron은 07:00 정확 실행을 보장하지 않는다. 예약 실행의 실제 시작과 Vercel 배포 후 화면 `checkedAt`/`marketDate`를 확인해야 한다.
- 구조화된 실제 수주 잔고·신규 수주 데이터가 충분하지 않아 초기 전환 우선순위는 흑전·매출 가속·낮은 PRI의 프록시다. 수주→이익 전환을 검증했다고 주장하지 않는다.
- 실제 텔레그램 발송은 dry-run에서 하지 않았다. 배포가 17:30 KST 정기 알림 이후라 새 조건의 첫 정기 스캔은 다음 실행이며, 발송/0건/오류 기록을 확인해야 한다.

## Kairos 스킬을 Heimdallr Telegram에 연결 (별도 작업)

### 한 일

- `C:\Codex\Kairos\kairos` 원본과 설치된 `$kairos` 스킬의 형식·참조·브리지 테스트를 검증했다. 원본·설치본 `SKILL.md`는 `working` 코드 서식 한 줄만 달랐고, `source-policy.md`는 줄바꿈 바이트 차이였다. 분석 조건은 실질적으로 일치한다.
- 기존 Heimdallr GitHub Actions 수신기에서 인증된 개인 채팅의 종목명·코드 단독 입력만 `kairos_requests`에 기록하도록 했다. 짧은 리포트 회신은 유지한다. 로컬 브리지는 Supabase 수신함만 조회하고 Telegram `getUpdates`를 호출하지 않는다. update ID 중복차단, Codex 한 번 깨우기, `working/sending/uncertain/sent` 상태와 검증된 Notion 링크 전달을 구현했다.
- `docs/migrations/kairos_requests.sql`, 로컬 예약 수집기 설치 스크립트, 작업 지침, PRD §8.7, ADR 19를 작성했다. 현재 Codex 작업 ID `01a0b3d1-390f-7301-bd23-be3bdcda4329`를 로컬 큐 트리거로 등록했다.

### 실측

- 원본·설치본 `quick_validate.py` 각각 `Skill is valid!`; 기존 Kairos 브리지 **16/16** 테스트 통과. Drive 국내·해외·산업 폴더 3개와 Notion 양식·저장 부모 페이지 읽기 성공.
- 이 저장소 새 테스트 포함 집중 **35 passed**, 네트워크 collector 3종 제외 오프라인 **808 passed·1 skipped**. 로컬 봇 ID **8933940541** 확인, 다른 작업 디렉터리에서도 브리지 `status` 실행 성공. 로컬 큐는 작업 0건이며 트리거는 등록됨.
- 운영 Supabase에는 새 테이블이 아직 없어 `PGRST205` 확인. 기존 Kairos 예약 작업은 `C:\Users\user\OneDrive\문서\ChatGPT\Kairos`에서 실행되고 **09:41 UTC 성공·종료코드 0**이었다. 그 활성 큐에는 `pending` **2건**(한 건 `wake_sent=1`)이 남았고 대상 Codex 작업의 마지막 완료 턴은 9/13·상태 `notLoaded`였다. `C:\Codex\Kairos` 복사본의 09:20 마지막 성공을 실제 수집기 상태로 오인하면 안 된다(T167).

### 운영 적용

- 사용자의 Supabase 로그인 후 운영 프로젝트 `drpxciqkbjlruximqbox`에 `docs/migrations/kairos_requests.sql`을 실행했다. SQL Editor 결과는 `Success. No rows returned`. `python -m src.db.init`에서 15개 service 테이블 확인, `kairos_requests` **0행**, anon 조회 권한 거부를 확인했다.
- 로컬 `HeimdallrKairosCollector`를 1분 반복으로 설치·시작했다. 예약 작업의 실제 실행 경로는 이 저장소의 `.venv\\Scripts\\pythonw.exe`와 `telegram_bridge\\bridge.py ingest`, 첫 실행 결과는 **0**이다. 별도 수집 검증에서 `mirrored=0`, `wake=idle`이었다. 봇 ID **8933940541**, Codex 트리거 설정됨, 로컬 요청 **0건**.
- 격리 브랜치 `codex/kairos-telegram-notion`의 [PR #1](https://github.com/gotomirae/Heimdallr_Call/pull/1)은 GitHub CI·Vercel 미리보기 모두 성공한 뒤 `main`에 squash 병합했다(`d05459b`). 운영 커밋의 `listen`·`test` 검사 성공. 운영 `telegram_listen`을 `analyze=false`로 수동 실행한 결과 **25초·성공·전용 봇 8933940541·처리 0건**이었다. 로컬 `main`을 동기화한 뒤 예약 수집기 반복 실행 결과도 **0**, 최근 수집 **10:04 UTC**, 오류 없음·작업 0건이었다.
- 실제 Telegram 기업명 1건의 `pending→working→sent` 및 Notion 저장·링크 전달 확인은 다음 사용자 요청 때 수행한다. 설정 검증을 위한 시험 Notion 페이지는 만들지 않았다.

## Kairos 사용량 복구 후 자동 재개

- 기존 `working`은 로컬 수집기의 `busy`로 남아 Codex를 다시 깨우지 않는 공백을 확인했다(T168). `claim` 시 ID별 `telegram_bridge/state/checkpoints/ID.md`를 생성하고 기존 장부는 덮어쓰지 않도록 했다. `poll`은 장부 경로를 보이며, `checkpoint ID`로 기존 `working`의 빠진 장부를 복구할 수 있다. 원고·출처·Notion 페이지 ID와 검증 단계를 장부에 갱신하고 동일 ID에서 이어가는 작업 지침을 추가했다.
- 현재 Codex 작업에 30분 간격 heartbeat `kairos`를 연결했다. `claim` 뒤 활성화하고 `working`이 사라지면 자동 일시 중지한다. 실행 중에는 계정의 5시간·주간 사용량이 모두 회복됐는지 확인해 같은 요청을 계속한다. `sending/uncertain`은 자동 재전송하지 않는다. 계정 사용량 실측은 5시간 **20%**, 주간 **6%** 남음이며 현재 요청은 **0건**이라 자동화는 **PAUSED**이고 실제 제한→재개 전이는 아직 발생하지 않았다.
- 원본 `C:\Codex\Kairos\kairos\SKILL.md`와 설치된 `$kairos`에도 기존 무조건 heartbeat 금지를 새 요청마다 중복 생성하지 않는 규칙으로 고치고, Heimdallr `working` 재개·중복 페이지/발송 방지를 명시했다. 두 경로 모두 `quick_validate.py`에서 `Skill is valid!`.
- 회귀 검증: Kairos 연결 집중 테스트 **10 passed**, `git diff --check` 통과, 로컬 `poll`은 작업 **0건**·수집 오류 없음. 자동화 설정 파일은 `C:\Users\user\.codex\automations\kairos\automation.toml`에서 일시 중지 상태·대상 작업 ID·주기를 확인했다.
