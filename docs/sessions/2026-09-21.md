# 2026-09-21 — 발굴 목록·공시 수주·전망 구분 배포 마무리

## 이어서 완료한 일

- 9월 20일 구현분의 전체 오프라인 회귀를 다시 실행해 **885 passed·1 skipped·3 network deselected**를 확인했다.
- 최종 Next 빌드 **10/10**을 확인했다. 운영 Supabase를 읽는 로컬 화면에서 `/` **HTTP 200·826,954자**, `/stock/000150` **HTTP 200·145,938자**였다.
- 두산 화면의 현재 위치 해설은 발표된 **2026.2Q**를 기준으로 하고, **2026.3Q(E)**는 별도 전망 카드·점선 계열로 분리됨을 확인했다. 공시 주요계약 수주잔고 **105,618.64억원**은 전체 회사 수주잔고가 아니라는 범위 문구와 DART 원문 링크를 함께 표시한다.
- Kairos 원본·설치본 `SKILL.md`와 `deck-structure.md`는 각 파일 쌍 SHA256이 일치하고 `quick_validate.py` **2/2 유효**다. Telegram 신규 기업 요청이 없으므로 분석·Notion·전송은 실행하지 않았다.

## 운영 상태

- 최신 `origin/main`의 9월 21일 08:30 KST 매크로 스냅샷 위에 재배치한 커밋 `7b49145`를 운영 `main`에 푸시했다. GitHub CI run `35594611611`은 깨끗한 설치·테스트·진입점 임포트를 포함해 **38초·성공**했다.
- Vercel 운영 `/`은 **HTTP 200·826,848자**에서 `발굴 목록`·`초기 성장`·미국 소비자물가·비농업 고용·세계경제전망을 확인했다. `/stock/000150`은 **HTTP 200·145,938자**에서 `이번 발표 분기`, `다음 분기 · 컨센서스 전망`, `공시 주요계약 수주잔고`, 원값 `105618.64`를 확인했다.
- 새 수주 파서 완료 표식이 없는 기존 발췌를 예약 수집기가 한 번씩 다시 읽고, 검사 완료 뒤에는 같은 접수번호를 재호출하지 않도록 보강했다. 두산 운영 DB를 다시 저장한 뒤 `공시 수주지표 확인`·`10,561,864백만원`을 재조회했고 일반 수집 대상은 **0건**이었다. 전체 오프라인 **886 passed·1 skipped·3 network deselected**.

## 종목 추천 복구

- 최근 예약 실행을 재조회했다. 9월 16·17일은 기술 조건 충족 **0건**, 9월 18일은 경인양행·고영 **2/2건** 발송 성공이었고 운영 DB에도 두 행이 남아 있었다. 알림 명칭이 `매수 관찰 후보`·`기술 관찰`이라 추천으로 식별하기 어려웠다.
- 개별 메시지를 `오늘의 추천 종목`, 일일 요약을 `오늘의 종목 추천`으로 바꾸고 0건일 때 `조건 충족 추천 0건`을 명시한다. 예약 지연에 대비해 평일 **17:37·18:17·19:07 KST** 3회 기회를 주되 KST 하루 최대 2건과 종목·분기 중복 차단을 유지한다.
- 20:48 KST 운영 재생은 펀더멘털 **135 → 일봉 128/128 → 가격 65 → 5·20일선 15 → MACD 3**이었다. 중복 경인양행 1건을 제외하고 잇츠한불·유니트론텍을 새 제목으로 Telegram에 **2/2건 실제 발송**했고 운영 DB에서 `as_of=20260921` 두 행을 재조회했다. 전체 오프라인 **887 passed·1 skipped·3 network deselected**.

## 매크로·추천 정렬 가독성 개편

- 미국·글로벌 매크로를 단일 서술 상자에서 시장 온도·전망 변수 2개 카드, 4단계 자동 계산 흐름도, 공식 발표 핵심 요약표, 적합 섹터 TOP 카드, 초기 성장 체크리스트, 출처 푸터로 재구성했다.
- `추천 정렬` 문구는 매크로 본문과 종목 수 상태에서 제거하고 목록 표 바로 위에만 **황금색 2xl/3xl 굵은 배너**로 표시한다. 사용자 다중 정렬이 적용되면 같은 배너 안에서 현재 표 상태를 알린다.
- 운영 Supabase를 읽는 로컬 화면에서 모바일 기본 뷰와 데스크톱 1440×1000 뷰를 확인했다. 황금색 배너가 표 바로 위에 1회만 표시되고, 모바일에서는 공식 발표 표가 가로 스크롤되며 나머지 카드는 한 열로 쌓인다. 대상 테스트 **112 passed·1 skipped**, Next 빌드 **10/10**.

## 추천 메시지 펀더멘털 투자 아이디어

- 기술 조건은 추천 시점 선별에 유지하고 `투자 아이디어` 본문은 본업·실적 가속·수익성·가치/PRI·핵심 가설·다음 확인 순으로 분리했다. 종목별 주요 제품과 현재 분기 OPM·OPM YoY/TTM 변화, 투자 매력도, PRI, 공개된 연간 F.PER/F.ROE만 사용한다.
- 흑자전환 종목은 존재하지 않는 영업이익 성장률을 만들지 않고 `매출 가속 + 영업이익 흑자전환`으로 쓴다. 수주·고객·수요 원인이 입력에 없으면 단정하지 않는다.
- 운영 무발송 재생은 **135 후보→128/128 일봉→기술 신호 3**이었다. 잇츠한불은 주요 화장품 제품·OPM **14.8%**·PRI **13.7**, 경인양행은 주요 염료 제품·OPM **11.2%**·PRI **9.4**를 근거로 펀더멘털 가설을 생성했다. Telegram·DB 쓰기는 **0건**이었다.
