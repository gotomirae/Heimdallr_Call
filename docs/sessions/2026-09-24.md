# 2026-09-24 — GPM 전수 보충 마무리·정상 결측 멱등화

## 한 일

- 기존 정밀재무가 완성되고 `gross_profit`만 비어 있는 종목은 CFO·주식수를 다시 수집하지 않고
  현재·직전·전년동기·전년직전의 전체재무 4콜만 사용하도록 `src/finance/detail.py`를 최적화했다.
- 희소 GPM payload 때문에 기존 CFO까지 0건으로 보이던 실행 요약을 GPM 보충 대상과 전체
  정밀수집 대상으로 분리했다. 매출총이익 정상 결측 종목 코드도 경고로 드러낸다.
- DART가 매출총이익을 별도 공시하지 않는 종목의 반복 호출을 막기 위해
  `gross_profit_checked_at` 계약·DDL·마이그레이션 검사를 추가했다. DDL 적용 전에는 42703만
  기존 컬럼 조회로 폴백하며 다른 오류는 다시 올린다.
- 남은 운영 GPM 대상 9종목을 실제 수집·저장했다. 결측을 0으로 바꾸지 않았다.
- 운영 Supabase에 `ret_d40`, `excess_d40`, `gross_profit_checked_at` DDL을 적용하고
  PostgREST 컬럼 검사를 다시 통과시켰다. 정상 결측 9종목에 확인 표식을 저장한 뒤 기본
  정밀재무 실행 대상이 0종목임을 재확인했다.
- 결과 추적 1,086건을 전부 다시 계산·저장해 D+40을 운영 반영했다.
- 대시보드 변경을 GitHub `main`에 푸시했다. 첫 CI에서 KIND 실네트워크 테스트 3개가
  공용 러너 IP 403으로 실패해, 기본 CI를 `not needs_network` 회귀 게이트로 분리했다.
- 원격 매크로 스냅샷 2개 커밋을 rebase로 보존한 뒤 확장 수집기로 2026-09-23 미국 장을
  다시 생성했다. 강제 푸시 없이 `main`을 fast-forward했다.

## 실측

- 최신 게이트 대상 **275종목**, 매출총이익·GPM 측정 **266/275**, DART 원자료 정상 결측
  **9종목**(`003480,017670,038390,064260,064850,079160,181710,225570,403550`).
- 마지막 배치: **9종목·전체재무 36콜·주식수 0콜·예외 0·18행 upsert**.
- 새 최적화 단건 재생: 전체재무 **4콜**, 주식수 **0콜**. 신규/집중 테스트 **27 passed**.
- 전체 오프라인 회귀: **918 passed·1 skipped·3 deselected**.
- Next production build **11/11**. 최초 CI의 코드 회귀는 **912 passed·7 skipped**, KIND
  실네트워크 3개만 403으로 실패했다.
- 수정 CI `35938271341` **30초 성공**. 운영 Vercel 홈 **200·1,079,277B**, SK하이닉스 상세
  **200·261,369B**, Fear & Greed 한국어 페이지 **200·12,602B**를 확인했다. 홈에는 시장 온도·
  Fear & Greed, 상세에는 D+40·동기화 차트·GPM, 번역 페이지에는 시장 의미·공식 원문 링크가
  모두 들어 있었다.
- 운영 마이그레이션 검사: 필수 컬럼 **전부 통과**.
- GPM 표식 저장 후 기본 재실행 대상 **0종목**. 운영 `gross_profit_checked_at` **9행**.
- 결과 추적 **1,086/1,086행 저장**. D-5·D0·D+1·D+5는 **1,086건**, D+20은
  **1,083건**, D+40은 **78건**, D+60은 **0건** 측정. D+40 미측정 1,008건은
  `거래일 부족(아직 이르다)`로 확인했다.

## 막힌 것

- 사용자가 Anthropic 전송과 유료 1건을 명시 승인했다. 생성 1회에 **$0.107092**가 들었지만
  비엄격 tool 출력이 Canonical 구조를 깨뜨려 유효 분석으로 인정하지 않았다. 추가 유료 호출은
  승인한 1건 범위를 넘으므로 하지 않았다. 운영 행은 `invalid=true`, 요청은 `failed`로 표식했다.

## 다음 세션이 알 것

- 이번 Phase의 운영 작업은 완료됐다. 새 분기 보고서가 들어오면 `gross_profit_checked_at`은
  새 `(code, fiscal_year, fiscal_quarter, fs_div)` 행에서 다시 비어 있으므로 정상적으로 재수집된다.
- D+40 표본은 아직 78건뿐이다. PRI D+40 IC **+0.432**는 기대한 음수 방향과 반대지만
  표본이 작으므로 더 쌓인 뒤 판단한다.

## 추가 작업 — Anthropic 안전 경계·매크로 상관관계·공식 일정·섹터 UI

- SK하이닉스 실제 입력을 Anthropic 무료 count 경로로 계측했다: 전체 schema 기준 검색 포함
  **23,059**, 검색 없음 **17,471**토큰. 즉시 분석용 compact strict schema는 전체 발췌를
  유지해도 **14,091토큰**, 800자 최종 폴백은 **12,520토큰**이다.
- 대시보드 worker에 저장 전 전체 스키마 검증을 추가하고 strict 해제 자동 폴백을 금지했다.
  과금된 불완전 응답은 삭제하지 않고 감사 메타와 실패 상태를 보존하며 화면에서 숨긴다(T185).
- 미국 최근 완료 거래일을 09:00 KST까지 재시도한다. Fear & Greed와 나스닥을 이중축으로
  겹치고 일간 변화 상관계수를 표시했다. 2026-09-23 기준 공통 **59일**, `r=+0.804`였다.
- BLS·BEA·FOMC 일정 제목은 공식 문서를 직접 새 탭으로 열고 내부 한국어 핵심 설명을 분리했다.
  중요 일정은 **3개**(10/14 CPI, 10/28 FOMC, 12/09 FOMC+SEP)만 황금 별표로 표시했다.
- 섹터 다중 선택, 새 이름 입력, 종목별 브라우저 저장 편집 UI를 기본 펼침으로 만들었다.
- 전체 오프라인 **925 passed·1 skipped·3 deselected**, Next build **11/11**.
- GitHub CI `35956645943` 성공. 운영 Chrome에서 대시보드의 `r +0.80`·황금 별 3개·
  섹터 편집 UI를 확인하고, 10/14 CPI 클릭이 BLS 공식 일정으로 새 탭 이동하는 것과 내부
  한국어 핵심 설명을 확인했다. SK하이닉스 상세에는 불완전 응답 안전 숨김 상태가 표시된다.

## 추가 작업 — 단일판매·공급계약·엄격 추천 조건

- DART 공시 분류에 `order_contract`를 추가하고 OpenDART 원문에서 계약명·계약금액(원)·최근
  매출 대비·상대방·계약기간을 구조화해 기존 발췌 테이블에 저장했다. 정기보고서 수주잔고와
  수시공시 계약을 화면에서 별도 표로 나란히 표시하고 둘을 전체 신규수주로 합산하지 않는다.
- 최근 3일 DART 772건 중 단일판매·공급계약 **25건**, 규칙 밖 공시명 **0종**을 확인했다.
  운영 구조화 **25/25**, 계약금액 **24건**, 계약명 **9건**, 기재 제한 **1건**이다.
- 추천 게이트에 동일 주체 외국인/기관 3거래일 연속 순매수, 검증된 세부산업 3년 CAGR 15%
  이상, 2분기 매출·영업익 YoY 가속과 OPM 상승, 고점 대비 20% 조정 후 반등/횡보를 추가했다.
  초기 흑전은 영업익 YoY를 만들 수 없어 우회 통과하지 않는다. 알림을 산업 성장·기업 실적·
  투자 아이디어·가격/이평/MACD/RSI·네이버 다음분기·6개월 트리거·3개 링크로 재구성했다.
- 운영 1차 퍼널은 **1,151→275→167→103**이었다. 반도체 섹터 전체에 HBM CAGR을
  적용하면 20종목이 남지만 화학·범용부품까지 섞이는 것을 실측해 폐기했다. 제품 설명에
  `HBM·고대역폭`이 명시된 경우만 성장 프로필을 붙이자 CAGR 후보·최종 추천·발송은 **0건**이었다.
  조건 없음은 정상 완료이며 범용 반도체에 HBM 성장률을 전파하지 않는다.
- 정정공시 요약행 숫자 결합 오류를 실데이터에서 발견해 정확한 라벨 행만 허용했다(T186).
  최대 계약금액은 수정 후 **1,074,219,374,798원**으로 정상 범위였다.
- 전체 오프라인 **931 passed·1 skipped·3 deselected**, KIND 실네트워크 **3 passed**,
  Next production build **11/11**. 텔레그램 발송은 하지 않았다.
