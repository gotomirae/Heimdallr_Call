# 2026-09-10 — 운영 마이그레이션·컨센서스 충전

## 완료한 것

- Supabase 프로젝트 ref `drpxciqkbjlruximqbox`의 SQL Editor에서 ROE·수익률·PRI 2.0
  마이그레이션을 `IF NOT EXISTS`로 적용했다. 결과는 `Success. No rows returned`였다.
- `python -m src.db.check_migration` 실조회에서 필수 컬럼 **34/34**, 선택 컬럼 **1/1**을
  확인했다.
- 전수 컨센서스 수집 첫 실행은 1,160종목을 모두 읽은 뒤 PK 23505로 저장 0건이 됐다.
  한스바이오메드(042520)의 비12월 결산 연간값이 분기값으로 중복 파싱된 것이 원인이었다.
- 결산월에 무관한 연간 블록 판별, 불명확 중복 기간 제외, 100종목 단위 부분 저장을 구현했다.
  042520 실데이터는 2026.3Q 매출 **398억원**, 영업이익 **57억원** 한 건으로 확인했다.

## 검증

- 컨센서스·append-only 저장 회귀 **55 passed**.
- 오프라인 전체 **815 passed · 1 skipped · 3 needs_network deselected**.
- Dashboard production build **10/10 route 성공**.
- 운영 DB를 붙인 production server 실렌더: 발굴 목록 **HTTP 200 · 1,553,846B**,
  롯데정밀화학 상세 **HTTP 200 · 209,844B**. 목록의 ETF 테마·F.ROE와 상세의
  PRI 신뢰도·4축·10분기 표를 응답 HTML에서 확인했다.

## 운영 실행 결과

- 컨센서스: 대상 **1,160종목**, 스냅샷 **1,409행 저장**, 추정치 **476종목 확보**,
  추정기관 2곳 이상 **288건**. 동일 기간 중복 제외 경고는 0건이었다.
- 시세·PRI: `price_snapshots` **1,159행**, KIS **1,159 성공·0 실패**, 네이버 장기 일봉
  **1,159/1,159**, 1·3·6·12개월 및 3·6·12개월 상대수익률 전부 **1,159건**.
- PRI 입력: EVENT **1,066**, REVISION **0**, VALUATION **830**, RELATIVE **1,159**.
  90일 전망 이력이 아직 없어 계산 하한과 신뢰도 기준을 분리했다(T146).
- 스크리닝: `screen_results` **1,150행 저장**, 게이트 통과 275·탈락 811·판정불가 64,
  PRI **1,066/1,150**, 분모 70점 774·50점 292, 등급 ★ 51·○ 91·△ 6.
- 첫 실렌더에서 오늘 `price_snapshots`와 이전 `screen_results.pri_detail`이 어긋난 것을
  발견해 스크리너를 다시 저장했다. 롯데정밀화학은 최종적으로 PRI 2.0 **17.49**,
  측정 분모·신뢰도 **70/100**, `mode=v2`이며 화면에서 legacy 문구가 사라졌다.
- 결과추적: `outcome_tracking` **1,086행 저장**. D−5·D0·D+1·D+5는 각
  **1,086/1,086**, D+20 **370/1,086**, D+60 **0/1,086**(거래일 미도래).
- 유료 LLM 호출·텔레그램 발송·KIS 주문 API 호출은 **0건**이다.

## 막힌 것 / 다음 세션

- 현재 없음.
- 커밋 `c10a0da`를 `origin/main`에 반영했고 GitHub Actions CI
  [34471774495](https://github.com/gotomirae/Heimdallr_Call/actions/runs/34471774495)가 성공했다.
- Vercel 운영 화면 재검증: 발굴 목록 **HTTP 200 · 1,552,291B**, 롯데정밀화학 상세
  **HTTP 200 · 209,856B**. ETF 테마·F.ROE·PRI 신뢰도·4축·10분기 표가 배포본에도 보였고
  legacy PRI 문구는 없었다.

---

## PRI 3.0 — 성장 정당화·상승 원인·성장단가·과열 통합

### 완료한 것

- 기존 실적 발표 반응·전망 괴리·역사 밸류·중기 상대수익을 유지하면서, 12개월 상승 원인,
  3년 내재 성장률 갭, 동일 투자섹터 성장조정 PER, 현재 단기 과열을 합친 8축 PRI로 전환했다.
- 12개월 수익은 `(1+주가)=(1+TTM 순이익)×(1+PER 변화)`로 분해하고, 주가가 오른 경우에만
  로그 기여도로 멀티플 몫을 계산한다. 하락 구간은 존재하지 않는 ‘상승 이유’를 만들지 않는다.
- 내재 성장은 임의 할인율을 넣은 DCF가 아니라 현재 TTM PER이 3년 뒤 자기 9분기 평균 PER로
  정상화되는 데 필요한 연 성장률에서 같은 EPS 기준 컨센서스 성장을 뺀 값이다.
- 성장조정 PER은 동일 투자섹터 유효 표본 5개 이상 중앙값과만 비교한다. 결측 축은 0점이 아니라
  분모에서 제외하고 계산 하한 45·참고용 신뢰도 80을 분리했다.
- 과열은 현재 RSI·5일 수익률·52주 고점 접근만 쓴다. 발표 직후 외국인 5일 수급은 현재값이
  아니어서 제외했고, 화면에 미래 매수자를 예측하는 값이 아님을 명시했다(T147).
- 산식·배점·화면·텔레그램·LLM 입력 설명·설정 상수·ADR 14를 함께 갱신했다.

### 운영 실측

- `screen_results` **1,150행**을 PRI 3.0으로 upsert했다. 축별 측정은 발표 1,066,
  상승 원인 333, 내재 성장 갭 400, 역사 밸류 830, 피어 성장단가 255,
  중기 상대수익 1,149, 단기 과열 1,149, 전망 괴리 0이었다.
- PRI 판정 가능 **800/1,150**, 중앙값 **26.1**. 분모는 45점 255·50점 3·60점 153·
  65점 92·75점 124·80점 50·90점 123이었다. 등급은 ★ 66·○ 85·△ 2·· 70·✕ 2,
  발송등급 151종목이었다. 90일 전망 이력이 아직 없어 전망 괴리 축은 0건이다.
- 고영은 `mode=v3`, PRI **26.78**, 신뢰도 **90/100**으로 저장됐다. 12개월 주가 +89.3%,
  TTM 순이익 +877.0%, 멀티플 몫 0%, 필요 성장 −21.8%, 컨센서스 성장 +24.2%,
  성장 갭 −46.0%p, 합성 과열 0/100이었다.
- 로컬 production 실렌더는 고영 상세 **HTTP 200 · 220,677B**, 설정 **HTTP 200 · 86,170B**.
  요청한 네 제목과 현재 가격 쏠림 주의 문구를 응답 HTML에서 모두 확인했다.

### 검증·안전

- PRI·상수·화면 계약 표적 회귀 **124 passed**, 전체 오프라인 회귀
  **823 passed · 1 skipped · 3 needs_network deselected**, Dashboard build **10/10 route 성공**.
- 로컬 커밋 `0ee1404`까지 완료했다. `main` 원격 푸시와 CI·Vercel 운영 배포는 별도 외부
  변경이므로 명시 승인 전에는 수행하지 않았다.
- LLM 유료 호출·텔레그램 발송·KIS 주문 API 호출은 **0건**이다.
