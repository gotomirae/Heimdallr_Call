# 2026-09-03 — 대시보드·PRI 5축 개편 (DB 적용 대기)

## 한 일

- 분기 실적 차트의 주가 라인을 제거하고 OPM 라인을 추가했다. 별도 실제 주간 종가는 실적 차트의 첫 분기부터 현재까지로 기간을 맞췄다.
- PRI를 사용자 지정 5축(25/25/20/10/20)으로 교체했다. 52주 신고가 대비, 최초 발표일 종가 대비, 과거 최대 9분기 TTM PER 평균 대비, 발표일부터 5거래일 외국인 순매수/거래량, Wilder RSI(14)를 수집·저장·점수화한다. 결측은 분모 제외, 분모 45 미만은 판정 보류다.
- 컨센서스의 모든 소비 경로(스크리너·LLM·텔레그램·대시보드)에 `source=naver`를 강제했다.
- 분기 히스토리에 공개된 손익·성장률·마진·TTM·현금흐름·재무상태·주식수·출처 필드를 모두 표시했다.
- LLM 단계 흐름(잠정 최초 알림/초기 분석 → 정기보고서 확정 분석 자동 업그레이드)을 화면에 명시하고, 핵심 문장 전체를 노란 배경으로 강조했다. 가치와 가격 비교를 필수 카드로 승격했다.
- 텔레그램 링크는 대시보드·네이버증권·StockEasy로 유지했다. 검증된 트리거가 `수혜: 기업명(000000)`을 포함할 때 수혜사별 네이버·StockEasy 직접 링크를 붙인다.

## 실측·검증

- 비네트워크 테스트 `775 passed · 1 skipped · 3 deselected`, Dashboard build 10/10 성공.
- 전체 테스트는 코드 회귀 2건을 수정한 뒤 비네트워크 전부 통과했다. KIND 라이브 3건은 샌드박스 네트워크 차단으로 별도 제외했다.
- KIS/네이버 실호출 3종목 성공: 삼성전자 250,000원, 리노공업 64,300원, HPSP 50,200원. 주문 엔드포인트 2종 차단 확인, 네이버 폴백 성공.
- Supabase 실측: 기존 컬럼은 정상이고 새 PRI 컬럼 12개가 아직 없다. REST에는 SQL 실행 RPC가 없고, 현재 브라우저 Supabase 계정은 프로젝트 ref `drpxciqkbjlruximqbox` 접근 권한이 없어 SQL Editor가 조직 목록으로 되돌아갔다.

## 막힌 것 / 다음 세션

- 사용자가 브라우저에서 Heimdallr Supabase 프로젝트(`drpxciqkbjlruximqbox`)에 접근 가능한 계정으로 로그인해야 한다. 그 직후 `src/db/schema.sql` 증분 ALTER를 적용한다.
- DDL 적용 후 `python -m src.collectors.price_run --save`, `python -m src.screener.run --save`로 전 종목을 충전하고 P1~P5 측정 건수·SC6·등급 수를 실측한다.
- 그 다음 커밋·push, CI/Vercel 10개 라우트 확인, 실제 상세화면 확인 후에만 완료 텔레그램을 보낸다.
