# 2026-09-08 — 최근 10일 증권사 리포트 3단계 마무리

## 이어서 한 일

- Anthropic 서버 검색 오류 블록이 결과 배열이 아닐 때도 Adapter가 유료 응답 뒤 `TypeError`로
  죽지 않도록 방어하고 회귀 테스트를 추가했다.
- 3단계 실행 로그와 Actions 요약에 실제 서버 웹검색 횟수를 표시한다.
- 새 정책이 반영된 현재 코드로 전체 오프라인 테스트와 Dashboard production build를 다시 확인했다.
- `--send` 없는 운영 DB 읽기 전용 dry-run으로 현재 실제 대상 수를 확인했다.

## 실측

- 운영 ★·○ 범위: **149종목**
- 실제 5거래일 창 완료·최근 리포트 웹검색 대상: **0종목**
- 컨센서스 변화 대상: **0종목**
- 조회 시점 비용: **$21.51/$27 · 오늘 10/120**
- 오프라인 테스트: **801 passed · 1 skipped · 3 network deselected**
- Dashboard build: **10/10 routes**
- 유료 LLM·서버 웹검색·DB 쓰기·Telegram 발송: **0건**

현재는 3차 대상이 없으므로 추가 실행을 하지 않았다. 미래 정기보고서의 실제 5번째 KOSPI
거래일에 ★·○ 종목만 한 번 열리고, 그 호출 안에서만 지정 텔레그램 두 곳 우선·최대 3회
웹검색이 수행된다.

---

## 가치·가격 비교와 차트 보강

### 한 일

- 발굴목록에 평가 분기까지 최근 4개 분기 순이익 기준 PER, 네이버/FnGuide F.PER,
  올해 예상 ROE 열과 각 열 정렬을 추가했다. 목록은 기존의 당일 `price_snapshots` 한 번만
  읽으며, 종목별 네이버 호출을 새로 만들지 않았다.
- 네이버 증권이 연결한 FnGuide `cF1002.aspx?frq=0` 표에서 가장 가까운 두 연간 (E)의
  F.PER·올해/내년 ROE를 수집한다. 매일 컨센서스 수집 뒤 오늘 실제 시세가 있는 행에만
  복사하며, LLM은 사용하지 않는다.
- 종목 상세의 가치와 가격 비교를 최근 4Q PER·F.PER·올해→내년 ROE와 섹터 중앙값 차이로
  재구성했다. 섹터 표는 시총·같은 평가 분기 매출·영업이익·OPM·올해/내년 ROE·4Q PER·F.PER를
  함께 표시한다.
- MACD 입력에서 같은 ISO 주의 중복 수집 행을 마지막 실제 거래일 한 점으로 정규화하고,
  0선·MACD·Signal·양/음 히스토그램을 명확히 표시했다. 매출 YoY와 영업이익 YoY는 서로 다른
  독립 축 차트로 분리하고 흑전·적전은 퍼센트를 만들지 않고 라벨로 남겼다.

### 실측

- 네이버/FnGuide 에스엘(005850): 2026E 매출 **5조 6,065억원**, 영업이익 **4,425억원**,
  F.PER **6.61배**, ROE **13.68%** → 2027E **13.40%**.
- 운영 DB 읽기 전용 고영(098460): 원 주간행 **160개**, ISO 주 **158개**. 화면 표시 구간의
  MACD 유효점 **127주**이며 선·신호·히스토그램을 실제 렌더에서 확인했다.
- 고영 분기 차트: 매출 YoY **6/9**, 영업이익 YoY **5/9** 측정. `25.4Q 흑전`은 % 없이 표시했다.
- 발굴목록 실제 화면: 성장 가속 **209종목**에서 4Q PER 열의 값·결측과 F.PER·ROE 신규 열,
  정렬 버튼을 확인했다. 기존 DB에는 신규 ROE 컬럼/값이 없어 F.PER·ROE는 아직 `—`다.
- 회귀: **804 passed · 1 skipped · 3 needs_network deselected**, 핵심 회귀 **63 passed**.
  Dashboard production build **10/10 routes**.
- 유료 LLM 호출·운영 DB 쓰기·Telegram 발송·배포: **0건**.

### 운영 반영 시 할 일

1. `src/db/schema.sql`의 `consensus_snapshots`·`price_snapshots` ROE 3개 증분 DDL을 적용한다.
2. 다음 `universe_daily`의 기존 시세→컨센서스 순서로 한 번 실행하면 F.PER·ROE가 채워진다.
   별도 유료 백필이나 LLM 재분석은 필요 없다.
3. 현재 변경은 로컬에만 있으며 운영 배포는 하지 않았다.
