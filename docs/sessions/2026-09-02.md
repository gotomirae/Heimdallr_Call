# 2026-09-02 — 최종 완성 · 관심 종목 · 비용 · 링크 · L2″ 정밀 재무

## 한 일

- 대시보드 상단의 중복된 `전 종목` 탭과 `/screener` 리다이렉트 경로를 제거했다.
- 발굴 목록 각 행에 ☆/★ 관심 종목 토글을 추가했다. 선택은 브라우저 `localStorage`에 영구 저장되고 `/watchlist`에서 동일한 표·필터로 본다. 이후 게이트 판정이 바뀌어도 종목이 숨지 않게 관심 탭은 게이트 전체를 기본으로 쓴다. `0126Z0` 같은 6자 영숫자 코드도 복원한다(T6).
- `/api/cost`를 1,000행 `range()` 페이징으로 바꾸고 KST 월 기준으로 최근 12개월 실호출 비용·호출 수를 집계했다. 다음 달 예측은 최근 비용 발생 3개월 평균, 이력이 없으면 현재 월 일평균×다음 달 일수를 쓰고 화면에 근거를 같이 표시한다.
- 텔레그램 종목 리포트 링크를 `대시보드 · 네이버증권 · StockEasy`로 고정했다. StockEasy 실제 종목 링크를 열어 `https://stockeasy.intellio.kr/stock-analysis/stock-info/{code}` 형식을 확인했다. 텔레그램 리포트에서 DART 링크는 제거했지만, 대시보드 상세의 접수번호 기반 DART 원문은 유지했다.
- PRD §9 경로 표와 회귀 계약을 현재 동작에 맞게 갱신했다.
- 운영 감사에서 `quarterly_backfill`이 전 종목 3개년 재수집으로 120분에 취소되고 후속 단계가
  전부 건너뛰던 사실을 확인했다. 스케줄은 최근 45일 미반영 공시만 수집하고, 전체 이력은
  수동 `full_history`로 분리했다(T133). 잡 제한은 정밀 수집까지 포함해 180분으로 늘렸다.
- 누락돼 있던 L2″ 정밀 재무 수집기 `src.finance.detail`을 구현했다. 최신 게이트 통과 확정종목만
  `fnlttSinglAcntAll`과 `stockTotqySttus`를 호출하고, 현재 누적−직전 누적으로 분기 CFO를,
  현재 누적+전년연간−전년동기 누적으로 TTM CFO를 만든다. BS·주식수 YoY는 전년 같은 보고서와
  대조한다(T134). 이미 D축이 채워진 종목은 다음 실행에서 건너뛴다.
- LLM 입력에 `ttm_cfo/cfo/capex/fcf/receivables/inventory/shares_*`를 추가하고, 고정된
  “D축 미측정” 문구를 실제 잠정/확정·결측 상태에 맞는 동적 설명으로 바꿨다.

## 실측 · 검증

- 변경 관련 회귀: **138 passed · 1 skipped**.
- 전체 offline: **756 passed · 1 skipped · network 3 deselected** (`--basetemp` 작업 공간 지정).
- Dashboard production build: **10/10 routes 성공**, `/watchlist` 동적 경로 포함.
- 전체 기본 실행은 기존 Windows temp ACL 오염(T118) 13건과 sandbox의 KIND 네트워크 차단 3건으로 실패했다. 작업 공간 `--basetemp`와 network deselect로 변경 전체를 다시 검증했다.
- OpenDART 실측: 삼성전자 2026 반기 CFS의 영업활동현금흐름은 당해 누적
  **145,355,192,000,000원**, 전년 동기 누적 **33,941,002,000,000원**. BS 매출채권 비교값은
  2025년 말 기준이라 전년동기 비교에 쓸 수 없음을 확인했다.
- 표본 10종목: TTM CFO·희석률·CFO·FCF **10/10**, 매출채권 **10/10**, 재고 **9/10**.
- 운영 전체: 기존 샘플 1 + 남은 274종목, 전체재무 **1,099콜**, 주식수 **550콜**, API 오류 0.
  `quarterly_fundamentals` **550행** 저장. 최신 게이트 통과 275종목에서 TTM CFO·희석률·CFO
  **275/275**, 매출채권 **275/275**, 재고 **269/275**, FCF **261/275**. 재고 6건은 게임·건설·
  서비스 기업의 실제 공시 무계정이라 `None` 유지. 자동 재수집 대기 **0종목**.
- 최종 스크리너 **1,150행** 저장: 게이트 통과 275, D1/D2 결측 **0**, D3 결측 **8**,
  조용한 감점 평균 **20.0→6.2점**, 중앙점수 **55.3→67.4**, 발송대상 **37→78종목**,
  SC6 **15/20(75%)**.
- LLM 누락 12종목을 재호출해 **3건 저장·9건 사실 숫자 검증 차단**, 비용은 월
  **$0.83→$1.80**. 이후 발송대상 2종목(코칩·제주반도체)을 1회씩 재시도했으나
  코칩은 16,384 출력토큰 절단($0.1710), 제주반도체는 입력 밖 숫자로 저장 차단했다.
  잘못된 분석 대신 텔레그램의 숫자 리포트 폴백을 유지한다.
- 최종 검증: **770 passed · 1 skipped · needs_network 3 deselected**, Dashboard build
  **10/10 routes**, `git diff --check` 통과. `next lint`는 ESLint 설정이 없어 대화형 설정 화면만
  띄우므로 독립 검증으로 세지 않았다(빌드의 타입 검사·컴파일은 통과).

## 막힌 것 · 다음 세션

- 운영 DB 쓰기는 완료했다. 코칩·제주반도체를 포함한 LLM 9건은 Provider가 사실 숫자를 변형하거나
  출력 상한에서 잘려 품질 게이트가 저장을 막았다. 검증을 낮추지 말고 자동 배치의 새 샘플을 기다린다.
- 관심 종목은 현재 브라우저별 저장이다. 여러 기기 동기화가 필요해지면 사용자 인증과 서버사이드 저장 계약을 먼저 정해야 한다.
